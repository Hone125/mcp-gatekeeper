# 卡点清单

> 这个文件只写**两件事**：卡在哪、以及**照抄哪几行能解开**。
>
> 它存在的理由：一个只有「失败了」三个字的卡点清单，等于把问题原样退回给读的人。
> 所以下面每一节都精确到「复制这一行 / 填这一个值」，并给出**做完之后应该看到什么** ——
> 否则读的人没法判断自己是不是真的解开了。

**先说清楚没被卡住的部分**：协议层、鉴权、SQL 护栏、知识库检索、MCP 握手、
以及全部单元测试都**不需要模型**，已经全部跑通（`python -m pytest -q` →
543 passed，退出码 0）。下面三个卡点里，**卡点 1 只影响「问数据库」那条链路的
真实评测数字**（那部分数字已经跑出来并落盘在 `RESULTS.md`，本机现在只是没凭据再跑一遍），
**卡点 2 已经解除**（远端仓库由你本人建好、push 由本会话执行），
**卡点 3 只在「刚 clone 下来、还没配词表」的机器上出现** —— 它不会让任何东西算错，
只会让三项检查诚实地判 SKIP 而不是假装通过。

---

## 卡点 1：没有可用的模型凭据（影响阶段 4 的真实数字）—— **已跑通一次，但本机现在又不可达**

> **跑通的那一次（2026-09-18）**：靠一份当时配在仓库根 `.env` 里的凭据，
> 四条链路全部跑完并落盘，数字见 `RESULTS.md` 第二节。
> **为什么那一份 `.env` 现在不在仓库里**：本仓有一条发布自检要求工作区里
> **没有** `.env`（理由见 `DECISIONS.md` D-43）。两者不可能同时成立，
> 所以跑完之后把它移到了**仓库之外**的一个文件里 —— 没有删，
> 也没有把它复制进任何产物。**路径刻意不写在这个文件里**：
> 本仓有一条检查不许产物出现绝对路径，写进来等于把作者机器的目录结构带进对外产物。
>
> **本机现在的实测状态（2026-09-18，只看「在不在」，没有打印任何值）**：
>
> | 探测项 | 结果 |
> |---|---|
> | 当前进程里的 `LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL` | 三项**都为空** |
> | 用户级持久环境变量里的 `LLM_*` | **没有** |
> | 机器级持久环境变量里的 `LLM_*` | **没有** |
> | 仓库根 `.env` | **不存在**（`git ls-files` 里只有 `.env.example`） |
>
> 而 `mcp_server/env_file.py` **只读仓库根的那一份 `.env`**，读不到仓库之外的任何文件。
> **所以：现在重跑那四条链路，仍然会以退出码 5 结束** ——
> 想再跑一次，得先按下面「最小动作」把三项设成环境变量（或把 `.env` 放回仓库根，
> 代价是发布自检会有一项红）。
>
> 这不是「卡点没解开」，是**凭据和发布自检这两件事本来就互斥**：
> 前者要求机器上有密钥，后者要求工作区里没有密钥文件。把这条写清楚，
> 是为了让下一个人不必先踩一遍「明明跑通过，怎么现在又不行」。
>
> 下面这一节的内容**原样保留** —— 它描述的是「没有凭据时会发生什么」，
> 而那正好就是本机当下的状态，所以它不只是历史记录。

### 现象

**没有凭据时**，下面四条命令都以退出码 **5** 结束，并各自写出一份未完成声明：

| 命令 | 无凭据时的退出码 | 无凭据时会写出的文件 |
|---|---|---|
| `python experiments/09_text2sql_eval.py` | 5 | `reports/text2sql_NOT_RUN.md`（本机现在不存在） |
| `python experiments/13_text2sql_colloquial.py` | 5 | `reports/colloquial_NOT_RUN.md`（本机现在不存在） |
| `python experiments/21_noise_band.py` | 5 | `reports/noise_band_NOT_RUN.md`（本机现在不存在） |
| `python experiments/12_cost_report.py` | 5 | `reports/cost_report.md`（写明账本为空） |

★ 那三份 `*_NOT_RUN.md` **现在不存在** —— 本机已按本节配过一次凭据、
四条链路都跑通了，跑通时脚本会自己把未完成声明删掉（`09` 的第 404 行就是这件事）。
它们会在**换一台没配凭据的机器**上重新被创建。保留这张表是因为
「跑不成时会发生什么」本身是要交代的，否则别人看到退出码 5 不知道该查哪里。
（今天这一栏里的 `reports/cost_report.md` 是**唯一**一个两种情况下都会写出的文件。）

报错原文是：`缺少环境变量：LLM_API_KEY, LLM_BASE_URL, LLM_MODEL（见 .env.example）`
（出自 `mcp_server/t2sql_core.py` 的 `real_llm()`）。

**这不是代码坏了**，是本机这三项确实为空。本仓库**没有编造任何 EX / token / 成本 /
噪声带数字**去填这个洞 —— 相关报告里写的是「未完成」而不是一个数。

### 解开它：最小动作

> ⚠️ **先看一眼这条，能省你一轮排查**：本仓有一条检查要求工作区里
> **没有** `.env`（哪怕被 gitignore 也一样）。所以下面「复制成 `.env`」这条路
> 会让 `pytest` 有 1 条红、收尾自检第 7 项 FAIL。**那是设计如此，不是坏了**
> （理由见 `DECISIONS.md` D-43）。
>
> - 只想跑评测 → 按下面做，接受那两条红；
> - 想让发布自检也全绿 → 跳过第 1、2 步，直接设环境变量（见下面的方框）。

**第 1 步**，复制模板：

```powershell
Copy-Item .env.example .env
```

**第 2 步**，打开 `.env`，把下面三行填上你自己的值（只有这三行必填）。
**下面这是格式示例，不是推荐值** —— 端点与模型名以你自己服务商的文档为准：

```
LLM_API_KEY=sk-你的密钥
LLM_BASE_URL=https://你的服务商的端点/v1
LLM_MODEL=你的服务商的模型 id
```

`.env` 已被 `.gitignore` 排除，不会被提交；`.env.example` 里也列了几个常见端点。

> 不想建 `.env` 也行 —— 改成设真实环境变量（效果一样，显式环境变量优先级更高）：
>
> ```powershell
> $env:LLM_API_KEY = "sk-你的密钥"
> $env:LLM_BASE_URL = "https://你的服务商的端点/v1"
> $env:LLM_MODEL = "你的服务商的模型 id"
> ```

**第 3 步**，验证（这一步只发一次极短的请求，用来确认凭据通了）：

```powershell
python experiments/09_text2sql_eval.py --limit 1 --versions v3
```

**做完之后应该看到**：命令不再打印 `[未完成] 缺少模型配置`，
而是打印出一道题的评测结果，并且 `reports/usage_ledger.jsonl` 被创建、里面有一行记录。
看到这个就说明凭据通了，接着按下面的顺序把真实数字跑出来：

```powershell
python experiments/09_text2sql_eval.py            # 36 题 × 3 版，先跑这个
python experiments/21_noise_band.py               # 实测噪声带（必须在 20 和 13 之前）
python experiments/13_text2sql_colloquial.py      # 口语化题集 + 与正式问法的配对比较
python experiments/20_pair_test.py                # 提示词版本之间的配对检验
python experiments/12_cost_report.py              # 用量与成本
```

**顺序不能换，理由逐条**：

- `21` 要在 `20` 之前：`20` 读它量出的噪声带，读不到就以退出码 3 结束，
  而**不会**假定噪声带为 0（见 `DECISIONS.md` D-22）。
- `21` 也要在 `13` 之前：`13` 报告里的「配对比较」那一节同样要噪声带，
  缺了它那一节会写成「未完成」。
  （这一条是**实测踩出来的**：先跑 `13` 再跑 `21`，`13` 的报告里
  「配对比较未完成：缺实测噪声带」，得再跑一遍 `13` 才有。）
- `12` 放最后：它读的是前面所有调用攒下来的账本。

### 如果第 3 步报的是别的错

- `402` / 余额不足 → 充值。**别改代码**，这不是 bug（`llm_retry.py` 的注释里写了：
  4xx 立刻抛出、不重试，因为重试一万次结果一样）。
- `401` → 密钥错。
- 连不上 → `LLM_BASE_URL` 写错了，注意结尾的 `/v1`。

### 成本提示

36 题 × 3 版 ≈ 108 次调用起步，重写重试会再多一些（每次重试都单独计一次账）。
成本报告会按实际调用次数和 token 数报出来，金额在没填单价时留空 ——
**金额栏是空的，不等于免费**，那是「没有可引用的单价」（见 `DECISIONS.md` D-25）。

---

## 卡点 2：没有 GitHub 凭据（影响阶段 7 的 push）—— **已解除（2026-09-18）**

> **解除方式**：由你本人在网页上建好空仓库，push 由本会话执行。
> 远端：`https://github.com/Hone125/mcp-guarded-toolkit`（public，建仓时是空壳）。
> **解除后的实测**：`git push -u origin main` 退出码 0；本地 HEAD 与远端
> `refs/heads/main` 同为 `旧编号·A`；推上去 10 个提交。
> 下面这一节**原样保留** —— 它在被解除前的那段时间里是准确的，
> 而「当时卡在哪」和「后来怎么解开的」是两条不同的信息。

### 现象

本机 `GITHUB_TOKEN` / `GH_TOKEN` 为空，没有 `gh` 命令，也没有 `.git-credentials`，
所以**无法用 API 自动创建远端仓库**。当时仓库只有本地 `main` 分支，**没有配置任何远端**。

按本任务的约定，这一步**不会**由我代做：对外推送是不可逆的副作用，
而「猜一个 URL 就去推代码」正是要避免的事。

### 探测记录（2026-09-18 实测，不是回忆）

只看「在不在」，**没有打印任何值**：

| 探测项 | 结果 |
|---|---|
| 环境变量 `GITHUB_TOKEN` | 空 |
| 环境变量 `GH_TOKEN` | 空 |
| 环境变量 `GIT_ASKPASS` | 空（没有非交互式的密码提供者） |
| `gh` 命令 | 不存在 |
| `~/.git-credentials` | 不存在 |
| `~/.config/gh/hosts.yml` | 不存在 |
| `~/.ssh/id_rsa`、`~/.ssh/id_ed25519` | 都不存在 |
| `git remote -v` | 无输出（一个远端都没有） |
| `git config --get credential.helper` | `manager`（只读，未改动） |

**结论**：本地无法完成「建远端 + 推送」。这不是配置写错了，是凭据确实不在本机。

`credential.helper=manager` 意味着首次 `git push` 会**弹窗让你登录** ——
登录这件事只能由你完成，我不经手任何凭据，也不会去改这条全局配置。

### 本地这一侧已经就绪（2026-09-18 实测）

`python tools/check.py` → **4 步全过，退出码 0**（单元测试 / 台账式自检 /
交付物核验 / 收尾自检）。**卡点 2 是唯一挡住推送的东西**：一解开就能直接推，
不需要再改任何代码或文档。

### 解开它：最小动作

**第 1 步**，在 GitHub 网页上建一个**空仓库**（不要勾选 README / .gitignore / LICENSE，
否则首次 push 会因远端已有提交而需要先合并）。假设你建好的地址是
`https://github.com/<你的账号>/mcp-guarded-toolkit.git`。

**第 2 步**，把下面两行里的 `<你的账号>` 换成你的账号，然后逐行执行：

```powershell
git remote add origin https://github.com/<你的账号>/mcp-guarded-toolkit.git
git push -u origin main
```

首次 push 会弹出浏览器或命令行登录（本机 `credential.helper=manager`）。
**登录由你完成** —— 我不经手任何凭据。

**做完之后应该看到**：`git remote -v` 打印出两行 `origin`，
`git status` 显示 `Your branch is up to date with 'origin/main'`。

### 推之前值得自己确认一遍

```powershell
git log --oneline          # 只有本仓库自己的提交，没有别人的历史
git status --short         # 干净
git ls-files | Select-String "\.env$"   # 应当无输出（.env 不该被跟踪）
```

---

## 卡点 3：词表没有随仓库分发（影响收尾自检的第 1~3 项）

> **这一节是给「刚 clone 下来的人」看的。** 你不需要先理解整个设计 ——
> 只要看到 `tools/check.py` 以退出码 **5** 结束、报告里第 1~3 项写着 `SKIP`，
> 那就是遇上了这个卡点，照下面做即可。

### 现象

在一台**没有** `MCP_TOOLKIT_WORDLIST` 环境变量、且仓库**同级目录**下没有词表文件的机器上：

| 命令 | 退出码 | 会看到什么 |
|---|---|---|
| `python tools/check.py` | **5** | 前 3 步照常全过；第 4 步（收尾自检）报 `[未完成] 3 项 SKIP：没有实测依据，不是通过` |
| `python experiments/26_final_selfcheck.py` | **5** | 同上，另在末尾打印一行指向本节 |
| `python experiments/19_verify.py` | 0 | S1 / S2 / S3 三项记 **SKIP**，FAIL 仍是 0（SKIP 不计入 FAIL，也不算通过） |
| `python -m pytest -q` | 0 | 全部通过。**词表相关的用例会自己跳过**，不需要你再做什么 |

★ **这三项判的是 SKIP，不是 PASS。** 这句话是这个卡点存在的全部意义：
「我没法查」和「我查过了，干净」是两回事，报告上必须分得开。

### 为什么词表在仓库外面

被查的那些词，字面量原本就写在这份自检脚本的源码里。于是出现一个**看上去全绿、
其实什么都没证明**的局面：脚本把词表排除在自查范围之外（否则自己查自己，
永远报红），结果就是——这份脚本报「仓库里没有这些名字」，而**这些名字本身
就躺在被排除的那几个文件里**。对一个公开仓库来说，这等于既泄漏了词、又给了
一个假的安心。

修法是：**词表搬出仓库**，脚本里只留结构和装载逻辑。判定口径一个字都没改
（`DECISIONS.md` D-46 记了完整决策与四种被否决的替代做法，`selfcheck.py`
文件头有一张同样的表）。代价就是——**词表现在得由你自己提供**。

### 解开它：最小动作

**第 1 步**，在**仓库的同级目录**下（也就是 `mcp-guarded-toolkit` 这个文件夹的
**外面、挨着它**）新建一个文件，文件名固定为：

```
mcp-guarded-toolkit-wordlist.py
```

**第 2 步**，把下面三行填上你自己的词，存成 UTF-8：

```python
FORBIDDEN_NAMES = ["你自己的项目名", "任何你不想让它出现在公开仓库里的名字"]
MEDICAL_TERMS = ["你所在行业的术语甲", "术语乙"]
FORBIDDEN_NUMBERS = ["任何你不想被写出来的数字"]
```

★ **三个键一个都不能少，一个都不能是空的。** 这不是洁癖：如果只读进来两张表，
第三张就是空的 → 那一类**永远 0 命中** → 报告上写着「通过」，而实际上那一类
根本没在查。所以装载器**要么三张全收，要么一张都不收**，没有中间状态。

**第 3 步**（只有你把文件放在别处时才需要），指给它：

```powershell
$env:MCP_TOOLKIT_WORDLIST = "D:\某处\my-wordlist.py"
```

环境变量优先于同名文件。设了它但那个文件不在 → 报告里会点名是这个环境变量的问题
（不会把你的目录结构抄进报告）。

**第 4 步**，验证：

```powershell
python experiments/26_final_selfcheck.py
```

**做完之后应该看到**：退出码变成 **0**，第 1~3 项那一栏从 `SKIP` 变成 `PASS`。
如果变成 `FAIL` —— 那不是装载器坏了，是**它真的在你的仓库里扫到了你刚填进去的词**，
报告会逐条列出「哪个文件第几行」。那正是这三项该干的活：把东西挪走，或者
承认它就是该公开的、把它从你的词表里删掉。

### 三种「看起来能解开」但被否决的做法

写在 `selfcheck.py` 文件头那张表里，这里只重复结论，省得你自己试一遍：

| 做法 | 为什么不行 |
|---|---|
| 把词表编码一下塞回源码（base64 / 异或 / 反转） | 判定口径一个字没变，但**泄漏也一个字没变** —— 只是从「肉眼能看见」变成了「解开就能看见」。这叫安全剧场。 |
| 干脆把词表删掉 | 那三项就**永远查不出东西**了，等于把一条检查关掉，而不是修好它。 |
| 换一份通用的敏感词表（比如网上的开源词库） | 这份词表要查的是**本仓自己的历史包袱**，通用词库查不到它们，等于换了个不相关的检查。 |
| 把检查放宽，容忍命中 | 判定口径放松是这次最不能做的事：**修的是词表的存放位置，不是判定宽窄。** |

---

## 卡点 4：git 历史里仍然查得到那些字（**本次没有执行任何重写**）

> **这一节是「已知问题 + 三条路 + 我的建议」，不是「我做过什么」。**
> 这次修复只改了**工作区**：`mcp_server/selfcheck.py` 现在干净了，
> 但**修复之前的那些提交原样躺在历史里**，谁 clone 下来跑一句
> `git log -p` 就能看到它们。
>
> ★ **一条路都没有执行。** 重写历史是不可逆的对外副作用，按约定必须由你拍板。

### 实测证据（2026-09-18，本机）

命令（**用仓外那份词表现取模式，不把词逐字写进仓库** ——
写进来就等于在仓库里再造一座明文桥，见 `DECISIONS.md` D-46）：

```powershell
python -c "import subprocess,sys;sys.path.insert(0,'.');
from mcp_server import selfcheck as sc
ents=(*sc.FORBIDDEN_NAMES,*sc.MEDICAL_TERMS,*sc.FORBIDDEN_NUMBERS)
p=subprocess.run(['git','log','--all','-p','--','mcp_server/selfcheck.py'],
                 capture_output=True,text=True,encoding='utf-8',errors='replace').stdout
L=p.splitlines()
print('补丁总行数',len(L));print('命中行数',sum(1 for l in L if any(e in l for e in ents)))
for s in subprocess.run(['git','log','--all','--format=%H','--','mcp_server/selfcheck.py'],
        capture_output=True,text=True).stdout.split():
    b=subprocess.run(['git','show',s+':mcp_server/selfcheck.py'],capture_output=True,
                     text=True,encoding='utf-8',errors='replace').stdout
    print(s[:8], sum(b.count(e) for e in ents))"
```

实测输出（**逐字抄下来，没有加工**）：

| 量 | 实测值 |
|---|---|
| `git log --all -p -- mcp_server/selfcheck.py` 的补丁总行数 | **939** |
| 其中命中词表条目的行数 | **51** |
| 触及这个路径的提交数 | **3** |
| 提交 `旧编号·B`（本次修复）里该文件的命中出现次数 | **0** |
| 提交 `旧编号·C` 里该文件的命中出现次数 | **88** |
| 提交 `旧编号·D` 里该文件的命中出现次数 | **84** |

> **补记（2026-09-18，第三轮）**：上面那组数**又复算了一遍，逐字不变**。
> 另外按任务书点名的那条命令（`git log --all -p -- mcp_server/selfcheck.py`
> 再 `Select-String` 那 6 个模式 —— **不在这里逐字列出**，理由同本节开头：
> 写进来就是在仓库里再造一座明文桥）单独量了一次：
>
> | 量 | 实测值 |
> |---|---|
> | 那条命令的输出总行数（`Measure-Object -Line`） | **999** |
> | 其中命中那 6 个模式的行数 | **20** |
>
> 两个口径都对，差的只是**模式集大小**：上面那张表用的是仓外词表的**全部 50 条**
> （所以命中 51 行），这一条只用任务书点名的 **6 个**（所以 20 行）。
> 两者都 > 0 —— **结论一致：泄漏确实还在历史里，工作区干净不代表历史干净。**
> 行数不同（999 / 939）是两边的计数方式不同（PowerShell 数组逐行 vs Python
> `splitlines()`），不是内容不一致。

也就是说：**泄漏存在于 2 个历史提交里**，一共 51 行。它们和
`reports/leak_fix_verify.md` 里那条负控（`旧编号·E` 那一版命中 50 条 / 出现 88 次）
是同一批字 —— 两条实测互相对得上。

> **补记（2026-09-18）**：负控钉的 sha 从 `旧编号·E` 改成了 **`旧编号·F`**
> （= 修复提交 `旧编号·B` 的**父提交**，也就是「词表还在 `selfcheck.py` 里的
> 最后一版」这句话**真正**成立的那个提交）。
> 改的原因只是注释里的那句话不准确：`旧编号·C` / `旧编号·G` / `旧编号·E` / `旧编号·F`
> 四个提交**根本没有动过这个文件**，blob 全是 `旧编号·H`（22581 字节）。
> 所以**上表里每一个数字都不变**：换 sha 前后量出来都是命中 50 条 / 出现 88 次
> （同一份 blob），`旧编号·B` 仍然是 0。

> **补记（2026-09-18，第四轮）**：这一条**只更新外部前提与残留范围**，
> 判定结论（走丙：不重写历史）**不变**；甲与乙**都不执行**，理由见下。
>
> **① 外部前提变了：那个存量索引仓已转为 private（不具名，详见 `DECISIONS.md` D-07a）。**
> 判别方式与结论：它的仓库页、API、raw 三处全部 **404**，而它的
> `info/refs?service=git-upload-pack` 端点返回 **401** ——
> **401 = 存在但私有，404 = 不存在**，所以是**转私有**，不是被删掉
> （本节不写那个仓名，也不写它的地址，理由同上）。
> 同一时间该账号的**公开仓数量 4 → 3**；本仓 `mcp-guarded-toolkit` 仍是 public，
> 与本地 HEAD 一致。该存量仓的 `forks_count` = `stargazers_count` = **0**。
>
> **由此产生一个可核对的后果：历史残留已经没有了可对照的公开落点。**
> 顺着旧仓名点过去是不可达的，那些旧指标值也随之成为**没有出处的孤立数字**。
> 残留的净增量只剩「本工具层与那个存量项目有渊源」这一句话的暗示。
>
> **② 范围口径说明（两个数不要放在一起看）。**
> 本节上面那张表的口径是**只看 `mcp_server/selfcheck.py` 这一个路径**：
> 补丁 **939** 行 / 命中 **51** 行 / 触及 **3** 个提交。
> 另有**全仓全文件**口径，2026-09-18 新量：
>
> | 全仓全文件口径 | 量 |
> |---|---|
> | diff 层：补丁里能读到敏感串的提交 | **4 个**（`旧编号·I`、`旧编号·D`、`旧编号·F`、`旧编号·B`） |
> | 树层：快照里含敏感串的提交 | **23 个中的 13 个**（`旧编号·B` 起干净） |
> | 命中行按类别 | 公司名 **44** 行、旧仓名 **14** 行、课程名 **13** 行、旧指标值 **36** 行 |
>
> **两张表都对，差的只是范围**（一个路径 vs 全仓全文件）。所以「3」与「13」
> 不是互相打架的两个数，是同一件事在两种范围下的读数 —— **引用时必须带上口径**，
> 光写一个数会被读成另一个口径。
>
> **③ 甲与乙都不执行。** 理由三条（完整版见 `DECISIONS.md` D-53）：
>
> 1. 本仓 **7 个文件**里逐字引用了 **14 个历史提交**的 sha（`PROGRESS.md` 12 行、
>    `BLOCKERS.md` 7 行、`DECISIONS.md`、`experiments/27_leak_fix_verify.py`、
>    `reports/delivery_selfcheck.json` / `.md`、`reports/leak_fix_verify.md`，
>    其中 **3 处**写的是完整 40 位）—— 重写之后，这些引用全部指空。
>    ★ **一处更正**：本节末尾原先那句写的是「sha 引用一共 3 处」，
>    那是只按**完整 40 位**数的；按**全部出现**数，是 **7 个文件 / 14 个提交**。
>    结论方向不变（重写会让这些引用失效），只是原先那个数字偏小。
>    那句原文**照旧留着不改**，由这条补记订正。
> 2. **负控在原理上需要一个「仍含敏感串的历史提交」存在**（它的作用就是证伪
>    「0 命中会不会只是尺子坏了」），而 `experiments/27_leak_fix_verify.py` 的
>    `PRE_FIX_COMMIT` 正是把这个提交当**输入参数**。重写历史会把负控样本一起抹掉 →
>    那个脚本指向一个不存在的对象 → 跑不起来。
> 3. 本节「甲」那一栏里已经写明：收尾自检**第 15 项的判据就是「修复前那个提交还取得到」**，
>    重写后它必然判 FAIL —— 那是它**该**报的，不是它坏了，但结果是**闸门从此常红**。
>
> 一句话：**为了抹掉一句暗示而自断证据链，代价大于收益。**
> 「不重写」是**决定**，不是待办；将来若真要动，先做完 `DECISIONS.md` D-53 里
> 那两步解锁条件，在那之前任何人不许对本仓做历史重写。

> **补记（2026-09-19）：本节引用的编号已整体改写成标签，编号搬去了仓库外面。**
>
> 这一条改的**不是判定**（「不重写历史」照旧成立，本条一个字没松），改的是
> **编号放在哪里**。起因是本节开头自己写过、当时却没有顺着想下去的一点：
> **7 位缩写在托管站点上照样能解析到旧对象**，所以「只写缩写」不构成遮挡 ——
> 那些编号等于**把钥匙挂在墙上**。工作区一直是干净的，而钥匙一直挂在公开文档里。
>
> **一、编号现在长这样。** 本文件与 `PROGRESS.md` / `DECISIONS.md` / `reports/` 里
> 原先逐字写出的编号，一律改写成一个标签 `旧编号·X`（一个对象一个字母）。
> **句子、读数、判定一个字都没改**，改的只是编号的可解析性。原编号连同它所在的
> 原句逐字留档在**仓库外面**（维护者本地那份对照表，与仓名无关，按标签可查）。
>
> **二、本轮实测（口径：仓外临时脚本，对下列 7 个文件扫 `[0-9a-f]{7,40}`，
> 再把同一个对象的 7 位 / 8 位 / 完整写法归到同一个标签上）。**
>
> | 量 | 实测值 |
> |---|---|
> | 涉及的文件 | **7 个**（`PROGRESS.md` 34 处 / `BLOCKERS.md` 19 处 / `experiments/27_leak_fix_verify.py` 8 处 / `DECISIONS.md` 4 处 / `reports/leak_fix_verify.md` 3 处 / `reports/delivery_selfcheck.json` 2 处 / `reports/delivery_selfcheck.md` 2 处） |
> | 编号出现处数 | **72 处**；同一个扫描按「行」数是 **57 行** —— 一行里可以有几个编号，**两个口径都对**，引用时必须带口径 |
> | 不同的对象 | **22 个**：**19 个提交号**、2 个 blob 哈希（也就是上面那句「blob 全是 `旧编号·H`」里的那个，它是**含词表的那一版文件**的哈希，同样是钥匙）、1 个属于**另一个仓库**（所以它在本机对象库里本来就解析不到） |
> | 写法 | 7 位 **51** 处、8 位 **10** 处、12 位 **2** 处、**完整 40 位 9 处** |
> | 搬完之后，仓里还能解析回真实对象的编号 | **0 处**。同一支脚本再扫一遍：形态像编号的串还剩 **62** 处，逐个 `git cat-file -t` **全部解析不到** —— 它们是产物哈希、第三方数据里的十六进制串这一类，**不是提交号，要留着**（上一轮 `DECISIONS.md` D-09 那条「用哈希验证过的来源」靠的就是它们）〔★ 这是当时那一跑的读数；后来写进台账的推送记录与逐字节核对表里又有 5 个串能解析 —— 2 个是**新仓自己的**提交号、3 个是**内容寻址的** blob sha，**能走回旧历史的仍是 0 处**，逐条判见 `PROGRESS.md` 同一天那一节「九」〕 |
>
> ★ **这张表与 ① 里原先那组数对不上，而且我不去猜为什么。**
> ① 写的是「7 个文件 / 14 个提交 / 3 处完整 40 位」（`PROGRESS.md` 12 行、
> `BLOCKERS.md` 7 行）。本轮把**两个口径都算了一遍**（按处 72 / 按行 57），
> 都得不到 14 与 3。那组数是 2026-09-18 量的，**当时的口径没有写全，复算不出来**。
> 按本仓的规矩 —— **复算不出来的读数不许继续引用** —— 那组数**按作废处理**；
> 本条与 `DECISIONS.md` D-53 里凡引用它的地方，一律以上面这张表为准。
> ★ **要强调的是方向没变、而且更重**：重写历史会让这些引用**指空**这件事，
> 在 22 个对象 / 72 处这个规模上比 14 / 3 **更**成立。
>
> **三、判定一个字没松。** 编号搬走之后，「修复前那个提交还在不在」不该再是**唯一**证据，
> 所以负控从一条证据改成两条独立证据（判据逐条对照见 `DECISIONS.md` D-54）：
>
> | 证据 | 怎么判 | 覆盖谁 |
> |---|---|---|
> | **甲：尺子当下可证其灵** | 仓外词表里的样本**只在内存里**过一遍扫描器（不落盘），必须报红 | 任何机器，不需要 git 历史 |
> | **乙：旧历史没被无声抹掉** | 仍按原来的判据读那个对象（命中 > 0 → PASS；命中 = 0 → FAIL） | 维护者本机 |
>
> 第三条分支新加了一条：**本机只有一条初始提交的历史时判「无实测依据」，不判 FAIL**。
> 原先那支 FAIL 的措辞是「历史被重写过」—— 在新克隆的人那里那是**一条不实的严重指控**；
> 而本机**存着**旧历史却取不到那个对象时，**照旧判 FAIL**，措辞一字不改。
>
> ★ **两处更正，原文照旧留着不改，由本条订正**（规矩同上面那条「一处更正」）：
>
> 1. 上面 **②** 里点名的常量 `PRE_FIX_COMMIT` **现在不存在了**。它的输入参数
>    （那个编号）搬去了仓外，`experiments/27_leak_fix_verify.py` 里只剩一个解析函数，
>    **代码里不再有任何写死的编号**。`DECISIONS.md` D-53 里 ③ 处提到这个常量名的地方，
>    同样按这一条订正。
> 2. 由 1 带出的后果，照实说：**② 那条依据现在只剩一半。**
>    以前「取不到那个对象」= 脚本**跑不起来**，这是**硬**后果；现在甲不依赖 git 历史，
>    脚本照样跑得起来、照样有结论。但**②的方向仍然成立**（强度比以前弱）：
>    「旧历史没被无声抹掉」这条证据本身，**仍然要求那个对象在**。
>    也就是说 —— 不重写历史**依然**是这条证据链的前提，只是前提不成立时
>    闸门报的是**这一条**红，而不是整个脚本崩掉。
>
> **四、地址也收成了一处。** 本文件与 `PROGRESS.md` 里那几处写死的远端地址，
> 记的是**当时推到了哪里**，属于事实，**照旧留着不改**；现在程序读地址的地方只有一处
> （`mcp_server/paths.py` 的 `repo_slug()`：环境变量 → `git remote get-url origin` →
> 仓外针脚；两处本地来源互相打架时返回「读不到」，**不猜**）。所以换远端时
> 要改的是**一处代码 + 一处仓外针脚**，不是满仓文档。
>
> **五、搬迁买到什么、不买到什么（照实写，不替它吹）。**
> 买到的是**切断「按编号取回旧明文」这条路**：新仓的历史自一条初始提交起算，
> 文档里再没有一个能解析回旧对象的编号；旧仓转私有之后，顺着编号点过去也取不到东西。
> **买不到**的是本节 ① 里那句话说的东西 —— 工作区**内容一个字节没改**，
> 「本工具层与那个存量项目有渊源」这层**暗示仍在**（那是「走丙」的净增量，
> 本轮没有动它，也不声称动了它）。另外，**已经分发出去的副本不会因此消失**：
> 已经 clone 的、fork 的、缓存过的、被代码搜索索引过的，一次搬迁都收不回来 ——
> 这就是下面「乙」那一栏自己写的警告，本轮同样适用，不因为换了做法就作废。
>
> **六、闸门 4 已通过并执行完毕（2026-09-19 当天）。** 用户新建了一个**空的**新仓、
> 给出地址并明确授权，于是「换远端」这一步做完了：`origin` 已指向新仓，
> 仓外那份留档里的地址**同一口气**改掉（两处不一致会被判「切换做了一半」）。
> 迁移之后重跑闸门与两条自检都是绿的，读数与逐条命令在 `PROGRESS.md`
> 「迁移本身（2026-09-19）」那一节。用的是**普通推送**，一次 `--force` 都没用 ——
> 前提正是「新仓真的是空的」，所以推之前先只读核了一遍（`size == 0` 且提交列表 409）。
>
> ★ **上面 ⑤ 里那句「旧仓转私有之后」现在还没到**：旧仓**没有**转私有、也没有删 ——
> 那是**闸门 5**，等用户。在那之前，「顺着编号点回旧对象」这条路**还没被切断**，
> 本节的判定（① / ⑤ 说的泄漏状态）**照旧成立**，不要因为搬了家就当成已经收工。
>
> **补记（2026-09-19 晚些时候）：上面这一条已经过期 —— 用户把闸门 5 做了，旧仓已转私有。**
> 本机用**匿名请求**（不带 token、不读凭据）实测：旧仓的仓库接口与 raw 直链都是 **404**，
> 按**编号**取对象（7 位与完整 40 位各一个）也是 **404**；同时拿**新仓**当对照，它是 **200**
> —— 对照证明这台机器上「匿名请求」这条路本身是通的，「旧仓 404」不是一个网络问题。
> 逐条读数与那条读数边界写在 `PROGRESS.md` 同一天那一节「十一」。
> **⑤ 说的那种泄漏状态，到此为止。**

### 三条路

| | 做法 | 代价 | 实际效果 |
|---|---|---|---|
| **甲** | 用 `git filter-repo --replace-text` 改写全部历史 + `git push --force` | **本次全部提交的 sha 都会变**。本仓有多处文档逐字引用了 sha（`reports/leak_fix_verify.md` 的负控、本节、`PROGRESS.md`），改完它们全部指空；收尾自检第 15 项**会因此判 FAIL**（它的判据就是「修复前那个提交还取得到」）—— 那是它**该**报的，不是它坏了。另外要装 `filter-repo`（本机没有），表达式文件由仓外词表生成，**不许落进仓库** | 工作区之外的旧对象**不会立刻消失**：GitHub 一侧仍可按 sha 取到悬空对象一段时间，别人已经 clone / fork 的副本永久保留。而且必须 force push（不可逆） |
| **乙** | 删掉远端仓库、重建一个空的、把当前快照重新推上去 | 需要你本人在网页上操作（我没有、也不该有你的凭据）；仓库地址的变化要同步到所有引用它的地方；**修复的过程记录也没了**（历史被压成一个初始提交，「这里曾经有一座明文桥、怎么拆的」这段叙事随之消失） | 和甲一样：**已经存在的副本和缓存不会因此消失**。它买到的是「新访客看不到历史」，不是「数据不存在了」 |
| **丙** | **不处理**，把这件事写清楚（就是本节） | 零 | clone 下来的人跑 `git log -p` 仍然能看到那 51 行。**但它被明确写在这里了**，不是「没人发现所以不算」 |

### 我的建议：**走丙**（不重写历史）

理由按重要性排：

1. **甲和乙都买不到它们声称要买的东西。** 想删的不是「仓库里的一个文件」，
   而是「已经分发出去的一份数据」。分发了就是分发了 —— 已经 clone 的副本、
   fork、CI 缓存、代码搜索索引，都不会因为一次 force push 而回滚。
   花掉「历史被改写」这个不可逆代价，换来的只是「未来的访客看不到」。
2. **泄漏的内容是名字和数字，不是凭据。** 仓库里从来没有过密钥文件或密钥形态的串
   （那是收尾自检第 5、7 项在管的，两项都是 0 命中）。所以这不是「必须立刻止损」的量级 ——
   如果是密钥，我的建议会**反过来**：先轮换密钥，再谈历史。
3. **甲会把这个仓库自己的证据链打断。** 修复前那份文件是可以取出来重跑的负控，
   改完历史它就取不出来了 —— 也就是说，「我们确实验证过泄漏规模」这句话
   将**再也无法自证**，只能靠人相信。
4. 如果那些名字**在合同上确实敏感**，那么正确的动作也不是甲或乙，而是
   **丙 + 去改真正分发它的地方**（对外材料、投递出去的文件），
   并且由你和相关方确认，而不是我一个人在这儿 force push。

**如果你不同意这个建议**：甲和乙的具体命令我可以随时写出来，
但**必须你先说「执行」**，并且在那之前我会把「改完之后哪些文档要同步改」列全
（sha 引用一共 3 处：`reports/leak_fix_verify.md`、本节、`PROGRESS.md`）。

---

## 这一节该长什么样（写给以后的我）

卡点解除之后，**不要删掉本文件**，而是把对应的那一节改成：

```
## 卡点 1：已解除（2026-XX-XX）
解开方式：……（填了两行 .env）
解除后的实测数字：见 reports/text2sql_results.v3.json（尚未生成 —— 这一行由解除后的人填）
```

理由是原任务里那条最常见的违反形态：**「我临时改了一下确认它能跑，随后已恢复」——
恢复之后就什么都没留下**。一个卡点被解开的过程，和它被卡住的那段时间一样，
都是这个仓库应该留下的记录。
