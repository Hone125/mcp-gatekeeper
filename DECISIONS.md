# 技术决策记录

> 这份文件记的是**选了什么 / 为什么 / 放弃了什么**。
>
> 写它的理由很实际：一个决定在三个月后回看，最没用的信息是「当时选了 A」，
> 最有用的信息是「当时为什么没选 B」—— 因为选 B 的诱惑通常还在，
> 而否决它的那个理由（某次实测、某个代价）早就没人记得了。
>
> 每条都尽量带上**依据**：要么是一次实测的数字，要么是一段能重跑的脚本。

设计思想上继承自作者早先的一个个人项目；实现、数据、全部数字都是在本仓库里重新产生的。

---

## D-01 代码按职责拆，不按原型的文件平铺

**选了**：`auth.py` / `guardrails.py` / `db_tools.py` / `kb_tools.py` / `t2sql_core.py` /
`cost.py` / `llm_retry.py` / `server.py` / `paths.py` 各管一件事。

**为什么**：护栏是这套东西里最需要被独立验证的部分。如果它被包在 Text2SQL 状态机里面，
那么「测护栏」就变成了「先起一个 MCP 服务、再准备一份数据、再调一次模型」——
一个本该 50 毫秒跑完的纯函数测试，会变成一条需要网络和 API key 的集成测试。
拆开之后 `test_sql_guardrail.py` 直接 `import guardrails` 就能跑，不碰数据库、不碰模型。

**放弃了**：与原型逐文件对应（便于对照阅读）。换来的是可测性。

---

## D-02 SQL 护栏做三层，而不是一层正则

**选了**：L1 文本校验（本模块）→ L2 `mode=ro` 只读连接 → L3 `sqlite3.set_authorizer` 引擎回调。

**为什么**：正则挡不住变形的写法。L3 不是「以防万一」，它有一个**实测出来的**真实战果：

```
SELECT * FROM pragma_table_info('Track')
```

这句话不含 `PRAGMA` 关键字（`pragma_table_info` 里 `PRAGMA` 后面跟的是下划线，
`\bPRAGMA\b` 匹配不上），不以写关键字开头，也不含 `sqlite_` —— L1 完全放行。
但它执行时 SQLite 会以 `SQLITE_PRAGMA` 动作回调 authorizer，L3 拒绝。
这条现在是一个固定的测试用例，也是「第三层不是装饰」的证据。

**代价（照实记）**：因为 L1/L3 都禁内部表，`list_tables()` / `get_schema()` 没法走
`run_sql`，只能另开一条**只读但无 authorizer** 的内部连接。这个区分写在 `db_tools.py`
的文件头：用户写的 SQL 走三层，工具自己拼的固定语句走内部连接 —— 用户能决定「查哪张表」，
决定不了「查什么语句」。两条路都是 `mode=ro`，写不进去任何东西。

**放弃了**：引入 `sqlparse` 之类的解析器。它只做切词和粗略分类，不构建语义树，
判断不了 `LIMIT` 属于哪层子查询，也判断不了某个标识符是表名还是列名 —— 用了它还得自己写正则，
白多一个依赖。**语义正确性交给引擎**：SQLite 在最权威的位置告诉我们这句话想干什么，解析器只能猜。

---

## D-03 缺 `LIMIT` 自动补，不拒绝

**选了**：没写 `LIMIT` 就补一个 `LIMIT 200`，并在返回体里标 `limit_injected: true`；
显式写了超过上限的 `LIMIT` 才拒绝。

**为什么**：这条规则防的是内存被打满，**不是安全**。拒绝的后果是状态机会为一句话反复重写、
白烧 token，而改写达到了同样的目的。

**留痕方式**：`reports/guardrail_results.md` 里它单独占一行，期望栏写「改写（非拒绝）」，
和「`LIMIT` 超上限 → 拒绝」并排，免得被读成「漏了一条负例」。

---

## D-04 检索用 jieba + FTS5，不用向量库

**选了**：jieba 切词 → SQLite FTS5 倒排索引 → bm25 排序。零外部服务、零费用、可离线。

**为什么**：四条约束同时成立才行 —— 不花钱、不联网、clone 下来能跑、结果确定。
向量检索要调 embedding 接口（要钱、要网、别人 clone 跑不通）；本地向量库要装
`milvus-lite` 之类的重型依赖（本机可用内存约 1 GB）。而「给定关键词找段落」这个任务上
倒排索引本来就够用，且**完全确定**：同一份语料、同一个查询，跑一万遍结果一模一样。
确定性对一份要报数字的评测很重要。

**放弃了**：语义检索（近义词、换一种说法就搜不到）。这是明确的代价，不是没想到。

### D-04a 偏移量只有一个来源

**选了**：`documents` 普通表存原文；FTS5 虚表只存切词后的文本，把
`doc_id/ord/char_start/char_end` 设为 `UNINDEXED` 带在行上。要展示原文时，
一律用 `documents.text[char_start:char_end]` **现场切片**。

**为什么**：存两份正文就有两个真相。构建期和读取期只要对「怎么切、怎么归一化」的理解
差一个字符，返回的就是**一段通顺、合理、但来自别处**的文字 —— 没有异常、没有警告、
退出码 0，只有内容悄悄错了。这类错误在一个「检索演示」里几乎不可能被肉眼发现。
所以坐标只留一份，并用 `tests/test_passage_offsets.py` 做逐字符往返断言钉死。

### D-04b 不在切词列上用 `snippet()` / `highlight()`

**为什么**：它们返回的是**空格分隔的碎词**（那是索引列里的样子），不是可读原文。
用了就会返回「凡 用兵 之道 ， 先察 地形」这种东西。

### D-04c 每个查询词用双引号包起来

**选了**：把用户输入切词后，每个词包成 FTS5 的字符串字面量，再用 `OR` 连接。

**为什么 —— 这是实测的，不是推理的**：拿 44 条敌意输入（空串、裸引号、括号、星号、
反斜杠、`^`、`{}`、`~`、`C++`、`1+1`、`say "hi"` …）逐条真的丢给 FTS5 执行，结论是：

- **只要表达式非空，一条都没报错** —— 引号包裹确实把语法错误消灭干净了；
- 报错的 9 条**全部**是空表达式，错误都是 `fts5: syntax error near ""`。

所以唯一还需要处理的情形就是「一个可检索的词都没有」，那由 `search_passages`
在碰 SQLite **之前**返回 `KB_QUERY_SYNTAX`。这 44 条现在固化在
`tests/test_kb_search.py::test_no_nonempty_expression_is_a_syntax_error` 里。

**顺带发现**：`AND` / `OR` / `NOT` / `NEAR` 被引号包住后会退化成普通词，
即 `数据库 AND` 变成「匹配『数据库』或『AND』」，而不是布尔运算或语法错误。

---

## D-05 分块按行打包，偏移量取自原文行位置

**选了**：把正文按行累积，攒到 ≥500 字符就切一块；`char_start`/`char_end` 直接取
第一行和最后一行的位置。

**为什么**：固定窗口（每 500 字符硬切）会把句子从中间切断，返回的片段读起来是残句。
按行打包既让块边界落在语义单元上，又因为是**取原文位置**而不是**重新计算偏移**，
`text[char_start:char_end]` 逐字符等于块内容这件事是构造上成立的，不是碰巧成立的。

---

## D-06 参数归一不用 `value or default`

**选了**：显式判 `None`，其余一律 `int()`，转不了就报错。

**为什么 —— 这是被测试抓出来的真缺陷**：原先写的是
`top_k = max(1, min(int(top_k or DEFAULT), MAX))` 和 `length = int(length or DEFAULT)`。
`or` 会把 `0` 当成「没传」：

- `top_k=0` → `0 or 5` → 拿到 5 条，而调用方要的是 0 条；
- `length=0` → `0 or 800` → 拿到 800 个字符，而调用方要的是 0 个，**而且不报错**。

调用方看到的返回体完全合法，只是不是它要的。数字接口上的静默替换比崩溃危险得多，
因为崩溃有人报，静默替换只会让下游拿到一个错误的数然后继续算下去。

**修法**：`_as_int(value, default)` 显式判 `None`；`length <= 0` 报 `KB_BAD_RANGE`
而不是替换成默认值。

---

## D-07 卫生检查的排除边界写进测试，而不是默默跳过

**选了**：`tests/test_repo_hygiene.py` 里四类红线扫的范围**各不相同**，边界写在文件头：

| 红线 | 范围 | 理由 |
|---|---|---|
| 识别性名称 | 全仓（含公版正文） | 出现在哪里都是污染 |
| 原领域的专属词 | 只扫自己写的文件 | 古籍正文里出现这类字是文本本身，不是本仓库表达的内容 |
| 被禁数字 | 除第三方原样数据外全部 | 见下 |
| 夸大措辞 | 只扫自己写的文件 | — |

**为什么「第三方原样数据」要排除**：`data/chinook/Chinook_Sqlite.sql` 里实测有
行号和外键值恰好撞上被禁数字。这份文件是逐字节下载的，sha256 记在 `NOTICE.md` 里、
由 `01_build_db.py --verify-against` 核验。**为了绕开一个数字去改它，等于毁掉这份数据
可验证的性质 —— 那比撞车本身糟得多。** 撞车照实记进 `reports/final_selfcheck.md`
（阶段 6 产物，尚未生成）。

**另外**：扫描器自己（`tests/test_repo_hygiene.py`）必须排除 —— 它的词表里逐字写着那些
禁词，扫自己必然全中。那是词表（数据），不是内容。这个排除**只许针对这一个文件**，
`test_the_scanner_excludes_only_itself` 盯着它不许扩大。

**第一次运行就抓到一个真实命中**：我在一条注释里用原领域的词打了个比方。
按规则那属于越界，改掉的是那条注释，不是规则。

### D-07a 禁词表只存在于测试文件里，文档不复述

**选了**：这份文件**不逐字列出**被禁的那些词，需要看权威清单就去读
`tests/test_repo_hygiene.py` 里的 `FORBIDDEN_NAMES` / `MEDICAL_TERMS` / `OVERSTATED` /
`FORBIDDEN_NUMBERS`。

**为什么**：扫描器分不清「使用」和「提及」。一份为了说明规则而把禁词逐字写出来的文档，
在扫描器眼里和一份真的违反规则的文档长得一模一样 —— 上面那三处就是这么被抓出来的。
与其给扫描器开一个「这些地方可以写」的例外（例外一开就守不住了），
不如让**词表只有一个权威副本**，其余地方一律引用。

**代价（照实记）**：读这份文件的人看不到具体是哪些词，得跳一次文件。
这个代价比「例外清单慢慢变长」小得多。

---

## D-08 不替第三方许可条款做解释

**选了**：`NOTICE.md` 与清单里只陈述**脚本实际做了什么**（「移除了页眉页脚」），
不对 Project Gutenberg 的许可条款做任何解释或转述，只给许可原文链接。

**为什么**：原先写的是「Trademark 条款允许在移除品牌标识后自由再分发」——
这是一句替别人做的法律解释，我没有依据。写进产物就是编造。
生成脚本里因此加了一条注释，说明为什么这里只描述动作、不解释条款。

---

## D-09 外部数据的来源 URL 必须用哈希验证过才写进文档

**选了**：`NOTICE.md` 里 Chinook 的下载地址，是用 `03_write_notice.py --verify-source`
把该 URL 取下来算 sha256、与仓库内文件逐字节比对通过之后才写的。

**为什么**：上一轮抓取时没有记录确切 URL。凭印象写一个「大概是这个地址」，
和编造出处没有区别。比对通过（595545 字节，哈希一致）之后它才是事实。
这个检查保留成脚本的一个开关，随时可重跑。

---

## D-10 `NOTICE.md` 由脚本生成，不手写

**选了**：`experiments/03_write_notice.py` 从 `data/kb/manifest.json` 生成
`NOTICE.md`，40 篇的出处表格机器生成。

**为什么**：手抄 40 行必然有错（漏一篇、抄错编号、字符数对不上），
而且**改语料之后它会静静地过期**。生成的话，「文档说的」和「数据是的」不可能不一致。
机器可读的权威是 `manifest.json`，`NOTICE.md` 是给人看的那一份。

---

## D-11 `LICENSE` 的版权署名用仓库的 git 身份

**选了**：`Copyright (c) 2026 lyl`。

**为什么**：这是仓库 `git config user.name` 的值，是唯一有依据的署名。
编一个看起来更正式的名字是凭空造人。

---

## D-12 噪声带实测，不写死常量（阶段 4 执行）

**选了**：`21_noise_band.py` 清缓存后把同一臂连跑两遍，逐题比 `exec_match`，
翻转题数即噪声带，落盘 `reports/noise_band.json`（该文件需模型凭据才能生成，
本仓库尚未生成）；`20_pair_test.py` **从该文件读**。

**为什么**：噪声带如果是一个写在文档里的声明值，那么「差异超出了噪声带」这句话
就没有任何依据 —— 谁也不知道那个声明值是怎么来的。
**若两遍完全一致（噪声带 = 0）**：报告照实写「重复对照未观测到波动，噪声带实测为 0
（n=1 对，不排除更小概率的波动）」，**不许**因此把任何差异说成超出噪声。
措辞纪律的权威清单同样在测试文件里（见 D-07a）。

---

## D-13 重写耗尽后返回完整失败现场

**选了**：状态机三次机会用尽后返回
`{status:"failed", sql: 最后一次SQL, attempts:3, history:[逐次 sql/ok/blocked/reason], error:...}`。

**为什么**：这是个人技术验证项目，暴露失败现场比掩盖有价值。而且这些失败样本
正好是台账里最值钱的负例。

**放弃了**：返回一个干净的 `EXHAUSTED` 错误码（好看，但把信息全丢了）。

---

## D-14 逐版留档，退步版本不删

**选了**：`v1` 自由投影 → `v2` 追加最小投影约束 → `v3` 追加自救提示与列名指引，
每版跑满题集，各落一份 `reports/text2sql_results.v{1,2,3}.json`。

**为什么**：`v1` 天然是投影过度、严格匹配偏低的版本。删掉它，报告就只剩下
「最终版本最好看」这一半事实。**负结果照实报**这条纪律，具体到执行层面就是：
中间版本也要跑、也要落盘、也要进报告。

---

## D-15 护栏单独做成 CLI，让「退出码」成为证据

**选了**：`mcp_server/guardrails.py` 带一个命令行入口，
`python -m mcp_server.guardrails [--raw] [--no-authorizer] [--db 路径] "<SQL>"`，
退出码 0 放行成功 / 1 放行但执行失败 / 2 被拦下 / 3 数据未就绪 / 4 用法错误。

**为什么**：`11_guardrail_test.py` 每条负例都要写「期望 / 实测 / 退出码」。只走进程内
返回值的话，「实测」就是我自己打印的一句话 —— 那是我在给自己的作业打分。走子进程，
退出码由操作系统给出，证据强度不一样。**这是本项目里「证据」二字的执行标准。**

**放弃了**：只做库、让测试直接 import。那样更省事，但报告里最硬的那一栏就没了依据。

---

## D-16 stdout 是协议通道，人话一律走 stderr

**选了**：`server.py` 里 `--check` 模式才走 stdout，真正的服务端模式下一律走 stderr。

**为什么**：stdio 传输下 stdout 就是 JSON-RPC 通道。启动时打一行
`已注册 6 个工具：...` 到 stdout，客户端解析器直接抛 `Invalid JSON`，整个会话崩掉。
**这个 bug 在进程内的所有测试里都看不见** —— 手动跑服务端时人是看着终端的，
根本分不出「stdout 和 stderr 混在一起了」。只有真实 stdio 握手能抓到它。

**放弃了**：靠注释提醒「别往 stdout 写东西」。提醒守不住，`08_mcp_smoke.py` 里
那条 stdout 纯净性检查才守得住。

---

## D-17 `extract_sql` 只负责取出来，合不合法交给护栏判

**选了**：`_RE_STMT` 从「只认 SELECT/WITH」放宽到认得所有语句开头
（含 INSERT/UPDATE/DELETE/DROP/PRAGMA/ATTACH…），取到的语句**一律送交护栏**。

**为什么**：原来的写法有个很隐蔽的后果 —— 模型回一句 `DROP TABLE artist` 时，
`extract_sql` 把它判成 `kind="empty"`（「模型说了句废话」），于是：

1. 护栏**根本没机会看到这条语句**，`R4_FORBIDDEN_KEYWORD` 这条规则在状态机
   这条路径上永远走不到 —— 拦下它的活儿实际上只有护栏的单元测试在做；
2. 回灌给模型的是「请只输出 SELECT 或 WITH」，而模型真正犯的错是「你写了个写操作」，
   它一个字都没被告知，下一轮八成还是错的方向。

职责划清之后：`extract_sql` 取，护栏判。判定权回到该拿它的地方。

**放弃了**：在 `extract_sql` 里直接把写操作标成 `blocked`。那等于把护栏的规则
复制一份到状态机里 —— 两份规则迟早会不一致，而且不一致的地方没人测。

---

## D-18 `table_schema` 与 `schema_doc` 的键名统一为 `name`

**选了**：`table_schema()` 返回体里表名那一格叫 `name`（与 `list_tables` 一致），
并补了 `tests/test_db_tools.py`（这个模块此前**一条测试都没有**）。

**为什么**：原来叫 `table`，而 `schema_doc()` 读的是 `name`。一个键名不一致，
渲染出来的结构里表名全是 `?`。函数不报错、不抛异常、格式还整整齐齐，
只是模型拿不到表名 —— 于是失败现象会指向「模型写的 SQL 表名不对」，
排查方向从一开始就是错的。补测试比修那一行更重要。

**放弃了**：在 `schema_doc` 里写 `t.get("name") or t.get("table")` 兜底。
那种兜底会让同一个错误下次以另一种形式再长出来。

---

## D-19 控制台编码：脚本自己负责，不指望调用方加 `-X utf8`

**选了**：`mcp_server/console.py` 提供 `setup_stdio()`，每个 `experiments/*.py`
和以后的 `tools/check.py` 在 `main()` 开头显式调一次；
`tests/test_repo_hygiene.py` 静态扫描所有入口，新脚本漏掉就红。

**为什么**：在 GBK 控制台上直接跑 `python experiments/11_guardrail_test.py`
（不带 `-X utf8`），41 条用例全部跑过、打印出「41 / 41」，**然后在最后一行打印 ✅ 时
抛 `UnicodeEncodeError`，进程以 1 结束**。调用方看到的结论是「护栏实验失败」。

这类「活干对了、退出码是错的」比崩溃更糟：它教人学会「这个脚本的退出码不用信」，
而退出码正是本仓报告里最硬的一类证据（见 D-15）。

**放弃了「把编码正确性寄托在调用方记得加参数上」。** 这个选择其实已经试过一次了：
子进程调用（`-X utf8`）那一侧是对的，但父进程自己没人管 —— 于是就在父进程这侧炸了。
约定得写在能被强制执行的地方。

### 附：这里有一条**被负控推翻的错误解释**，照实留着

我最初以为崩溃来自 `guardrails.py` 里那句走 stderr 的 ⚠ 警告
（`PYTHONIOENCODING=gbk` 时它编不出来）。照这个想法写了一条回归测试，
并在 `guardrails.py` 里写了一整段解释。

**写完负控（把 `setup_stdio` 打桩成空函数，看那条测试会不会红）之后，它当场被证伪**：
打桩之后 CLI 的退出码仍然是文档里那个码，stderr 里也没有 `UnicodeEncodeError`。
原因是 **Python 的 stderr 默认用 `backslashreplace`**（打不出的字符写成 `⚠` 字面量，
不崩），而 **stdout 默认是 `strict`**（直接抛）。真正踩到的那个坑是
`11_guardrail_test.py` 往 **stdout** 打 ✅。

**处理方式**：错的解释从 `guardrails.py` 里删掉，换成正确的那段；
错误的测试用例删掉，换成一正一负成对的两条
（`test_gbk_stderr_does_not_crash_but_gbk_stdout_does` 与
`test_setup_stdio_saves_a_script_that_prints_an_emoji_on_a_gbk_console`）。

**为什么要专门记一笔**：这条解释当时「读起来很合理」，而且它已经写进了代码注释 ——
如果不做负控，它会一直躺在那儿，等着下一个人照着它去排查一个不存在的问题。
**负控的价值不是让测试变多，是让「我以为的原因」有机会被证伪。**

### 附二：`PYTHONIOENCODING` 的优先级高于 `-X utf8`

同一次排查里撞出来的第二件事：`08_mcp_smoke.py` 在 `PYTHONIOENCODING=gbk` 下会红一条 ——
「stderr 里有没有启动信息」判定为否。**服务端什么都没做错**，错在父进程：
它按 UTF-8 解码，却假定了一个自己没去设定的编码，而外层环境里的 `PYTHONIOENCODING`
会盖过子进程命令行的 `-X utf8`。

修法是 `child_env()`：**要么自己设定编码，要么别假定**。`experiments/08_mcp_smoke.py`
和 `tests/conftest.py` 各有一份（后者给所有子进程用例共用）。
回归口径一并写进 `PROGRESS.md`：三个实验在普通控制台和 GBK 控制台下退出码必须一致。

---

## D-20 评测口径单点收敛：`eval_grading`（纯）+ `eval_runner`（有副作用）

**选了**：所有「怎么算对」的规则放进 `mcp_server/eval_grading.py`（不碰数据库、
不碰模型、不碰文件系统），所有「跑一趟并把结果落盘」的动作放进
`mcp_server/eval_runner.py`；`experiments/09`、`13`、`20`、`21` 只做参数解析与编排。

**为什么**：`09`（36 题 × 3 版）、`13`（口语 vs 正式）、`20`（配对检验）、
`21`（噪声带）四个脚本都要做同样三件事 —— 跑参考解、判一题、描述两版差异。
如果各写一份，就会出现**同一份报告里有两种口径**：比如 `13` 把落空的查询算作错、
`09` 算作「答不了」。那种不一致不会报错，只会让两份报告的数字没法放在一起看。
更要命的是：`experiments/` 里的文件名以数字开头，**以数字开头的模块没法被 import**，
所以写在脚本里的口径只能靠「跑一遍脚本、看输出对不对」来测 —— 那不是测试，那是抽查。

**放弃了**：把口径留在各脚本里（好处是每个脚本自成一个文件、读起来不跳来跳去）。
换来的是「口径有唯一一份、且能被单元测试直接钉住」。

---

## D-21 衡量噪声带的尺子，自己也要有负控

**选了**：`21_noise_band.py --fake stable` 必须量出 **0**；
`--fake jitter`（第二遍故意抖 3 题）必须**正好**量出 **3**。两条都进自检。

**为什么**：噪声带是所有版本差异的判据，它有两种错法，而且**两种都不会自己暴露**：
量大了会把真实差异盖住（现象是「两版一样」，看着很稳健）；
量小了会把随机抖动写成版本差异（现象是「改进了」，看着有成果）。
只测「量出 0」的话，一个永远返回 0 的坏实现照样通过 —— 而它恰好是最危险的那种。

---

## D-22 读不到噪声带时拒绝下结论，绝不假定它为 0

**选了**：`20_pair_test.py` 在 `reports/noise_band.json` 缺失时以退出码 3 结束，
不产出任何结论性文字。

**为什么**：假定噪声带为 0 的后果很具体 —— 任何一点抖动都会被写成「版本差异」，
而读报告的人无从分辨那是不是噪声。「未完成」是一个可以被追问、被补上的状态；
「有一处差异，但没提噪声带」是一个看起来完整、实际上不可核对的状态。

---

## D-23 口语化题集与正式题集共用同一条参考 SQL

**选了**：`data/eval/questions_colloquial.json` 的每条 `ref_sql` 由脚本从
`data/eval/questions.json` **程序化抄写**，并用 `same_as` 指回正式题号；
`13 --check-questions` 会逐条断言两份 `ref_sql` **逐字相同**。

**为什么**：这个实验是仓库里唯一一个单变量对照 —— 只改措辞，不改数据、不改期望答案。
如果参考 SQL 是手抄的，两条 SQL 可能差一个列名或一个排序，那么测出来的就不是
「措辞的影响」，而是「我抄错了」。而 `same_as` 让配对比较有了对齐依据：
不用它，两臂的题号对不上，`pair_compare()` 会直接报错（掉题不是答错）。

---

## D-24 夸大措辞强制分两层：静态扫「给人读的成品」+ 运行时查真实产物

**选了**：`tests/test_repo_hygiene.py` 的静态扫描只覆盖**成品**（`reports/**` 与根目录的
结果文档）；程序生成的报告由 `eval_runner.write_report()` 在**真实产物**上全文查一遍。

**为什么**：这条规则一开始是拿「所有代码 + 文档」去扫的，结果是 **26 处命中，
全部是我自己写的执行机制** —— `eval_grading.BANNED_WORDS` 那个元组本身、
禁止这些词的说明文字、以及「断言能抓住它们」的测试。**词表和它的说明必然要写出这些词**，
扫它们等于在扫自己的执法记录。
处置是**收窄范围**而不是删词表：删掉词表就没有强制了，收窄范围只是把火力对准真正的
读者。代价是「程序生成的报告不在静态扫描范围内」，所以补上运行时那一道 ——
而且运行时那一道落在**真的会被提交的文件**上，比静态扫描更接近要害。
配套补了两条负控：`test_the_reader_prose_scope_is_not_empty`（防止范围被收窄到空集，
那样这条检查会永远通过）和 `test_the_overstated_scan_would_actually_catch_a_report`
（往假仓库里放一份含禁用词的报告，断言它真能报出来）。

**放弃了**：让词表只以变量形式存在、说明文字里不出现这些词。
那会为了迁就一条检查而把注释写得更差。

---

## D-25 不自动抓单价：金额允许为空，但绝不允许填 0

**选了**：金额需要单价，单价需要出处（价目页 URL + 抓取日期）。抓不到就不填，
报告里写 `—` 并写明原因；`cost.money()` **绝不返回 0**，也绝不用别家模型的单价兜底。
`BUILTIN_PRICES == {}` 由测试断言钉住。

**为什么**：一条有出处、需要人填的单价，比一条自动抓来但没人看过的数字可信。
自动抓价还会静默过期 —— 价目页改了，报告里的金额仍然是个数，且没有任何东西会提醒。
把 `BUILTIN_PRICES` 钉成空字典，是为了防止以后有人「好心」加几个单价进去：
那会让这个仓库凭空长出一份没人维护、也没人知道何时过期的价目表。

---

## D-26 报告里一律写仓库相对路径

**选了**：`12_cost_report.py` 用 `_rel()` 把账本路径转成相对仓库根的路径再写进报告，
`tests/test_repo_hygiene.py` 有一条检查扫绝对路径。

**为什么**：绝对路径会把作者机器的目录结构带进对外产物，别人 clone 下来看到的
是一串和他无关的东西，而且这条信息没有任何用处。这条检查是真的会红的 ——
它第一次跑出来就是红在我自己写的成本报告上（详见 `PROGRESS.md` 阶段 4 明细）。

---

## D-27 自检产物留在 `reports/selftest_templates/`，并配一份说明

**选了**：`13 --selftest` 把两个报告模板分支渲染到 `reports/selftest_templates/`，
目录里放一份 `README.md` 说明这些是假数据渲染结果；**不是**渲染完就删掉。

**为什么**：报告生成这条路必须能在**不碰 `reports/`** 的前提下被走通
（否则要么不测它、模板里的错字没人发现，要么拿假数据往真目录里写、真报告可能被污染）。
渲染到临时目录解决了污染问题，但留下一个新问题：clone 的人看不到任何证据。
留在仓库里的代价是「有人可能把这些数字当成真的」，所以配一份 README 说明它们的身份。

---

## D-28 `.env` 只在 LLM 链路上读 —— 并且这条链路必须真的存在

**选了**：`mcp_server/env_file.py` 读仓库根的 `.env`，**只有 `t2sql_core.real_llm()`
调它**；已存在的环境变量一律不覆盖。`.env.example` 里写明只有 LLM 那一节从 `.env` 读。

**为什么**：这条链路**原本是断的** —— `.env.example` 第一行写着「复制成 `.env` 再填值」，
`requirements.txt` 声明了 `python-dotenv` 并注明「只在需要 LLM 的链路上用到」，
但没有任何一处代码读过 `.env`。照文档做的人会填一个谁也不看的文件，
然后拿到「缺少环境变量 `LLM_API_KEY`」，再回头怀疑自己填错了。
**这类「文档说了、代码没做」的坑，坏处不在于修起来难（就是十几行），
而在于它把责任推给了照做的人。**

放在 `real_llm()` 里而不是各脚本的 `main()` 里：一处代码同时覆盖「命令行脚本」
和「长期运行的 MCP 服务端」两种入口，谁都不会漏，也没有启动顺序问题。

**放弃了**：让 `.env` 也能改 `MCP_JWT_SECRET` / `MCP_TOOLKIT_DATA_ROOT`。
理由是安全而不是省事：那些键决定「怎么验签」和「读哪个目录」，
让一个凭据文件能把数据根指到别处是件危险的事。所以 `.env.example` 里
其余各节明确写了「请用真实环境变量设置」。

回归口径是**成对**的两条用例：两处都没有凭据时必须报缺（负控），
**只有 `.env` 里有凭据时必须真的走到模型调用**（正控，断言模型名确实来自 `.env`）。
只写后者，一个「把凭据吞掉然后静默返回空串」的实现也能通过；
只写前者，这条链路等于没测。

---

## D-29 自检的判定逻辑单独成模块：`selfcheck`（纯）+ `19_verify`（有副作用）

**选了**：把「扫什么、怎么算命中、命中之后报告里写什么」全部放进
`mcp_server/selfcheck.py`；`experiments/19_verify.py` 只负责起子进程、收退出码、
渲染报告并落盘。

**为什么**：`experiments/` 下的文件名以数字开头，**以数字开头的模块没法 import**。
判定逻辑留在 `19_verify.py` 里，就只能靠「跑一遍 19、看它绿不绿」来测 ——
而这块代码本身就是「判定别人」的那段，它判错的样子不是崩溃，是**一张全绿的表**。

**放弃了**：让 `19_verify.py` 自己 import 自己（`importlib` 按路径加载）。
能跑，但读的人要额外理解一层加载机制，而收益只是少建一个文件。

---

## D-30 掩码 vs 排除：报告不能成为它自己报告的污染

**选了**：两种处置按「引用是为了什么」分开 ——

| 情形 | 处置 |
|---|---|
| 报告只需要指出**位置** | 掩码：`selfcheck.mask_all()` → `〔已隐去〕` |
| 报告必须显示**路径或模式**才有用 | 把**生成物**排除出它自己喂的那条检查（`deliverable_kit.SELF_REPORT`） |

并且两种处置都不靠自觉：`scan_report_carrier()` 在写盘前扫一遍**即将成为报告**的文本；
`19_verify.py` 的 S9 项在渲染之后回读一遍、把结论写回报告再重渲染，迭代到不动点。

**为什么**：这是本仓库踩得最深的一个坑，同一种回路出现过**三次**——
① `reports/verify.md` 逐字引用命中行，自己成了新的载体；
② 某条检查的**标题**（那个不巧的措辞）本身就在词表里，于是这条检查自己就是一处命中；
③ `reports/deliverable_check.md` 引用自己的命中，每轮多套一层反引号（实测长过三代）。

**代价要说清**：掩码之后的引用是**不完整**的。所以尾部被掩码时
（`verify_kit.tail_for_report`）会补一句「引用前已隐去敏感形态」——
一份读起来完整、实际被剪过的证据，比明说剪过更糟。

**连带修掉的根因**：脚本把绝对路径打在自己的 stdout 里
（`print(f"报告：{REPORTS / 'x.md'}")`），被抄进报告后报告就成了那个路径的载体。
根因修法是 `paths.rel()`：**落盘用绝对路径，打印用相对路径**。

---

## D-31 三类核验各占一个文件：`verify_kit` / `deliverable_kit` / `tools/check.py`

**选了**：三个东西分开，因为它们回答的问题不同 ——

| 文件 | 回答的问题 | 读者 |
|---|---|---|
| `experiments/19_verify.py` | 每个入口脚本的**实际退出码**对不对得上声明 | 想知道细节的人 |
| `experiments/25_deliverable_check.py` | 文档里写的和仓库里**对不对得上** | 照着文档做的人 |
| `tools/check.py` | 现在能不能交出去（一个数字） | 决定发布的那一刻 |

**为什么**：台账必须跑完全部、把失败也列出来；闸门只要一个退出码，遇到第一个
不过的就停下。合成一个脚本会得到一个「跑得很久、输出很长、但没人读」的东西。

**`tools/check.py` 不进退出码台账**：它自己会跑 `19_verify.py`，放进去就成了
19 跑 check、check 又跑 19。它由 `tests/test_verify_kit.py` 里那条 `--list`
的子进程用例守着。

**闸门对「未完成」也给非零退出码**：闸门只认「全都真的过了」。但它会把
「失败」和「未完成」分开打印 —— 两者处理方式不同，一个是改代码，一个是配凭据。

---

## D-32 文档命令的核验只到「字符串在源码里出现过」

**选了**：G4 扫文档里每条 `python <脚本>.py --flag`，脚本文件要存在、
`--flag` 要在**源码文本**里出现过；带 `<占位符>` 或通配符的当模板跳过。

**为什么**：不 import 脚本（数字开头的文件名 import 不进来），也不真的执行
（文档里的命令可能很贵、甚至需要凭据）。所以这条检查的强度是有限的，
**这个限度写进了报告**：它不保证 `argparse` 真的注册了那个参数。

**为什么占位符要跳过**：一个「怎么写一条命令」的示范本身会变成一处违规 ——
那样连在文档里教别人怎么用都不敢写。

**它的来历**：文档曾写着「把 `.env.example` 复制成 `.env`」，
而当时没有任何代码读过 `.env`。照着做的人填完值，程序说凭据没配。
这件事不是 G4 抓到的（那时 G4 还不存在），但 G4 就是为了这一类事写的：
**照文档做却失败，责任不该落到照做的人头上。**

---

## D-33 收尾自检单独成脚本，并接受「闸门跑两遍 19」的代价

**选了**：`experiments/26_final_selfcheck.py` 自己再跑一次 `pytest` 与 `19_verify.py`，
产出 `reports/final_selfcheck.md`；它**不进** `19_verify.py` 的退出码台账，
而是挂在 `tools/check.py` 链子的末尾。

**为什么不让它引用别人算好的结论**：一份「读 `reports/verify.md` 然后抄结论」的报告，
在 `reports/verify.md` 过期时会给出一份看起来完整、实际不成立的结论。
9 项里有两项本来就是退出码，退出码只能靠真跑一次拿到。

**代价**：整个闸门会起两次 `19_verify.py`，多花一分钟左右。**这个代价写在脚本文件头**，
不藏着 —— 「为什么这么慢」是读的人第一个会问的问题。

**为什么它唯一的守门人是那条 `--list` 用例**：一个不在台账里、又没人跑的脚本，
坏掉的样子不是报错，是**静默地不存在**。`tests/test_verify_kit.py` 里
`test_the_gate_chain_lists_its_steps_without_running_them` 断言链条里必须看得见它。

---

## D-34 「未完成」这个状态必须由脚本自己声明，闸门不替它猜

**选了**：`tools/check.py` 只认脚本文件头声明的 `pending_exit`；**没有任何一步**
现在带这个字段（需要模型凭据的三条链路在 `19_verify.py` 的台账里，不在闸门里）。

**为什么**：让闸门自己判断「这个非零码算不算合理」，等于把「什么叫合理地不通过」
这个判断权交给最不该有它的一方。谁都可以给自己的脚本加一条豁免，
所以豁免必须写在脚本文件头、和 `BLOCKERS.md` 里的最小动作一一对应，别人读得到。

**代价**：25 在阶段 6 之前挂过 `alt`（退出码 5 = README 尚未创建），README 落地时
**必须有人记得把它摘掉**。这一条没有机械检查守着 —— 记账在这里：
`verify_kit.EXIT_LEDGER` 里那条注释写着「这条不许再挂 alt」。

---

## D-35 退出码空间不能撞车：用法错误用 90，不用 4

**选了**：`tools/check.py` 的「用法错误」退出码从 4 改成 **90**。

**为什么**：加了第 4 步（收尾自检）之后，4 就同时表示「用法错误」和「卡在第 4 步」。
**一个会和正常结果撞车的错误码比没有错误码更糟** —— 读到 4 的人会去查收尾自检，
而真正的问题是他把参数敲错了。断言写成「用法码不能落在 1..N 里」，
以后加步骤也不用再改（`tests/test_verify_kit.py`）。

---

## D-36 文档里的数字只从脚本输出里抄，改完代码就重抄一遍

**选了**：`RESULTS.md` 与 `PROGRESS.md` 里的每个数字都注明「脚本 + 口径」，
并且每次跑完验证**重新抄一遍**（这一轮就改了两次：单元测试 511 → 513、
台账 SKIP 6 → 4）。

**为什么**：这类数字没有机械检查能兜住 —— 「这个数字对不对」只有重跑才知道，
而机械检查只能查「有没有口径」。所以唯一的办法是把「重抄」定成收尾动作的一部分，
而不是指望它自己保持正确。

**这一轮的真实教训**：`README.md` 第一轮就被 G6 红了一条（引用了当时还不存在的
`RESULTS.md`）。写文档的人最容易高估「我已经写了」—— 所以这条检查不放过
「文档里点名的文件必须真的在」，而处置方式是**把文件补齐**，不是放宽检查。

---

## D-37 停在推送这一步：不猜 URL、不碰全局配置、不代做对外副作用

**选了**：阶段 7 只做到「本地全绿 + 一份照着做就能接上的交接单」，**不做 push**。

**为什么三条路都不通**（2026-09-18 实测，只判在不在、不打印值）：
`GITHUB_TOKEN` / `GH_TOKEN` / `GIT_ASKPASS` 三个环境变量都是空，没有 `gh`，
没有 `.git-credentials`，没有 `~/.config/gh/hosts.yml`，没有 SSH 私钥。
API 要令牌、网页要浏览器登录、SSH 要密钥 —— 三条都没有。这是**停手条件 c**。

**为什么不去猜一个远端地址**：地址里含账号名。猜错了就是往一个不属于这个项目的
地方推代码 —— 对外、不可逆，正是红线要防的事。本任务的约定写得很直白：
「不要猜一个 URL 就去推代码」。

**为什么不去改全局 git 配置**：`credential.helper=manager` 会让首次 push 弹窗登录，
有人会想「先把它换成别的、或者先写个凭据文件绕过弹窗」。但**推送这件事本身
不需要动任何全局设置** —— 弹窗登录是正常路径，不是障碍。没有收益的改动，
在「全局配置只读」这条约束下就是纯风险。

**代价**：仓库现在停在「差最后一跳」。这个代价靠 `BLOCKERS.md` 卡点 2 补偿 ——
它的判据是**「照着做的人需不需要回来问我」**：不需要。有两行可直接复制的命令、
有「做完应该看到什么」，还有一张 2026-09-18 的探测表，说明这不是「我懒得弄」，
是真的没有凭据。

**没有放弃的东西**：本地那一侧的准备工作一件没省 —— `tools/check.py` 4 步全过，
解开卡点后**不需要再改任何代码或文档**就能直接推。

---

## D-38 落盘产物里不许有时钟读数

**选了**：`reports/` 下的产物一律不写耗时；引用子进程输出时把耗时掩码掉。

**问题是怎么暴露的**：`tools/check.py` 一跑完，`git status` 就有十来个文件显示
「已修改」，而它们的内容并没有任何实质变化。原因是三类东西：

| 来源 | 长什么样 |
|---|---|
| `pytest` 的尾巴 | 「N passed in …s」——**这里故意不写真实秒数** |
| 评测脚本每题的进度行 | `calls=1 0.02s` |
| 冒烟测试的超时用例 | 「…秒后失败：WouldBlock」 |

（上面第一行原本照抄了当时那次的真实输出，结果**那段输出里的测试计数恰好命中
禁用数字表**，静态检查当场报红。示例本身变成违规样本 —— 所以这里改成占位形式。）

**为什么这不是「难看」而是缺陷**：`git status` 是「有没有人改过东西」的信号。
它一脏，就再也不能用来回答「这份报告是不是刚跑出来的」——
而**「跑完闸门工作区还是干净的」是这个仓库最能自证的一句话**。

**修法分两层，缺一层都会复发**：

1. **别写**：`eval_runner` 的逐题记录不再存 `seconds`（耗时照旧打印给人看）；
   `08_mcp_smoke` 的超时用例不再把秒数抄进 `got`，改成把「**真的等了**」写成断言。
2. **引用时掩码**：子进程的 stdout 是任意文本，它可能带耗时。掩码收在
   `verify_kit.tail_for_report()` 一处 —— 原来只在 `judge_exit` 里掩码，
   于是不走台账的 `P1`（自己拼 `退出码 {got} | {tail}`）把 `pytest` 的耗时抄了进去。
   19 与 26 的 `_run` 都改成调它，一处收口，没有下一个漏的。

**只去小数**：`\d+\.\d+s` 而不是 `\d+s`。整数秒是**声明过的阈值**
（`read_timeout_seconds=20s`、`HARD_TIMEOUT=30s`），抹掉它等于丢掉报告本来要说的话。

**代价与判据**：给闸门加了 `--snapshot`，跑前跑后各取一次 `reports/` 的聚合哈希；
两次相同 = 逐字节可复现。这句话因此**可重跑**，而不是靠我说。
另外 `tests/test_repo_hygiene.py` 里有一条静态扫描守着它（含负控：
真放一个读数进去必须被抓到、整数秒不许误伤）。

---

## D-39 期望栏写着一条从来没被满足过的判据

**问题**：`08_mcp_smoke.py` 的超时用例，期望栏写「5 秒内失败并给出原因」，
实测一直是 **7.0 秒**，而结果是 ✅。判据是 `dt < 3 × 硬超时`（15 秒），
所以这条假期望从来没红过。

**为什么它危险**：读报告的人看到「期望 5 秒 / 实测 7.0 秒 / ✅」，
要么以为自己看错了，要么以为这个 ✅ 不可信。**两者都比这行字本身更糟** ——
它会动摇读者对整张表的信任。

**为什么实测会是 7.0 秒**：`dt` 从**起子进程之前**开始计时，
所以它天然比 5 秒的硬超时大一截（起进程 + 等待）。判据本来就该按这个口径写。

**处置**：期望栏改成真实契约 ——「等到硬超时（5s）**之后**、15s **之内**失败」，
并且**把下界也变成断言**。这条用例的重点本来就是「**真的等了**」：
只判上界的话，一个根本没等待、立刻抛错的实现同样能通过，
那就等于没测到超时机制。

**顺带记一条更一般的教训**：**一个宽到不会失败的判据，和一个错的期望栏，
合起来能装成一条通过的检查。** 修它的办法不是把期望栏改宽，而是把下界补上。

---

## D-40 截断必须发生在掩码之后（顺序错了会静默漏路径）

**选了**：把截断收进 `verify_kit.tail_for_report(tail, limit=...)`，顺序固定为
「先掩码、再截断」，并且**给那句「已隐去」的说明留出位置**。
调用方不许再在外面写 `[:300]`。

**为什么 `f(x)[:300]` 和 `f(x, limit=300)` 不等价**（实测复现）：

```
截到 275 字符：先截后掩 残留=[]         ｜ 先掩后截 残留=[]
截到 262 字符：先截后掩 残留=['C:\Users'] ｜ 先掩后截 残留=[]
截到 258 字符：先截后掩 残留=['C:\']      ｜ 先掩后截 残留=[]
```

**先截断会把一条绝对路径从中间切断**，剩下半截 `C:\Users`。半截路径
掩码认不出来（正则要更多分隔符），**检查也认不出来**（就是同一条正则）——
于是它安安静静躺在报告里，谁都不会报。这属于「漏了，而且漏得没有痕迹」，
比报红难查得多。

**为什么说明要占位置**：说明被截掉的话，就出现了「一份读起来完整、
实际被剪过的引用」—— 这正是 `tail_for_report` 一开始要避免的那种更糟的情况。

**它的守卫**：`tests/test_verify_kit.py::test_a_path_split_by_truncation_never_leaks_a_fragment`
断言的是「**文本里没有那个片段**」，而不是「检查通过」—— 因为这里的失败形态
恰恰是检查看不见。

---

## D-41 闸门红了要说出是哪一条红（这一轮它没说）

**问题**：闸门第 1 步失败时，尾部只留最后 3 行。`pytest -q` 的失败摘要长这样：

```
===== short test summary info =====
FAILED tests/x.py::test_y - AssertionError
1 failed, 519 passed in 22.90s
```

`FAILED` 那一行**正好卡在边界上**，有时留下、有时被切掉。本轮就赶上被切掉，
于是「有一条测试红了」这件事**没有指名**，我只能靠再跑一遍去看它绿没绿。

**处置**：`tools/check.py` 的 `_run` 除了最后几行，**专门把 `FAILED` / `ERROR`
开头的行捞回来**（最多 5 行）。

**这一条没有做成「已解决」**：那轮到底是哪条测试红的，我没有查到 ——
只能确定它在 19 重新生成 `reports/verify.md` 之后转绿，所以读的应该是那份报告。
**没查到就写没查到**，不编一个解释（原始记录见 `PROGRESS.md` 阶段 7）。
**→ 后来查到了，见 D-42。**

---

## D-42 那次「查不出来的红」查到了：环境的删除钩子，不是测试失败

**结论**：D-41 里悬着的那条红，原因是**本机运行环境里的一个安全钩子**拦下了
pytest 清理临时目录的批量删除，pytest 因此以退出码 1 结束。
**没有任何测试失败** —— 这也正是当时怎么也找不到 `FAILED` 行的原因。

### 证据（全部是实测，不是推理）

| # | 观测 | 怎么看到的 |
|---|---|---|
| 1 | 失败记录尾部**没有** `FAILED` / `ERROR` 测试名，只有一段钩子消息 | `reports/verify.md` 那个未提交的工作区版本，P1 行 |
| 2 | 钩子消息形如 `[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED] {"count":523,"threshold":50,"scope":"turn","targets":["〔作者机器路径已隐去〕…` | 同上（引用前已把路径隐去，见下） |
| 3 | 消息里那个路径确实存在：`…/AppData/Local/Temp/pytest-of-<用户名>`，共 **3157 个文件** | `find … \| wc -l` |
| 4 | 目录下躺着 **5 个 `garbage-*`**。pytest 每次运行只留 3 个 `pytest-NNN`，更老的会被改名成 `garbage-*` 再删掉 —— 它们**没被删掉** | `ls` |
| 5 | 每个 `pytest-NNN` 里约 **397 个文件**（`tmp_path` 类用例写进去的） | 同上逐目录计数 |
| 6 | 现在重跑 `python -m pytest -q` → **退出码 0，521 passed，没有钩子消息** | 本次实测 |

第 4 条是这套证据里最硬的一条：**「删除一直在失败」留下了堆积物**。
一次偶发的拦截不会留下 5 个没被清掉的目录。

### 推断（标为假设，未验证）

触发条件**推测**是「一个回合内被删除的文件数越过阈值」（消息里写着 `threshold:50`）。
一次闸门会跑两遍 pytest（闸门自己一遍、收尾自检又一遍），两遍各自清理约 400 个临时文件，
很容易在同一个回合里越过这个数。

**这只是与观测一致的假设，不是证明** —— 我没有去构造一个刚好越过阈值的最小实验，
所以不把它写成结论。

### 为什么这一条值得单独记

1. 它解释了 D-41 悬着的问题，也解释了**为什么「再跑一遍就绿」**：
   计数按回合算，换一个回合就重新计数。
2. 它说明「退出码 0」这个判据**在本机不是完全可靠的**：同一个仓库、同一份代码，
   会因为环境里一条与仓库无关的记账规则而报红。
   所以以后读到红，先看尾巴里有没有 `FAILED` 行 —— 没有的话，先怀疑环境。
3. **它不是放宽任何检查的理由。** 处置不是「pytest 失败就当没看见」，
   而是把现场抄进这份不会被覆盖的文件。

### 为什么必须抄到这里

现场原本躺在 `reports/verify.md` 里，而那是一份**生成物** —— 下一次运行会覆盖它。
这就是「负结果照实报」在生成物上的边界：**要留住一条负结果，就得把它挪到一个
不会被覆盖的地方**。本文件就是那个地方，所以上面第 1、2 条被逐字抄了过来
（路径已隐去：那是作者机器的绝对路径，正是 S7 要挡的东西，引用它不等于可以把它写进文档）。

### 没有做的事（边界说清）

- **没有**改任何检查去容忍非零退出码；
- **没有**去关掉或绕过那个钩子 —— 它不在本仓库里，也不该由本仓库处置；
- **没有**编一个「最小复现」来把假设坐实。

这三条都是刻意的：这一条记的是**环境的性质**，不是仓库的缺陷。
把环境问题处理成仓库问题，会让检查变松，而那比一次偶发报红糟得多。

---

## D-43 照着 `BLOCKERS.md` 解除卡点 1，会让发布自检变红

**现象（实测）**：按文档把 `.env.example` 复制成 `.env`、填上凭据之后：

| 命令 | 结果 |
|---|---|
| `python -m pytest -q` | **1 failed**：`tests/test_repo_hygiene.py::test_no_secrets_or_key_material_present` |
| `experiments/26_final_selfcheck.py` 第 7 项「工作区里没有密钥文件」 | FAIL |

原因是 `selfcheck.iter_secret_files()` **无条件**把 `.env` 判为违规 ——
它自己的口径就写着「哪怕被 gitignore，也不该躺在这里」。

**这不是 bug，是两条各自都合理的规矩撞在一起**：

- 规矩 A：跑模型链路需要凭据，而文档给的最顺手的做法是 `.env`；
- 规矩 B：**发布态**的工作区里不该有凭据文件。

两条都想要，就得承认它们是**两个状态**，不能同时成立。

### 处置：不改检查，改文档

理由是这个顺序：**检查刚抓到我，这时候放宽它，和「改检查让自己变绿」
在外部表现上一模一样** —— 而这个仓库反复防的正是这件事
（D-30 的载体回路、D-39 的假检查、D-41 的闸门说不清是哪条红，一路都是它）。

所以这一轮的处置是：

- `BLOCKERS.md` 卡点 1 写明「评测态」与「发布态」各自该看到什么，以及
  **用环境变量可以两边都不冲突**；
- `.env.example` 顶上写明同一件事（它正是被复制的那一份，警告要长在那里）；
- 这一条记在这里，因为它属于**设计取舍**，不是文档漏字。

### 留给仓库主的一个选择（我没有替你做）

`iter_secret_files()` 可以改成「只判**被 git 跟踪、或没被忽略**的密钥文件」——
那更贴近它真正要防的事（密钥被**提交**），代价是放弃「宁可错杀」的立场。

这是一个真正的取舍，不是改进。本条只把两条路的代价写清楚，**不替你选**。

### 后来又看了一遍代码：还有第三条路，而且它更好（2026-09-18 补）

上面把它写成了「要么放宽检查、要么留着红」的二选一 —— **这个二选一是我当时没想全**。
回去读了一遍被检查的那段代码才发现漏了最简单的那个解法。

`mcp_server/selfcheck.py:312` 的实现是：

```python
for pat in ("**/.env", "**/*.key", "**/*.pem", "**/.p12", "**/id_rsa*",
            "**/.git-credentials"):
    for p in root.glob(pat):
```

注意它是 `root.glob(...)` —— 扫的是**工作区这个目录**，
跟「这个文件有没有被 git 跟踪」一点关系都没有。检查的 docstring 也是这么写的：
「哪怕被 gitignore，也不该躺在这里」。

想清楚它在防什么之后，这个设计其实**是对的**，而且比我上面写的更对：

> 这个目录会被打包、会被别人 `git clone` 到本地、会被拷进 U 盘。
> 一个**从这里被拷走**的活凭据，和被提交上去的活凭据，危害是同一个量级。
> 所以判据不是「进没进 git」，是「在不在这个目录里」。

于是第三条路就摆在那儿了：**把 `.env` 挪出这个目录**。
检查一个字都不用改，工作区自然干净，凭据也离开了会被打包的范围。

**这一轮的处置**（发布态）：把 `.env` 移到仓库**外面**，位置告知仓库主；
需要跑模型链路时用环境变量（`.env.example` 与 `BLOCKERS.md` 里都给了现成的三行）。

这条与本条上半段并不矛盾，而是把上半段那个二选一**拆掉**了：
原来那两条路（放宽检查 / 留着红）现在还留在纸面上，是因为它们各自确实有代价；
但**在这一轮的实际处境下不必走任何一条**。写下来是因为，
「当时以为只有两个选项」本身就是值得留档的东西 —— 漏掉的往往是最省事的那个。

## D-44 台账默认只读，重跑要显式开一次开关

**背景**：`19_verify.py` 里那几条「需要模型凭据」的检查，写法是**真的去跑**那个脚本。
凭据到位之后，这个写法就有了代价 —— 实测：**跑一次闸门会重发约 200 次模型调用**，
并把 `reports/` 下的评测产物整体覆盖一遍。于是「跑一遍 Gate 看看红不红」
这个最普通的动作，变成了一次花钱且改变工作区的动作。

**处置**：默认行为改成**只读台账** —— 核对盘上有没有那个产物、里面的数字自不自洽；
要端到端重跑，得显式写 `--rerun-llm`。

**新增第三档判定 `SKIP`，而且它永远不是 `PASS`。** 判定细分了两种 SKIP：

| 情形 | 判定 | 写给读者的那句话 |
|---|---|---|
| 无凭据、没跑过、盘上也没有产物 | SKIP | 说明缺什么 |
| 本轮没重跑，但盘上**有**实测产物 | SKIP | 「盘上已有实测产物 X（那是之前配好凭据时跑出来的）」 |

第二行是这个决定的关键：**「有产物但没重跑」和「什么都没有」必须能分开**，
否则要么把没重跑写成通过（假绿），要么把已有的真结果说成不存在（假红）。
代码里由 `verify_kit.judge_not_rerun()` 独家生产这两句话，它**不返回 PASS**。

**为什么不做成「有产物就算 PASS」**：那样一次成功的旧运行会永远替后面的失败背书 ——
产物是死的，它不会告诉你它是不是这一版代码跑出来的。

**放弃了什么**：闸门不再是一条命令验到底。「验到底」要额外记住一个 flag，
这是有意的摩擦 —— 花钱的动作应当需要多敲一个词。

## D-45 自毒回路还有第三条入口：命中行里的**行号**

**这一条是从一次「查不出来的红」顺出来的。** 先记那次失败本身：

### 一次无法归因的失败（照实记，不删）

- **现象**：某次 `python -m pytest` 报 `3 failed`，其中
  `tests/test_repo_hygiene.py::test_no_forbidden_numbers_outside_verbatim_data` 失败；
  同一份代码在前后两次运行里，这一条都是绿的。
- **为什么没归因出来**：那次失败只看了一眼尾巴（`3 failed, ... passed`），
  **没有捕获 traceback**，而工具输出只留尾部，原始断言文本没有被存到磁盘上。
  换句话说：**这一条的证据链在我手里断了**，下面是排查，不是结论。
- **排除掉的候选**（每条都有实测动作，不是推理）：
  - 三份 `*_NOT_RUN.md`：重新生成出来再跑 → 仍然绿；
  - 闸门在同期重写的合成自检产物（`reports/*selftest*.json`、
    `reports/selftest_templates/*`）：逐个查禁用数字 → 一处没有；
  - `reports/verify.md` / `reports/final_selfcheck.md` / `reports/deliverable_check.md`
    的**当前版与 git 里的版本**：逐份查那个四位小数 → 都没有；
  - `.env` 被 `gitignore` 排除但仍在工作区：那是**密钥形态**检查的红，不是这一条。
- **结论**：**未归因**。留档在这里，而不是悄悄略过。

### 顺着它找到的机制（这一部分是实测的）

「报告自己带污染」这条回路在本仓已经出现过三次（D-30）。第四次的门在**行号**上：

```
$ mask_all('某报告.md:<被禁数字> 某一行')            -> '某报告.md:〔已隐去〕 某一行'
$ scan_report_carrier('某报告.md:<被禁数字> 某一行') -> ['<那个数字>']
$ mask_all('某报告.md:<不在词表里的数> 某一行')       -> 原样
```

命中行的渲染形式是 `文件:行号`，而**行号本身可能就是一个被禁数字**
（词表里有几个正是典型的行号量级）。于是：一份报告引用了某个命中，
那个命中恰好落在第 N 行（N 是词表里的数）—— **这份报告就自己成了新的载体**。
行号会随文件改动而挪位，这正好解释「同一份代码先红后绿」。

#### 写这一条的时候，我自己踩进了这一条（实测，2026-09-18）

上面那个代码块，**第一稿是把那几个数字逐字写出来的**（连同一个「不在词表里」的
对照值一起写）。后果是 `python -m pytest` 报：

```
AssertionError: 原项目数字 14 处真命中（另有 3 处已核验巧合）
```

14 处里 **8 处来自描述这个机制的三段文字**：本文件、`mcp_server/verify_kit.py` 的
`finalize_report_text()` 文档字符串、`experiments/25_deliverable_check.py` 的注释
（另外 6 处是同一条断言的展开行）。处置是把那几处换成占位符
（`<被禁数字>` 这种写法），**不是**把它们登记成巧合 —— 它们不是算出来的值，
是抄进来的，登记等于给自己开后门。

**这一条与上面那次「未归因的失败」的关系**：我不声称它就是当时那次的原因，
但它是**同一类**的第三个实例（前两个：报告里列举禁用词、报告里逐字引用命中）。
这次的证据链是完整的，所以它至少证明这个类别的红**会由「写字的人」触发**，
而不只由「被扫的内容」触发。

### 处置：抽出单一实现，补上漏掉的那一处

实测的分工现状是：

| 谁 | 落盘前跑没跑「扫自己 → 掩码」 |
|---|---|
| `experiments/19_verify.py` | 跑了（**自己手写一遍**；且它选择判红而不是掩码，见下） |
| `experiments/26_final_selfcheck.py` | 跑了（**自己手写一遍**，7 行） |
| `experiments/25_deliverable_check.py` | **一遍都没有** —— 而它自己就渲染 `文件:行号` |
| 其它（`09/12/13/20/21`） | 没跑 |

**同一段逻辑抄两遍、漏一处，就是「有缺口但看着齐全」。**
处置是抽出单一实现 `verify_kit.finalize_report_text(text, path)`，
由 `25` 调它（原来漏掉的那一处），并补了**成对**的两条用例：
自毒样本必须被掩掉（正控）＋ 登记过的巧合不许被掩掉（负控）。

### 想过但**否决**的方案：搬进 `eval_runner.write_report()`

那个函数是**所有**程序生成产物的唯一出口，看起来是最自然的位置。
否决理由两条，都是具体的：

1. 它一视同仁。`reports/pair_test.md` 里有一个登记在
   `selfcheck.VERIFIED_COINCIDENCES` 的合法字面量（一对提示词版本的精确 p 值，
   **自己算出来的**）—— 一律掩码会把一个真结果擦掉。
   于是这个方案还得把「按路径豁免」的逻辑也搬进去，搬的就不止是循环了。
2. **产物里有 JSON。** `"rows": <数字>` 掩成 `"rows": 〔已隐去〕` 就是**非法 JSON**，
   `json.loads` 直接抛 —— 那是把数据改坏，比带一点自毒更糟。

所以掩码**只施加在给人读的 markdown 上**，且按路径尊重登记豁免
（`selfcheck.coincidence_paths()` 里没登记这份文件就不放行）。

**没有做的事**：没有改那次失败的判定逻辑，也没有把这条检查放宽 ——
检查本身是对的，它抓到的（如果确实是这个机制）是**真污染**。
`19_verify.py` 那一处也保持原样：它**不掩码，而是把 S9 判成 FAIL**，
理由是「把污染掩掉」和「报告自己带着污染」是两件事，
前者会让人以为从没发生过。要不要把它也切到同一个函数上，留给仓库主决定。

### 清完自毒之后，G7 还剩四处红（实测，2026-09-18）

上一节把 `pytest` 修绿之后，`19_verify` / `25` / `26` 三条仍然是红的，
而它们**只有同一个根因**：`25` 的 G7 报「4 处指向不存在的文件且没交代」。
三条检查一起红，看起来像三个问题，实际是一个 —— 记录一下拆开的过程，
因为它们各自的成因完全不同。

**第 1、2、3 处：表格把「现在不存在」和文件名分到了两个自然段。**

`BLOCKERS.md` 里那张「无凭据时会写出的文件」表，指的是三份
`*_NOT_RUN.md`；这三份文件在跑通凭据之后被脚本按设计删掉了，
所以它们**确实不存在**，而且表格下面的 ★ 段落**确实写了「现在不存在」**。

但还是红了，原因是 G7 的分段：它按**空行**切自然段，整张 markdown 表格
是**一块**，而 ★ 那段是另一块 —— 「就近交代」够不着。

改法是把交代写进**单元格内部**（`reports/xxx_NOT_RUN.md`（本机现在不存在））。
选它而不选「把 ★ 那段并进表格块」，是因为前者在「按段判」和「按行判」
两种实现下都成立，不依赖 `_paragraphs()` 当下的切法。
**这条改的是措辞去迁就判据，不是判据本身** —— 判据「指一个不存在的文件必须
当场说明」是对的，代码块和表格是文档里最容易逃过「当场」二字的地方。

**第 4 处：上一轮的产物，在这一轮被当成了本轮的声明。**

这一处的成因和前三处不是一回事，值得单独看：

```
reports/final_selfcheck.md:46 报告不存在，也没说它还没生成：<某个被删掉的词> 
```

`final_selfcheck.md` 是 `26` 生成的、里面有一节「命中的行」是**引用**。
被引的那一行来自 `DECISIONS.md`，而那份 `DECISIONS.md` 是我**改之前**的版本。
于是出现了一个跨轮次的回路：

| 时刻 | 发生了什么 |
|---|---|
| 上一轮 | `26` 把当时 `DECISIONS.md` 里那行引用抄进了 `final_selfcheck.md` |
| 我改 | 把 `DECISIONS.md` 里那个示例路径换成了不会被 G7 认作引用的写法 |
| 这一轮 | `25`（跑在 `26` **前面**）读到的是**上一轮那份** `final_selfcheck.md`，仍然报红 |
| 再跑一次 | `26` 重写了它，红自己消失了 |

**结论：这不是判据的缺口，是「读的人跑在写的人前面」造成的滞后一轮。**
它自愈，代价是**改完文档的第一遍闸门可能是红的，第二遍才绿** ——
如果不知道这条，很容易把它误判成「改漏了哪里」然后去翻一个并不存在的问题。

处置：**没有为它加特例**。加特例等于承认「生成物里的引用可以不算引用」，
而 `DECISIONS.md` 记这一条的理由正是「引用也是载体」。
所以只是先单独跑一次 `26` 把产物刷新到当前文档，再跑闸门。
**实测结果**：紧接着的两遍完整闸门 4 步全绿，
`reports/` 聚合哈希两遍相同（38 个文件），也就是这次刷新没有引入任何不稳定产物。

---

## D-46 词表住在仓库外面（判定口径一个字没改）

**这一条记的是一个错误的设计，以及为什么修法是「搬家」而不是「换锁」。**

### 错在哪

`mcp_server/selfcheck.py` 里原来逐字写着三张词表：公司全名 / 课程名 / 原仓库名（8 条）、
原项目的 14 个数字（17 条）、领域术语（25 条）。它们**必须**住在那儿 ——
词表只能有一份，而扫描器要 import 它。

同时，这个文件在 `SELF_FILES` 里，被四类扫描全部跳过 —— 这条排除也**是对的**：
词表扫自己必然全中，那是数据，不是内容。

两个各自都对的设计叠在一起，得到一个**错误的结论**：

    「识别性名称 0 命中」为真，推不出「仓库里没有这些名字」。

于是整个仓库里最不该有这些字的地方，恰恰是唯一有这些字的地方。**实测确认过**：
`experiments/27_leak_fix_verify.py` 拿修复前的提交（`旧编号·E`）跑同一套扫描，
`mcp_server/selfcheck.py` 里命中 **50 条词表条目、共出现 88 次**。

### 修法：把真值搬出仓库

`MCP_TOOLKIT_WORDLIST` 指向一个**仓外的** `.py`；没设这个变量时去仓库**同级目录**
找 `mcp-guarded-toolkit-wordlist.py`。装载走 `ast.literal_eval`，**不做 exec** ——
词表是数据，不需要执行能力。

**判定口径一个字都没改**：扫什么、怎么算命中、什么算通过，全部照旧。
改的只是「词表住在哪」。这是本次修改唯一要做对的事 —— 一旦顺手把判据放松一点，
这次修改的性质就从「修一个洞」变成了「盖住一个洞」。

### 读不到词表时判 SKIP，不是 PASS

这一条是必须交代的后果：词表搬出去之后，「读不到」从不可能状态变成了**常态**
（任何人 clone 下来都是这个状态）。那时候判 PASS 就等于说
「本机没有词表，所以仓库是干净的」—— 那是这一整套检查最想防的那种谎。

处置复用 D-44 立的口径：**SKIP 永远不是 PASS**。具体到三个地方：

| 谁 | 读不到词表时 |
|---|---|
| `19_verify.py` 的 S1/S2/S3 | 判 `SKIP`，detail 写「词表未随仓分发（见维护者本地）」 |
| `tests/test_repo_hygiene.py` 里带 `needs_wordlist` 的用例 | `skipif`，理由同上 |
| `26_final_selfcheck.py` 的第 1~3 项 | 判 `SKIP`，**退出码 5**（不是 0） |

退出码那一格是这次唯一的「新机制」：`tools/check.py` 的 `pending_exit` 字段一直
存在但从没人用过（它自己的注释就这么写着）。第 4 步是**发布结论**，
「9 项里有 3 项没资格下结论」不能算通过；但也不该显示成「失败」——
那会让人去翻仓库内容，而该做的是造一个词表文件。所以第 4 步声明 `pending_exit: 5`，
闸门读到就打印「未完成」，理由与 `BLOCKERS.md` 卡点 3 的最小动作一一对应。

### 负控样本跟着搬

`NEGATIVE_FIXTURES` 的 S1/S2/S3 原来逐字写着样本内容。词表搬走之后，
如果样本还写死，这个文件**又成了那个唯一有这些字的地方** —— 绕一圈回到原洞。
所以样本改成**从词表里取**（`_wordlist_sample()`）；词表不可用时那三条例控整条 SKIP。

SKIP 而不是「拿占位串凑合跑」：占位串既掩不掉也扫不出来，拿它跑只会得到一条**假红**，
而假红比 SKIP 更糟 —— 它看起来像「检查很严格」。


## D-47 交付级自检有三项的**作用域**取大了（口径没动，动的是范围）

**这一条记的是一类错法：判据本身没错，但它的范围把合法的东西也圈进去了。**

三处，第一次拿真实交付物跑就全红：

| 项 | 原来的范围 | 撞上的合法反例 | 后果 |
|---|---|---|---|
| 第 5 项 | 交付物 HTML 上比对**整张**名字表 | 词表第 1 张表头三条就是雇主名与内部系统名 | 简历不写雇主就没法核实 → 这一项**永远红** |
| 第 7 项 | 整份 HTML 里找 ` · MCP` 这个形状 | MCP 条的技术栈是 `Python · MCP 官方 SDK` —— 形状一模一样 | 想躲的那个写法，正是要抓的那个写法 |
| 第 8 项 | 要求**整份**简历里没有指向别处的 github 链接 | 犀牛鸟条本来就有两个正确的公开链接 | 把一份本来就对的链接判红 |

三处的方向相同：**误报**。误报的代价不是「太严」，而是**让人去改一份本来就对的文件** ——
改完还是红，于是这一项被学会忽略，这才是最坏的结果。

### 修法：收范围，不放宽口径

`0 命中才算过` 一个字没改，改的是「在哪儿数」：

- **第 5 项** → 只查「**仓库标识**那一类名字」。名单放在**仓外词表**里
  （第 4 个键 `RESUME_FORBIDDEN_NAMES`，是第 1 张表的子集）。**缺这个键时退回整张表**
  （更严）：漏写的后果必须是红，不能是「这一项没查」—— 那两种状态在报告上长得一样。
- **第 7 项** → 只在 `<h2>实习经历</h2>` 这一节里数。**找不到这一节判红**，不是放过：
  结构变了和内容干净是两件事。
- **第 8 项** → 只查标题带 `MCP` 的那一条（条目数 ≠ 1 判红），且链接**不要求带协议头**
  （交付物里写的是 `github.com/…`，只认 `https://` 会把正常写法判成「一处都没有」）。

### 放弃的方案：把那几条名字硬编码进仓库

最省事的做法是模块里写一行 `RESUME_REPO_NAMES = ("med-…", …)`。**放弃的理由就是刚修的 D-46**：
那等于仓库又把那些名字抄了一遍 —— 明文桥的成因正是「敏感串在仓库里存了第二份」。

### 钉住它的用例

`tests/test_delivery_selfcheck.py` 新增 4 条，覆盖的正是「反直觉的那一侧」：
雇主名**可以**通过、旧仓库名**必须**红、带不带协议头都要认出来、
别的条目的仓库链接**不管**、实习那一节缺失**判红**。


## D-48 简历第 10 版：撤下的是量，不是结论（附一处与报告尾零不一致）

### 1. §4 D2 的 15 个数字按字面执行 —— 全简历 0 命中

★ **这一节故意不写那几个量本身**。第一次写的时候我逐字把它们列在下面的表里，
结果 `19_verify.py` 的 S2/S3 立刻报红 —— 台账是仓库里的产物，产物里写下禁用数字，
它们就成了新的命中（本仓踩过四次的回路，见 D-30 / D-45）。
所以这里改成**描述**：哪一行的哪个量被撤了，值本身去简历的历史版本里看。

清单里有一半落在**实习条**上（语料规模那两处、切分规模那两处、同义词条数、
两档 R@5 的具体值、被引用的编号条数、弃答阈值、向量规模），
而那几个量是**真实记录**，只是没有公开落点。
处置：**撤下量，保留能力与结论**，且**不造替代数字**（造替代就是编）。

| 撤下的量（按行说，不写值） | 改成 |
|---|---|
| 分工行的「文件数 / 体积」括注 | 直接删（六类文档那句本身够用） |
| chunk 数与文档数 | 「切分为 chunk 级并逐篇建立文档级映射」 |
| 同义词条数 | 「同义词注入」 |
| 两档 R@5 的具体值 | 「结构化条目的 R@5 高于文件级文档」 |
| 被引用编号的条数 | 删（「越界编造 0」保留） |
| 弃答阈值的具体取值 | 「把阈值取在该区间内」（区间两端那两个数保留） |
| 「N 向量规模下」里的 N | 「该向量规模下」 |

**没动的**：0.053→0.368（7 倍）、+0.083、12 种融合策略、0.9167/0.9271 与
0.9688/0.9896、5:0 与 p=0.0625、0.263、68.9%（62/90）、三桶 76 = 54/2/20、
1.0000 与 0.9800、0.7857、205→78/55、区间两端、149.5 ms 与 0.20 ms、并发 6。
实测：改后按 15 个数字逐个 `count`，**全部 0**（扫描口径与交付级自检第 5 项同一套）。

### 2. 措辞：换掉的是「指向」和「判定强度」，不是事实

- `成本` → `费用`（2 处）：这一批交付的数字**不含单价**，金额栏是空的（D-25），
  简历上不留「成本」字样，免得被读成「花了多少钱」。
- `显著` / `提升` → 中性说法（5 处）：没超出实测噪声带的，只写方向不写判定。
- `安全门` → `安全校验`（2 处）、`带引用生成` → `引用式生成约束`（1 处）：
  这两个词是两条之间的**互指标记**，改名后含义不变、指向消失。

★ 任务书同时写了「全简历 0 命中 `安全门`」和「技能栏里的『规则式…安全门』可以保留」——
这两条**不能同时成立**（保留就必然命中）。取交集：**两处都改名**，机械扫查为 0，
含义一个字不变。放弃了「保留技能栏那一处」的写法 —— 那会让同一段要求里的
「0 命中」自相矛盾。

### 3. ★ 有意保留的一处不一致：简历写两位小数，报告写四位

简历上写 `精确双尾 p = 0.5 / 0.25`，而 `reports/pair_test.*` 写的是同一个数的四位小数。
**同一个值，简历把尾零去掉了** —— 因为那个四位写法在 D2 的禁用清单里
（它同时是原项目的一个数字）。出处仍然是 `reports/pair_test.*`，口径没变。
这是**已知的、故意的**不一致，写在这里以免下次有人「顺手把简历对齐报告」时改回去。

### 4. 「内容优先级」六项不是排版顺序

①（不可答 0/4→4/4）与 ⑥（三版 EX）**住在同一条 bullet 里**，无法一条排第一、
另一条排第六 —— 所以那六项只可能是**保留优先级**，不可能是版式顺序。
因此维持「协议层 → 安全层 → 语义层」的递进顺序，六项全部在，一条没删。
放弃了：把 Text2SQL 那条提到最前（① 更显眼，但同一条里的 ⑥ 也被一起顶上去，等于没排序）。

### 5. 填充率的口径（新脚本 `tools/resume_fillrate.py`）

量的是**每页文字最下沿到哪儿**，不是「几页」—— 页数只说明过没过门槛，
余量才说明还能不能再改一个字。

- 输入：`DELIVERY_RESUME_DIR` 下唯一那个 `*4.0*.pdf`，用的是与交付级自检第 14 项
  **同一个**查找函数（两边各找各的，报告里的数字就可能指着不同文件）
- 量法：PyMuPDF 取每页文字块 bbox 的最大 `y1`（左上角为原点）
- 底线：841.89 − 10mm = **813.54pt**；余量 = 底线 − `y1`
- **两个百分比都打**：占整页（841.89，与任务书里「88%/96%」同口径）与占底线
  （813.54，这才是「还能不能加字」的分母）

第 10 版实测：p1 740.3pt（88%）、p2 805.7pt（96%）、**余量 7.8pt**、页数 2 ——
与第 9 版的 740/806（余量约 7pt）**逐点一致**，说明这次改动没改变排版几何。

### `SELF_FILES` 的语义没有放宽，反而更窄了

那条排除**保留**，但理由换了：原来写的是「词表所在的两个文件」，
现在是「负控样本所在的两个文件」—— `NEGATIVE_FIXTURES` 里有一条长得像密钥的文本
和一条作者机器的绝对路径，它们住在 `selfcheck.py` 里，扫自己必然命中。

同时**新加一条测试**（`test_the_exempt_files_do_not_hide_a_word_list`）：绕过
`SELF_FILES`、直接扫这两个文件，断言**一个词表条目都没有**。
这条测试原来的方向是反的（断言「词表确实在这两个文件里」，见下），
词表搬走之后那个断言会逼着谁把词表搬回来。

两条合起来才补上那个推不出的结论：
`S1 = 0`（豁免区**之外**）**加上**「豁免区**之内**也没有」，才真的推出「全仓没有这些词」。

### 明确没有采用的四种做法

| 没采用的 | 为什么 |
|---|---|
| base64 / 异或 / 反转编码词表 | 安全剧场。字符还在仓库里，只是变成读不了的形状；而判定要读它，解码方式就得一起发出去 |
| 直接删掉词表 | 检查变成「无条件通过」—— 把报警器拆了当安静 |
| 换成通用词表（只留一个常见的领域词） | 判定被放宽。公司名、课程名、原仓名这些**具体的**词才是要防的东西 |
| 改检查去容忍命中 | 同上且更糟：报告显示「通过」，而仓库里仍留着那些字 |

被否决的第五种：**把那条巧合字面量留在 `selfcheck.py` 的源码里**，
然后让「豁免区干净」那条测试给它开个口子。否决理由是那条测试一旦有例外，
例外就长在**同一个文件**里 —— 下次想加例外的人只要改一行就行。
现在改成在源码里**拼**出来（前半截和后半截两个字符串相加，见那一段的注释），
运行时值不变、判定不变，**源码里没有那个连续的串**；
而它的真正常住址 `reports/pair_test.md` / `.json` 里照旧是逐字写的
（那两处由 `VERIFIED_COINCIDENCES` 登记放行，别处一律不放行）。

这不算编码：拼法在源码注释里写得明明白白，藏起来的是**那一个连续字面量**，
不是「这里有这么件事」。

### 实测（口径：`experiments/27_leak_fix_verify.py`，可重跑）

| 查的是哪一份 | 命中条目数 | 出现次数 |
|---|---|---|
| 工作区（修复后） | **0** | **0** |
| 负控：`旧编号·E`（修复前） | 50 | 88 |
| 公网 raw（**本次修复推送前**） | 50 | 88 |

第三行是这次修改的全部理由：**公网上那份当时真的是带这些字的**，
而且它已经在公网上躺了（建仓当天推的）。推送之后的实测值见
`reports/leak_fix_verify.md`。

### 这一条顺便发现了什么

阶段 0 的台账里，我按惯例写了一行核验原仓「只读」的表格，里面带了原仓的目录名 ——
**当场被 `test_no_identifying_names_anywhere` 抓住**（2 处命中）。已改成不带名字的写法。

记在这里是因为它说明了一件事：这条纪律在人身上是**真的会滑**的 ——
写「我确认过那个仓库没被动过」的时候，把那个仓库的名字写出来是极自然的动作。
机器抓住它，比事后有人翻出来好。

---

## D-49 词表不在本机时，判据必须判 `SKIP` —— 这一族漏了四处，其中一处把结论写反了

### 怎么发现的：不是想出来的，是**真克隆一次**跑出来的

为了给简历那句「clone 后不联网可跑」找出处，我做了一次**真克隆**（`git clone`，
带 `.git`，且放在一个父目录里没有词表的地方），在克隆里跑 `pytest`：

    5 failed / 539 passed / 14 skipped

逐条看这五条，**四条是「词表不在 → 那几张表是空的」**，一条是
「示例库还没构建」（`data/` 下那两份是构建产物，不入库，先跑构建脚本即可）。

### 其中一处不是「红得难看」，而是**结论写反**

交付级自检第 15 项的判据是**反的**：它要求「历史里还扫得到那些字」**才算过**
（那正是「历史没被重写、残留还在」的证据）。词表不在本机时，扫描必然是 0 命中 ——
于是这一项判 **FAIL**，注释还写着「说明有人已经重写过历史」。

也就是说：**「没查」在这里被读成了一条不实的、而且相当严重的指控。**
这与 D-46 是同一个坑的**反面** —— D-46 是「没查」被读成「干净」，
这里是「没查」被读成「出事了」。两面都会骗人，所以两面都要显式分岔。

### 改了什么（口径一个字没改，只补「没有实测依据」这条分岔）

| 位置 | 原来 | 现在 |
|---|---|---|
| 自检第 15 项 | 词表不在 → 命中 0 → **FAIL** | 词表不在 → **SKIP**，注释指明「也不等于历史被重写过」 |
| 三条**拿词表里的真词当样本**的用例（掩码 / 自毒行号 / 登记巧合） | 直接取表里的词 → `IndexError` | 加 `needs_wordlist` 标记，判 SKIP |
| 历史残留的正控与负控两条用例 | 无词表时场景无法构造 | 判 SKIP |
| 看库结构那一条用例 | 靠「默认那个库建没建」兜底，克隆里先撞上「没有库」 | 两个参数都显式传；**判据没改**，去掉那层隐式依赖 |

★ 守卫**写在用例上，不写在整个文件上**：那几份文件里多数用例与词表无关，
整文件跳过才是真的把覆盖降下来（那等于为了让数字好看而自废测试）。

### 实测（同一份工作区，两个环境）

| 环境 | 收集 | 通过 | 跳过 | 失败 | 退出码 |
|---|---|---|---|---|---|
| 本机（仓外词表在位） | 564 | 564 | 0 | 0 | 0 |
| 真克隆（无词表、无构建产物） | 564 | 544 | 20 | 0 | 0 |

两边相加都对得上：544 + 20 = 564。

> **补记**：这是**做这条决定时**的实测值。之后 D-50 又补了三条用例，
> 同一份工作区现在是 **567 收集 / 本机 567 通过、克隆 547 通过 + 20 跳过**（相加同样对得上）。
> 两张表都在，是为了让「哪一次量的是哪一版」看得出来 —— 不把旧数字改掉去凑新状态。

### 顺带实测出来、写进 README 而不是藏起来的两件事

1. **克隆里 `tools/check.py` 不会全绿**：第 2 步那几条红全是「示例库还没构建」
   （注释精确指着 `python experiments/01_build_db.py`，那一步不需要联网）；
   第 4 步因为靠词表的项判 SKIP 而停在「未完成」。
   这是**设计代价**，不是缺陷 —— 词表放进仓库就等于再造一座明文桥（D-46）。
   README 原来那句「不联网就能跑通全部测试与自检」对新克隆**不准确**，
   已改成「跑完全部测试与自检」并补两条「已知边界」。
2. 改第 15 项时**打破了它旁边那条负控用例**（它靠「0 命中 = 历史被重写」构造场景，
   第 15 项一改成 SKIP，它拿到的是 SKIP）—— 真克隆第二次实测抓到的（`1 failed`）。
   这条一起加了守卫。**「改对了判断，却弄坏了守着它的那条测试」**值得单记一笔。

### 放弃了什么

- **把这些项一律判 PASS**（克隆就全绿）：那正是 D-46 修掉的坑，口径不许放宽。
- **把词表随仓分发**：那是明文桥本身。
- 钉住它的用例：`test_wordlist_missing_means_skip_never_pass` 的参数化清单里
  加进了第 15 项 —— 这一个坑现在有**五条**用例守着。

---

## D-50 「联网那一项」从来没被真跑过 —— 第一次跑就发现判据写反了

### 怎么发现的

这一轮真跑了 `experiments/28_delivery_selfcheck.py --online`（以前一律离线跑，
那样第 4 / 13 项判 SKIP，走不到取回那一行）。第 4 项**当场报 ERROR：联网取回失败** ——
而同一个地址、几乎同一时刻，`experiments/27_leak_fix_verify.py` 取回得好好的。

### 原因：成功的那一侧判反了

取回函数 `fetch_remote` **成功时返回的是 HTTP 200 + `bytes`**（那正是 `curl.exe` 的
返回形态，也是任务书点名的那条命令），而调用处写的是
「状态码 `!= 0` 或不是 `str` 就算失败」——

**于是取回成功反被读成「没取到」。**

### 为什么它一直没被发现（这一条比 bug 本身值钱）

1. 那段调用写在**以数字开头的脚本**里（`experiments/` 下的脚本不能被 `import`），
   所以「取回算不算成功」这条判断**没有用例守得住**；
2. 离线模式下这一项直接判 SKIP，**永远不会走到那一行**。

**「默认不跑的路径」就是「没被验证的路径」。** 判据写在测不到的地方，
等于把它的正确性交给运气。

### 修法

- 把「取回 + 用同一套词表扫」搬进 `mcp_server/delivery_selfcheck.py`
  （可 import、可 monkeypatch），驱动脚本只负责调用；
- 补三条用例，**两侧都钉**：HTTP 200 算成功（这一侧不钉的话，写反了照样全绿）、
  非 200 算没取到、「没取到」判 **ERROR 而不是 PASS**（后者就是 D-46 那个老坑：
  用「没查」冒充「查过了」）。

### 实测

修后 `--online` 跑满 15 项：**PASS 15 / FAIL 0 / SKIP 0 / ERROR 0**，退出码 0。
第 4 项的依据写作 `curl.exe（HTTP 200）`；第 13 项本地与远端同为 `旧编号·J`。

### 留给以后的一条

**凡是「默认不跑」的分支（要联网、要凭据、要交付物在机），至少要有一条用例
把它成功与失败两侧都钉住。** 只钉失败侧的用例看不出一件事：判据被写反了。

## D-51 核验脚本顺手改写了**别人的**产物 —— 跑完工作区脏着两份降级报告

### 怎么发现的

任务书阶段 5 要求「push 之后再跑一次公网 raw 验证」。照做，跑完
`experiments/27_leak_fix_verify.py`，`git status` 里冒出三份改动，
其中**两份不是它的产物**：

| 文件 | 变化 |
|---|---|
| `reports/leak_fix_verify.md` | 27 自己的产物 —— 应该变，正常 |
| `reports/verify.md` | `P1 python -m pytest` 那行从 **567 passed** 变成 **549 passed, 18 skipped** |
| `reports/final_selfcheck.md` | 收尾自检 9 项从 **PASS 9** 变成 **PASS 6 / SKIP 3**；交付级 15 项从 **PASS 13 / SKIP 2** 变成 **PASS 1 / SKIP 14** |

### 根因：降级演示那一步真的会跑闸门

27 为了证明「读不到词表时判 `SKIP` 而不是 `PASS`」，会带着一个**指不到文件的**
`MCP_TOOLKIT_WORDLIST` 去跑 `tools/check.py --only 4`。而第 4 步
（`26_final_selfcheck.py`）内部又会跑 `19_verify.py` 与 `pytest` ——
**这两样分别把 `reports/verify.md` 与 `reports/final_selfcheck.md` 重写一遍**，
而且是在「本机没有词表」的环境下重写的。

**所以那一跑等于把闸门的产物刷成了降级版。**

### 为什么这比「工作区脏了」严重

那两份报告读起来**和真的一模一样**：判定栏是 `SKIP`、数字都自洽、
「没有实测依据」那句话写得清清楚楚。差别只在于它描述的**不是这台机器现在的状态**。
谁要是顺手 `git commit -a`，仓库里就留下一份「本机连词表都没有」的官方记录 ——
而它其实是另一次实验的副作用。

这和 D-45 是同一族：**产物被生成它的过程污染**。区别是 D-45 污染的是**内容**
（报告里逐字引用了敏感串），这一次污染的是**别的产物**。

### 修法：跑之前按字节存下来，跑完原样放回

`mcp_server/verify_kit.py` 新增 `ProductsPreserved`（上下文管理器）：

- 存的是**字节**，不是文本 —— 换行风格也在还原范围内（本仓的 md 在 Windows 上会被 git 换成 CRLF，实测过）；
- 跑完把每份的**前后 sha256** 记进 `evidence`，报告里照抄 —— 于是「还原过」是可核的，不是一句声明；
- 本来**不存在**的文件，跑完若被创建出来就**删掉**（否则一个实验副作用会冒充成一份真产物）；
- ★ 还原失败**不静默**：记 `restored=False` + 错误类型，写进报告由人去看；
  也**不在这里抛异常** —— 那会盖掉调用方原本的异常（比如子进程超时）。

补三条用例（`tests/test_verify_kit.py`）：字节还原（样本故意含 CRLF）、
「本来没有的新文件要删掉」、★ **负控**：把文件设成只读逼出一次真的还原失败，
断言 `restored=False` 且带上错误类型。

### 实测

| 项 | 值 |
|---|---|
| 跑完 27 之后的 `git status --short` | **只有** `reports/leak_fix_verify.md` 与三份源码 —— 两份属于闸门的报告**不再被改** |
| 报告里那张还原表 | `reports/verify.md` `31c182e09fca` → `31c182e09fca`（是）；`reports/final_selfcheck.md` `a7c85538087f` → `a7c85538087f`（是） |
| 连跑两次 27，产物 sha256 | `549d5cba964e6934` = `549d5cba964e6934`（**逐字节相同**） |

### 代价（说清楚，不藏）

还原发生在**子进程跑完之后**。那一步中途被强杀（手工 Ctrl+C、断电）就会停在降级状态。
这时候 `git status` 会明确告诉你有哪两份被改过，重跑一次闸门即可复原 ——
**没有掩盖**，只是没有自动兜住。要做到「强杀也不留痕」得让第 4 步支持「只判不写」，
那是动闸门自己的语义，代价大于收益。

### 放弃了什么

- **让 27 改跑一个仓库副本**（最干净，但每次核验都要拷一份仓库，且副本里 `.git` 不在就会红得跟代码无关 —— D-49 那轮已经踩过）；
- **给闸门加 `--no-write`**（语义改动落在闸门身上，而这里要的只是「一次核验别越界」）；
- **接受脏工作区、在文档里写一句「跑完 27 记得 `git checkout -- reports/`」**
  （把纪律当机制用，正是这一整轮在修的那类问题）。

## D-52 「公网取法」那一行不进产物 —— 它是逐次可变的读数

### 现象

同一台机器上连着跑了三次 `experiments/27_leak_fix_verify.py`，
报告里那行 `公网取法` 依次是 **`curl.exe` → `urllib` → `curl.exe`**。
两次的 sha256 与命中数**完全一致**（都是同一份字节），只有这一行在抖。

### 为什么必须处置

`RESULTS.md` 第一节写着「跑闸门前后的聚合哈希相同」，`tools/check.py --snapshot`
就是量这句话的。一行会抖的读数留在产物里，等于**这份报告不可逐字节复现** ——
每跑一次 `git status` 就脏一行，而这行还得靠人判断「这次抖动没关系」。
**那正是 D-38 拒绝时钟读数（`pytest` 尾巴里那个 `N passed in …s`）的同一条理由。**

### 处置

- 产物里保留的是**取法策略**（先 `curl.exe`、起不来才退 `urllib`）—— 这是稳定的口径；
- **哪一种成功**只打控制台，不进产物，并写明「它与判定无关」；
- 口径节里把这件事本身写出来（含「实测两次成功的是不同的那一个」），
  免得下一个人以为漏记了。

### 实测

| 项 | 值 |
|---|---|
| 改后连跑两次，`reports/leak_fix_verify.md` 的 sha256 前 16 位 | `549d5cba964e6934` = `549d5cba964e6934` |
| 五行判定 | 1 / 负控 / 2 / 3 / 4 全部 `PASS`，退出码 0 |
| 负控命中数 | 50 条 / 88 次（未变） |
| 两侧 sha256 | 工作区与公网 raw 同为 `12bc32b05808…`（同一份） |

### 被否决的做法

- **写死一种取法**（只用 `curl.exe`，不起就判「没取到」）：把本机环境问题
  升级成「公网那一项没有实测依据」，牺牲的是证据强度；
- **两种都试、两个结果都记**（`curl.exe 失败 / urllib 成功`）：照样逐次可变，
  只是把一行不稳变成两行不稳。

## D-53 历史残留不重写：本仓的证据链逐字引用历史 sha，重写会自断

### 决策

**不执行甲**（`git filter-repo` 改写历史 + `git push --force`），
**不执行乙**（删掉远端仓库、重建一个空的再推当前快照）。
**继续走丙**：不处理，把外部前提、残留范围与解锁条件写清楚
（`BLOCKERS.md` 卡点 4 第四轮补记）。

★ 措辞上强调一次：**这是「带解锁条件的决定」，不是「将来要做的待办」。**
记下这条的目的，就是让它**不必**再做。

> **补记（2026-09-19）：本条只动了两处，其余照旧。**
>
> - **「不执行甲」「不重写历史」照旧成立**，一个字没松。所以下面三条依据的**方向**全部有效。
> - **「不执行乙」这一半被取代了**：成果迁去了新仓（新仓历史自一条初始提交起算），
>   旧仓**不删**、转私有。形状像乙，但乙被否掉的三条理由**逐条都被处理掉了**，
>   而且不删旧仓这一处与乙**根本不同** —— 完整对账见 `DECISIONS.md` **D-54**。
> - **本节里的历史编号已改写成标签** `旧编号·X`（原文与映射留在仓外）。
>   **句子、读数、判定一字未改**，改的只是编号在文档里的可解析性；实测读数见
>   `BLOCKERS.md` 卡点 4 的 2026-09-19 补记那张表。
> - ★ **依据①里那组读数（7 个文件 / 14 个提交 / 3 处完整 40 位）复算不出来，
>   按作废处理**（口径没写全），以 `BLOCKERS.md` 那张表为准；**方向不变且更重**
>   （22 个对象 / 72 处）。依据①末尾那句「按全部出现数是 7 个文件 / 14 个提交」
>   同样作废。
> - ★ **依据②与 ③ 各有一处要订正**：② 点名的常量 `PRE_FIX_COMMIT` **已不存在**
>   （编号搬去仓外，代码里不再有写死的编号），因此「取不到那个对象 = 脚本跑不起来」
>   这个硬后果**不成立了**，② 只剩一半（强度弱了，方向没变）；
>   ③ 说的「闸门从此常红」**已经不成立** —— 第 15 项现在把「本机只有一条初始提交的历史」
>   判成「无实测依据」，只有「本机存着旧历史却取不到那个对象」才判 FAIL。
> - **两个解锁条件的状态**：①的**目的**已达成、**手段**与原文不同；②被**更强的一步**取代。
>   逐条对账见 D-54 那一节（★ 两处都不是「做完了」，措辞在那里写明了）。

### 依据三条

**① 本仓的证据链逐字引用了历史 sha，重写会让它们全部指空。**

实测（2026-09-18）：本仓有 **7 个文件**逐字引用了 **14 个历史提交**的 sha：

| 文件 | 引用行数 |
|---|---|
| `PROGRESS.md` | 12 行 |
| `BLOCKERS.md` | 7 行 |
| `DECISIONS.md` | 有 |
| `experiments/27_leak_fix_verify.py` | 有（`PRE_FIX_COMMIT`，**不是旁注，是输入参数**） |
| `reports/delivery_selfcheck.json` | 有 |
| `reports/delivery_selfcheck.md` | 有 |
| `reports/leak_fix_verify.md` | 有 |

其中 3 处写的是完整 40 位。这些引用不是装饰：它们是
「**当时真的推了这个 sha**」的事实记录 —— 改历史之后再改台账去迁就新 sha，
那不是维护台账，那是**篡改台账**。

> 这条同时更正了卡点 4 末尾原先那句「sha 引用一共 3 处」：那是只按完整 40 位数出来的，
> 按全部出现数是 7 个文件 / 14 个提交。方向不变，只是那个数字偏小。

**② 负控在原理上需要一个「仍含敏感串的历史提交」存在。**

`reports/leak_fix_verify.md` 里那行负控（`旧编号·F` 那一版命中 50 条 / 出现 88 次）
的作用是**证伪一件事**：「工作区那 3 个 0 命中，会不会只是尺子坏了？」
没有负控，0 命中既可能意味着「干净」，也可能意味着「扫描器根本没工作」——
两者在报告上长得一模一样。

而负控的输入参数，就是 `experiments/27_leak_fix_verify.py` 里的 `PRE_FIX_COMMIT`
指向的那个**真实历史提交**。重写历史会把词表从所有历史里抹掉 →
负控样本消失 → 那个脚本 `git show` 一个不存在的对象 → **整条检查跑不起来**。
（用「捕获异常、返回空串」兜住它也不行：那会把负控从「实测报红」降级成
「无实测依据」，等于把这条证据拆掉。）

**③ 重写之后闸门会从此常红。**

`BLOCKERS.md` 卡点 4 的「甲」那一栏已经写明：收尾自检**第 15 项**的判据
就是「修复前那个提交还取得到」—— 重写后它**必然判 FAIL**。
那是它**该**报的（判据没写错），但结果是本仓最常跑的那道闸门永远有一个红点。
一个永远红的检查会训练人忽略它，这比那个红点本身更贵。

### 外部前提与收益对照

| 项 | 状态 |
|---|---|
| 那个存量索引仓 | 已转为 **private**（仓库页 / API / raw 全部 404，`info/refs?service=git-upload-pack` 返回 **401** —— 401 = 存在但私有，404 = 不存在，所以是转私有，不是删除） |
| 该账号的公开仓数量 | **4 → 3**；本仓 `mcp-guarded-toolkit` 仍是 public，与本地 HEAD 一致 |
| 该存量仓的 `forks_count` / `stargazers_count` | **0 / 0** |
| 历史残留的**可对照公开落点** | **没有了**：顺着旧仓名点过去是不可达的，旧指标值随之成为**没有出处的孤立数字** |
| 历史残留的**净增量** | 只剩「本工具层与那个存量项目有渊源」这一句话的暗示 |

**为抹掉一句暗示，去付「自断证据链 + 闸门常红 + 台账必须大改」这三笔代价，
代价大于收益。** 所以丙仍是结论。

### ★ 解锁条件（将来若真要重写历史，必须先做完这两步）

**这两步没做完之前，任何人不许对本仓做历史重写。**

1. **把负控从「依赖真实历史提交」改成合成夹具。**
   自己造一个临时提交或临时目录，里面放**占位串**（不是真实敏感串），
   使 `experiments/27_leak_fix_verify.py` 不再引用任何真实历史 sha
   （`PRE_FIX_COMMIT` 这一项要么删掉、要么改指合成对象）；
   同时改掉收尾自检**第 15 项**的判据，让它读那个合成夹具，
   而不是读「修复前那个历史提交还在不在」。
   —— 这一步做完，重写历史才不会再拆掉自己的证据链。
2. **把台账里所有引用历史 sha 的段落整体标注为「重写前记录，重写后 sha 全部失效」。**
   ★ **标注，不是改写**：那些记录是「当时真的推了这个 sha」的事实，
   改它就是篡改台账（见依据①末尾）。

### 被否决的做法

- **甲**：`git filter-repo --replace-text` + `git push --force` —— 硬禁止，且依据①②③ 都指向它；
- **乙**：删远端重建 —— 同上；另外本机**没有 `gh` CLI**，`GITHUB_TOKEN` / `GH_TOKEN` 都是空的，
  这一步本来也只能由人手动做；
- **把残留「顺手」清一下再说**（例如只改最新几个提交）—— 那既不是甲也不是乙，
  而是**没有记录在案的历史改写**，性质比甲更差。

### 与前面几条的关系

- `BLOCKERS.md` 卡点 4 记的是**现象与三条路**；本条记的是**为什么选丙、以及什么条件下才允许改选**；
- D-46 记的是「词表搬到仓库外面」；D-07a 记的是「文档不复述词表条目」——
  本条与它们共用同一条纪律：**涉及那些串时用中性类别名（公司名 / 课程名 / 旧仓名 / 旧指标值）
  指代，仓名不写，地址不写。**

---

## D-54 成果迁去新仓：切的是「按编号取回旧明文」这条路，不是内容里的暗示

### 决策

把本仓的**成果**迁到一个新仓：新仓的历史自**一条初始提交**起算，旧仓**不删**，
在核对无误之后由用户转成 private。「不重写历史」这条决定（D-53）**一个字没改**。

本轮只做**本地准备三步**；「建新仓 → 推 → 旧仓转私有」是**用户闸门**：
建仓、改可见性、删仓都只能由用户做，我不碰凭据、不碰远端仓（D-37）。

| # | 做什么 | 关键性质 |
|---|---|---|
| 1 | **干净快照**：不动工作区、不动索引、不动 `HEAD`，用 `git commit-tree` 从当前树造一条初始提交挂在本地分支上 | ★ **新仓那一条提交的树 == 现在的树**：两个分支的 `^{tree}` 必须相等 —— **迁移不改一个字节的内容，只改历史** |
| 2 | **编号搬去仓外**：文档/报告里逐字写出的历史编号改写成标签 `旧编号·X`，原文与映射留在仓外 | 编号在仓内**归零**（实测 0 处能解析回真实对象） |
| 3 | **地址单点收敛**：`mcp_server/paths.py` 新增 `repo_slug()` 一族 | 地址来源三级：环境变量 → `origin` → 仓外针脚；**仓内没有写死的地址** |

### 为什么形状像乙、却不是「执行乙」

D-53 里的「乙」= 删掉远端仓库、重建一个空的、把当前快照推上去，被否掉的理由有三条。
本轮的形状像乙，但那三条**逐条都被处理掉了**：

| 乙被否的理由 | 本轮怎么处理 |
|---|---|
| 要用户本人在网页上操作 | **本来就该由你做**：建仓与转私有都是你的动作，不是我代做 |
| 仓库地址的变化要同步到所有引用它的地方 | 地址收成一处（第 3 步）：换远端 = 改**一处代码 + 一处仓外针脚** |
| 「这里曾经有一座明文桥、怎么拆的」这段叙事随之消失 | **不消失**：旧历史原样留在本地的旧分支与仓外全量 bundle 里；台账那段叙事**一字未改**，只是编号变成标签、映射在仓外 |

★ 还有一处与乙**根本不同**：乙是**删掉**旧仓，本轮**不删**、只转私有。
可逆动作先做、不可逆动作后做 —— 这与 D-53 那条「甲和乙都买不到它们声称要买的东西」
是同一套判断：**转私有比删仓可逆，而两者买到的（新访客取不到旧对象）是同一件东西。**

### 买到什么、不买到什么（照实写，不替它吹）

| | |
|---|---|
| **买到** | **切断按编号取回旧明文这条路。** 新仓历史里没有那些明文；文档里再没有一个能解析回旧对象的编号（实测 0 处）；旧仓转私有之后，顺着编号点过去也取不到东西 |
| **不买到** | ① 工作区**内容一个字节没改**，「本工具层与那个存量项目有渊源」这层**暗示仍在** —— 那是「走丙」的净增量，本轮**没有动它，也不声称动了它**；② **已经分发出去的副本不会消失**：已经 clone / fork / 缓存过 / 被代码搜索索引过的，一次搬迁都收不回来 |

★ 第 ② 条正是 D-53 与 `BLOCKERS.md` 卡点 4 里「乙」那一栏自己写的警告，
**本轮同样适用，不因为换了做法就作废**。写在这里是为了让人不要误读本条的收益：
**它是可发现性，不是「数据不存在了」。**

### 负控改成两条独立证据（判据逐条对照）

编号搬走之后，「修复前那个提交还在不在」不该再是**唯一**证据。两条证据：

| 证据 | 怎么判 | 覆盖谁 |
|---|---|---|
| **甲：尺子当下可证其灵** | 仓外词表的样本**只在内存里**过一遍同一条扫描路径（`scan()` / `count_occurrences()`，**不落盘** —— 落盘就等于在仓里造一份明文），必须报红 | 任何机器，**不需要 git 历史** |
| **乙：旧历史没被无声抹掉** | 仍按原来的判据读那个对象：命中 > 0 → PASS；命中 = 0 → FAIL | 维护者本机 |

判据逐条对（左 = 改之前，右 = 改之后）：

| 状态 | 改之前 | 改之后 |
|---|---|---|
| 词表不可用 | SKIP | **SKIP**（原文不动） |
| 那个对象取得到，命中 > 0 | PASS | **PASS** |
| 那个对象取得到，命中 = 0 | FAIL | **FAIL** |
| 对象取不到，**本机有多笔历史** | FAIL「历史被重写过」 | **FAIL，措辞一字不改** |
| 对象取不到，**本机只有一条初始提交的历史** | FAIL「历史被重写过」 | **SKIP（未完成）** |
| 合成负控（甲）当场失灵 | （没有这一项） | **FAIL**（新增） |

★ **替代判据至少同样强** —— 这是本轮自己给自己划的线（计划里的停手边界：
**改判据只许平移或增强**）。逐条对下来：**FAIL 分支一条没删**（原 3 条，现 4 条），
**新增 1 条原来不存在的检测**，**唯一改掉的那一条原本判错**。所以是增强，不是放松。

第五行那一条为什么要改：那个状态下**并没有**「历史被重写过」的证据 ——
它只说明「这个克隆里没有那段历史」。在新仓的正常状态下它会**天天红**，
正是 D-53 依据③ 说的「一个永远红的检查会训练人忽略它」；
而且在新克隆的人那里，那是一句**不实的严重指控**。改判 SKIP 之后它落在本仓
自己那套「**未完成 ≠ 通过**」的口径里（D-34 / D-49）。
**把一条判错的判据改对，不等于把判据放松。**

★ 还有一处连带：`experiments/27_leak_fix_verify.py` 的退出码加了 **3（负控没有实测依据）**，
与闸门那套口径对齐；原来 1 号（FAIL）与 2 号（离线）不动。判定行照写「无实测依据」，
**不许**写成通过或失败。

### 地址：单点收敛，且不做兜底常量

`mcp_server/paths.py` 新增一节「本仓地址也收在这里」，写成**函数**（理由同 `data_root()`）：

- 环境变量 `MCP_TOOLKIT_REPO_URL` **优先** —— 说它以它为准，测试靠它把地址钉成确定值；
- 否则问本机的 `git remote get-url origin`，吃 `https://…` / `git@host:…` / `ssh://…` /
  `host/owner/name` / `owner/name` 几种写法；
- 仓外针脚（维护者留档里的 `REPO_URL`）是**仓外的最后一处**，仓内没有兜底常量；
- ★ **两处本机来源互相打架**（`origin` 指向一个仓、留档说另一个仓）→ 返回「读不到」，
  理由写「**切换做了一半**」。这是本轮**唯一新增的失败态**，它专门盯换远端换到一半这种中间态：
  挑错一个来源，下面几项就会去查**另一个仓库**并且报绿 —— 那比判「没查」危险得多。
- **不猜**：解析不到就返回 `None`，由调用方判 SKIP / ERROR，沿用仓外词表那套
  「读不到不等于通过」（D-37 / D-46）。

**不动的地方**（都不是「仓库地址」，改语义会变）：`mcp_server/auth.py` 的
`MCP_JWT_ISSUER` 默认值（令牌签发语义）、`mcp_server/selfcheck.py` 的 `WORDLIST_FILENAME`
（**仓外词表文件**的名字约定）。

**台账里那几处写死的远端地址照旧留着不改**：它们记的是「当时推到了哪里」，是事实；
由 `BLOCKERS.md` 卡点 4 的补记说明现在的地址在哪儿。

### 与 D-53 两个解锁条件的关系（照实对账，不替自己开脱）

D-53 写的是「将来若真要**重写历史**，必须先做完这两步」。**本轮没有重写历史**，
所以这两步**不是**本轮的准入条件 —— 但本轮正好把它们碰掉了一大半，逐条对：

1. **「把负控从依赖真实历史提交改成合成夹具……同时改掉第 15 项的判据，让它读那个合成夹具，
   而不是读『修复前那个历史提交还在不在』」**
   → **目的达成，手段不同**。目的（重写历史不再拆掉自己的证据链）达成了：
   脚本里**不再有任何写死的编号**，合成证据甲在任何机器上都能跑。
   但手段**没有**照原文办：第 15 项**仍然**读那个真实对象（就是证据乙）。
   **理由是照原文办会变弱** —— 如果把「旧历史没被无声抹掉」也换成合成夹具，
   那么「本机存着旧历史却取不到那个对象」这个**真**故障就再没有任何一条检查看着它了：
   合成夹具永远造得出来，所以它永远绿。
   ★ 也就是说，D-53 的解锁条件① 写的是**怎么让重写变安全**；本轮做的是
   **不重写、但让证据链多一条腿**。两者方向一致、强度不低，但**不是同一件事**，
   这一句必须写明，不能含混过去。
2. **「把台账里所有引用历史 sha 的段落整体标注为『重写前记录，重写后 sha 全部失效』。
   ★ 标注，不是改写」**
   → **被更强的一步取代了**。实际做的是**把编号整体搬出仓库**（标签化 + 仓外映射表），
   不是「标注失效」。比标注强：标注之后编号还在文档里，7 位缩写在托管站点上照样解析；
   搬走之后解析不了（实测 0 处）。
   ★ 代价一起写清楚：原句进了**仓外**那份对照表，所以**只看仓库**的人
   查不到某个标签对应的原编号是哪个对象了 —— 这是本轮**有意**接受的代价，
   它换来的是「钥匙不再挂在墙上」。

### 停手边界（本轮实际用到的）

- 不许 `--force` / 不许任何历史重写 / 不创建、不删除、不改名远端仓 / 不改可见性 —— 一条都没碰；
- **改判据只许平移或增强**：出现「唯一的出路是放松或删掉某条判据」就停下交还（本轮没出现）；
- 某条判据的唯一出路是**让某个编号继续留在文档里** —— 那说明遇到的是台账的原始实测记录，
  停下问，不自行决定（本轮没出现：72 处全部搬得动，因为**没有一处编号本身就是读数**）。

### 执行记（2026-09-19）：闸门 4 通过并执行完毕；闸门 5 仍未做

用户给出新仓地址（`https://github.com/Hone125/mcp-gatekeeper`）并明确授权推送之后，
上面那张时序表的第 4 步**已按原样执行**；**第 5 步（旧仓转私有）没有做**，仍等用户。
详细读数与逐条命令写在 `PROGRESS.md`「迁移本身（2026-09-19）」那一节，这里只记**决策层面的状态**：

| 项 | 状态 |
|---|---|
| 新仓是不是空的 | **是**（`size == 0` 且提交列表 HTTP 409）。★ 这是普通推送唯一的前提：不空就是非快进，而唯一出路是硬禁止的 `--force` |
| 推快照 | `git push origin publish:main` → `* [new branch]`，**一次 `--force` 都没用** |
| 把签出的分支指到快照 | `git reset --soft publish`（不是 `git branch -f`：git 拒绝移动签出中的分支）。树相同，所以工作区与索引一个字节没动 |
| 换地址 | `origin` 与**仓外留档的 `REPO_URL`** 同一口气改完 —— 这是「切换做了一半」那个失败态要求的 |
| 迁移改不改内容 | 两条独立证据：**树相同** + 推送后逐字节三方核对 **16/16** |
| 旧仓 | **没转私有、没删**；删不删以后再定 |
| 这一轮碰了哪些硬禁止 | **一条都没碰**：无 `--force`、无历史重写、无创建/删除/改名远端仓、无可见性变更 |

★ **计划与事实有两处不符**（照实记 —— 「计划里写的形状」不等于「当时的仓库状态」）：

1. 计划把「建 `publish` 分支」写在时序第 2 步，实际它**在本地准备那一轮就已经存在**了；
2. 仓外那份全量 bundle 是**先打的**、不含本轮的新提交 —— 预检把它查了出来，于是**重打了一次**。
   顶掉旧文件之前先证明「旧文件里那几个 head 都是当前 `HEAD` 的祖先」。
   ★ 第一版的「含不含」判据比的是 ref **尖端**而不是**可达性**，报了一次假警
   （「★否——先别顶掉」）；按它停手、查清之后才顶掉。

★ **在新 clone 的机器上跑出的三个口子**（交付级第 15 项判「未完成」与 `27` 退 3 都是**设计如此**；
另两处是真口子：一条用例直接断言 PASS、`19_verify` 把「示例库未构建」写成 FAIL）。
后者的修法用的是台账本来就有的 `alt` 机制、`expect` 一个字没松 —— 逐条见 `PROGRESS.md`。
写在这里是因为它**动了判据表**：判据表是「判定别人」的那段代码，它的任何改动都该在决策档里留痕。

### 与前面几条的关系

- **D-53** 记的是「不重写历史、为什么、以及解锁条件」；本条记的是**换一个轴**做同一件事：
  D-53 处理的是**历史里**的明文（结论：不动），本条处理的是**文档里指向它的编号**（结论：搬走）。
- **D-37** 记的是「停在推送这一步：不猜 URL」；本条的地址收敛是它在代码上的落地。
- **D-46** 记的是「词表住在仓库外面」；本条把**编号**也搬到了仓库外面，
  用的是**同一套解析形状**（环境变量 → 仓外约定路径 → 返回 `None` 不抛不猜），
  以及同一条纪律：读不到时**不回显**原值、不把路径写进报告。
- **D-44 / D-49** 记的是「SKIP ≠ PASS」；本条新增的「切换做了一半」与退出码 3 都归这一族。
