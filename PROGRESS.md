# 进度台账

> 这个文件在每个阶段结束时更新。会话随时可能中断，**以本文件 + `reports/` 里的产物为准**，
> 而不是以对话记录为准。

| 阶段 | 内容 | 状态 | 产物 | 下一步 |
|---|---|---|---|---|
| 0 | 初始化仓库骨架 | 已完成 | `.gitignore` `PROGRESS.md` `.env.example` `requirements.txt` | — |
| 1 | 重写通用组件（鉴权 / 护栏 / 账本 / 重试 / 路径） | 已完成 | `mcp_server/{paths,auth,guardrails,llm_retry,cost}.py`、`tests/` | — |
| 2 | 接入公开数据（示例库 + 公版文本） | 已完成 | `data/`、`experiments/00~03`、`mcp_server/{db_tools,kb_tools}.py`、`NOTICE.md`、`LICENSE`、`DECISIONS.md` | — |
| 3 | 协议层与安全层（**不需要 LLM，必须 100% 完成**） | 已完成 | `mcp_server/{t2sql_core,server}.py`、`reports/{auth,guardrail,mcp_smoke}_results.*`、`reports/mcp_smoke.*` | — |
| 4 | Text2SQL 链路（需要 LLM） | **已完成**（2026-09-18 配好凭据后跑完；逐条数字见 `RESULTS.md` 第二节与本文件「阶段 4 真实运行」） | `mcp_server/{eval_grading,eval_runner,env_file}.py`、`data/eval/questions{,_colloquial}.json`、`experiments/{09,12,13,20,21}_*.py`、`reports/text2sql_results.{v1,v2,v3}.json`、`reports/{text2sql_results,colloquial_vs_formal,pair_test,noise_band,cost_report}.md/json` | — |
| 5 | 评测 harness 与自检 | 已完成 | `mcp_server/{selfcheck,verify_kit,deliverable_kit}.py`、`experiments/{19_verify,25_deliverable_check}.py`、`tools/check.py`、`reports/{verify,deliverable_check}.md`、`docs/design.md` | — |
| 6 | 文档、许可、收尾自检 | 已完成 | `README.md` `RESULTS.md` `experiments/26_final_selfcheck.py`、`reports/final_selfcheck.md` | — |
| 7 | 发布准备 | **已完成：已推送到 `Hone125/mcp-guarded-toolkit`**（2026-09-18） | `BLOCKERS.md`（卡点 2 的探测记录保留，状态改为已解除） | — |
| 8 | 迁移准备：编号搬出仓库、地址收成一处、负控改成两条证据 | **已完成**（2026-09-19）：本地三步见「迁移准备（2026-09-19）」；「建新仓 → 推快照 → 旧仓转私有」两处**用户闸门均已执行**，见「迁移本身（2026-09-19）」与本文件「十一、闸门 5 已由用户执行」 | `mcp_server/paths.py`、`experiments/27_leak_fix_verify.py`、`mcp_server/delivery_selfcheck.py`、`DECISIONS.md` D-54 | — |

## 已知的中间态（不是缺陷）

- `tests/test_auth_matrix.py::test_auth_experiment_passes_and_covers_both_sides`
  在阶段 3 之前**必然失败** —— 它跑的是 `experiments/10_auth_test.py`，那个脚本阶段 3 才写。
  **阶段 3 结束时已按预期转绿**，见下。

## 阶段 6 明细（已完成）

### 建了什么

- [x] `README.md` —— 是什么、怎么跑、工具表（6 个工具 × 3 个 scope）、目录结构、
      数据与许可、怎么证明它是对的、当前完成状态、已知边界
- [x] `RESULTS.md` —— 全部实测数字，逐条注明「脚本 + 口径」；
      需要模型凭据的部分单列一节（**2026-09-18 之前**那一节写的是「未完成」且不含任何数字；
      凭据到位跑完之后，那一节已换成实测值）
- [x] `experiments/26_final_selfcheck.py` —— 发布前 9 项逐条报命中数
- [x] `reports/final_selfcheck.md` —— 9 项的判据、命令、命中数、命中行
- [x] `reports/deliverable_check.md` —— 交付物核验 8 条，**全部 PASS**
- [x] 25 的退出码台账条目从「挂 alt」改回 `expect 0`：README 已落地，它不再有
      「合理地不通过」的理由

### 阶段 6 实测数字（口径见各脚本的文件头）

| 数字 | 值 | 口径 / 出处 |
|---|---|---|
| 收尾自检 9 项 | PASS 9 / FAIL 0 / ERROR 0，退出码 0 | `26_final_selfcheck.py`：1~7 数命中行数、8~9 数子进程退出码 |
| 9 项的命中数 | 全部 0 | 同上：0 命中的确切含义是「词表里的东西一处都没有」，不是「绝对干净」 |
| 报数字的报告 | 12 份，其中 1 份无数字（天然豁免） | 同上第 6 项：`selfcheck.audit_report_numbers()` |
| 交付物核验 | 8 条：PASS 8 / FAIL 0 / SKIP 0，退出码 0 | `25_deliverable_check.py` |
| G4 扫到的命令 | 45 条 | 同上：README 与 RESULTS 里每条 `python <脚本>.py --flag` 都核过 |
| G6 查的仓库内路径 | 14 个，全部存在 | 同上 |
| G7 查的引用 | 15 处：11 处文件存在、4 处当场说明未生成 | 同上 |
| 台账条数 | 32（PASS 28 / FAIL 0 / SKIP 4 / ERROR 0） | `19_verify.py`，退出码 0 |
| 本仓 pytest | **521 passed / 0 skipped** | `python -m pytest -q` |
| 守门链 | 4 步全过，退出码 0 | `tools/check.py` |

### 阶段 6 抓到的真问题

1. **新写的 `README.md` 第一轮就被 G6 红了一条** —— 里面引用了当时还不存在的
   `RESULTS.md`。这正是这条检查存在的意义：**写文档的人最容易高估「我已经写了」**。
   处置不是放宽检查，而是把 `RESULTS.md` 补齐。

2. **退出码 `4` 撞车** —— 把收尾自检加成第 4 步之后，「用法错误」的退出码 4 就和
   「卡在第 4 步」撞了。一个会和正常结果撞车的错误码比没有错误码更糟：
   读到 4 的人会去查收尾自检，而真正的问题是他把参数敲错了。
   改成 90，并补了一条断言守着它（`test_the_usage_exit_code_cannot_collide_with_a_step_number`）。

3. **`RESULTS.md` 里的数字在改完之后就过期了** —— 它写着 511 passed / 1 skipped，
   而那之后测试又多了两条。这类「文档里的数字」没有机械检查能兜住（数字对不对
   需要重跑才知道），所以规矩是：**数字只从脚本输出里抄，改完代码就重抄一遍**。
   这一轮照做了。

### 最近一次全量验证（阶段 6 收尾）

| 命令 | 退出码 | 说明 |
|---|---|---|
| `python -m pytest` | 0 | **520 passed / 0 skipped**（阶段 5 收尾时是 511 passed / 1 skipped） |
| `experiments/19_verify.py` | 0 | 32 条：PASS 28 / FAIL 0 / SKIP 4 / ERROR 0 |
| `experiments/25_deliverable_check.py` | **0** | 8 条全过（阶段 5 时以 5 结束，因为 README 还没写） |
| `experiments/26_final_selfcheck.py` | 0 | 9 项全过 |
| `tools/check.py` | **0** | 4 步全过 —— **可以交出去** |

## 阶段 7 明细（停在停手条件 c）

### 做了什么

- [x] **探测凭据**（只判在不在，不打印任何值）→ 见下表
- [x] 把 `BLOCKERS.md` 卡点 2 补成「探测记录 + 可复制的两行 + 做完该看到什么」
- [x] `BLOCKERS.md` 里两处过期数字重抄：`439 passed / 1 skipped` → **521 passed / 0 skipped**；
      引用报错原文时补上漏掉的尾巴 `（见 .env.example）`
- [x] 确认本地全绿（本节的表）
- [ ] **push 本身** —— 做不了，原因见下

### 探测结果（2026-09-18 实测）

| 探测项 | 结果 |
|---|---|
| `GITHUB_TOKEN` / `GH_TOKEN` / `GIT_ASKPASS` | 三个环境变量都是空 |
| `gh` 命令 | 不存在 |
| `~/.git-credentials`、`~/.config/gh/hosts.yml` | 都不存在 |
| `~/.ssh/id_rsa`、`~/.ssh/id_ed25519` | 都不存在 |
| `git remote -v` | 无输出 |
| `git config --get credential.helper` | `manager`（只读，未改动） |

### 为什么停在这里（而不是再想办法）

1. **建远端仓库需要一个登录态**：API 走 `GITHUB_TOKEN`（空），网页走浏览器登录（我没有），
   SSH 走密钥（没有）。三条路都不通，这是**停手条件 c**。
2. **不能猜 URL**：远端地址里含账号名，我不知道你的账号，也不该猜一个。
   推错地址是不可逆的对外副作用 —— 正是红线里那条。
3. **不改全局 git 配置**：`credential.helper=manager`、`http.postBuffer` 等都是全局设置，
   本任务的约定是只读。**要推送这件事，不需要动它们中的任何一个**，所以没有任何
   「为了方便先改一下」的理由。

### 阶段 7 抓到的两个真问题

1. **产物里混着时钟读数，所以「跑完 `git status` 干净」这句话本来是不成立的。**
   `pytest` 的尾巴（`N passed in …s`）和评测脚本每道题后面的 `calls=1 0.02s`
   被抄进了 `reports/`，于是**每跑一次闸门就有十来个产物显示「已修改」**。
   后果不止是难看：`git status` 因此不再是「有没有变化」的信号，
   而「跑完还是干净的」是这个仓库最能自证的一句话。
   处置：**别把耗时写进落盘产物**（打印出来给人看可以），引用子进程输出时走
   `verify_kit.tail_for_report()` 掩码，再加一条静态检查守着
   （`test_the_committed_artifacts_carry_no_wall_clock_reading`）。
   现在有了可重跑的判据：`tools/check.py --snapshot` 跑前跑后哈希相同。

2. **`08_mcp_smoke.py` 的期望栏写着一条从来没被满足过的判据，旁边却是一个 ✅。**
   它写着「5 秒内失败并给出原因」，而实测一直是 7.0 秒（`dt` 从起子进程之前
   开始计时，所以天然比 5 秒大一截）。判据是 `dt < 3×`，所以它一直没红。
   一个读报告的人看到「期望 5 秒 / 实测 7.0 秒 / 结果 ✅」只会更糊涂。
   处置：期望栏改成真实契约（**大于硬超时下界、小于 3× 上界**），
   并且把下界也变成断言 —— 这一条的重点本来就是「**真的等了**，
   而不是很快失败了」，只判上界的话，一个根本没等待的实现也能通过。

### 阶段 7 的一次红转绿（照实记，没查到的那部分也写下来）

修好上面第 1 条之后，闸门**还红过一轮**（第 1 步 pytest 有 1 条失败、19 报 FAIL 1），
而再跑一遍就绿了，中间没有改过任何代码。能确定的部分：

- 第 4 步（收尾自检）在**同一次运行里**报 9/9 全过 —— 它自己会再跑一次 pytest 与 19；
- 也就是说，**19 重新生成 `reports/verify.md` 之后 pytest 就绿了**；
- 所以那条测试读的多半是这份报告：上一轮 S3 报红时写下的那一版里，
  还带着上一轮的痕迹（报告成为载体的同一种回路，见 `DECISIONS.md` D-30）。

**没查到的部分**：具体是哪条测试红的。原因很直接 —— 闸门失败时只留了最后 3 行
输出，而 `FAILED` 那一行正好被切掉了。**这一条按「没查到」记，不编解释**
（处置见 `DECISIONS.md` D-41：闸门现在专门把 `FAILED` / `ERROR` 行捞回来）。

**→ 后来查到了（2026-09-18 推送之后）**：那条红根本不是测试失败 ——
是本机环境里的一个删除钩子拦下了 pytest 清理临时目录的批量删除，
pytest 因此以退出码 1 结束，所以尾巴里永远不会有 `FAILED` 行可捞。
完整证据链（6 条实测 + 1 条标为假设的推断）见 `DECISIONS.md` D-42。

上面那段**不删** —— 当时确实没查到，那是当时的真实状态。
把后来查到的结果直接顶替掉，就等于假装当时就知道。

另外查到并修掉了一个**真的会漏东西**的顺序问题：原来调用方写的是
`tail_for_report(x)[:300]`，先截断会把绝对路径切成半截 `C:\Users` ——
掩码认不出、检查也认不出，于是它一直躺在报告里没人报。
现在截断收进函数内部，顺序固定为「先掩码、再截断」，并配了负控
（见 `DECISIONS.md` D-40）。

### 这一阶段的产物是什么

不是代码，是**一份别人照着做就能接上的交接单**：`BLOCKERS.md` 卡点 2。
判据是「照着它做的人需不需要回来问我」—— 不需要，因为它精确到了
「把 `<你的账号>` 换成你的账号，然后逐行执行这两行」，并写了做完之后应该看到什么。

### 最近一次全量验证（阶段 7 收尾）

| 命令 | 退出码 | 说明 |
|---|---|---|
| `python -m pytest -q` | 0 | 521 passed / 0 skipped |
| `experiments/19_verify.py` | 0 | 32 条：PASS 28 / FAIL 0 / SKIP 4 / ERROR 0 |
| `experiments/25_deliverable_check.py` | 0 | 8 条全过 |
| `experiments/26_final_selfcheck.py` | 0 | 9 项全过 |
| `tools/check.py` | **0** | 4 步全过 —— **可以交出去** |

### 提交之后又验了一遍「跑完闸门工作区是干净的」

`RESULTS.md` 第三节写着「跑完之后工作区应当是干净的」。这句话在**提交之前**
是没法验的 —— 提交前工作区本来就有一堆没提交的改动，脏不脏说明不了任何事。
所以两笔提交做完之后重跑了一遍：

| 步骤 | 实测 |
|---|---|
| 跑闸门前的聚合哈希 | `eacfbe7e…7310`（28 个文件） |
| `tools/check.py` | 退出码 0，4 步全过 |
| 跑闸门后的聚合哈希 | `eacfbe7e…7310`（**同一个**） |
| `git status --short` | **无输出**（干净） |

这一条是**阶段 7 新加的声明**（阶段 6 时还没有），所以它值得单独记一次：
声明写进文档、再在提交后实测一遍，两件事都要做。

### 这两笔提交怎么分的

- `旧编号·K` 代码与产物（`README.md` / `RESULTS.md` / 各脚本 / `tests/` / `reports/`）
- `旧编号·L` 台账（`PROGRESS.md` / `DECISIONS.md` / `BLOCKERS.md`）

分两笔的理由：台账那三份是**关于**这批改动的记录，混在一起的话
「代码干了什么」要翻过一大段中文才看得见。`data/` 一个字节都没动。

### 推送本身（2026-09-18）

| 项 | 值 |
|---|---|
| 远端 | `https://github.com/Hone125/mcp-guarded-toolkit`（public） |
| 建仓时的状态 | 空壳（API 报 `size: 0`、`/commits` 返回 409 = 空仓）→ 不存在分叉，不用先合并 |
| 推送前核对 | 本地 HEAD 与远端 `refs/heads/main` 同为 `旧编号·A` |
| 命令 | `git push -u origin main`，退出码 0 |
| 推上去的 | 10 个提交 |

推送时工作区里**有一处未提交的改动**（`reports/verify.md`，就是 D-42 那条环境钩子留下的现场）。
推送推的是**提交**而不是工作区，所以远端拿到的是干净的那一版；
但这处改动的存在本身说明：「跑完闸门工作区是干净的」这句话**有例外**，
例外来自环境而不是仓库。这一点照实记进 `DECISIONS.md` D-42 与 `RESULTS.md` 第三节，
**没有把声明悄悄删掉**。

### 推送之后顺手查出来的东西

准备推送时发现 `git status` 冒出一处改动，追下去把阶段 7 那条悬案结了（D-42）。
一句话版：**那次红不是测试失败，是环境的删除钩子拦了 pytest 清理临时文件**，
所以尾巴里从来没有 `FAILED` 行可捞 —— 当时「没查到」是真的没查到，不是没看。

## 推送之后第二轮收尾（2026-09-18）：把闸门推回全绿

推送**不是终点**：推上去之后又改了一轮，起因是「自毒回路」那条（D-45）。
这一轮的过程记在这里，因为它的形态很典型 —— **三条检查一起红，根因只有一个**。

### 起因与根因

上一轮把 D-45 的漏网之鱼修完之后，`pytest` 绿了，但 `19_verify` / `25` / `26`
**仍然各红一条**。三条一起红看着像三个问题，实际是同一个：`25` 的 G7
报「4 处指向不存在的文件且没交代」，另外两条是在跑 `25` 而已。

四处命中分成**两组，成因完全不同**：

| 组 | 命中 | 成因 |
|---|---|---|
| 一（3 处） | `BLOCKERS.md` 表格里那三份 `*_NOT_RUN.md` | 那三份文件**确实**已按设计删除、表格下方也**确实**写了「现在不存在」，但 G7 按**空行**切自然段，整张表格是一块、★ 那段是另一块 —— 「就近交代」够不着 |
| 二（1 处） | `reports/final_selfcheck.md:46` | **上一轮的产物**在这一轮被当成了本轮的声明。`26` 把当时 `DECISIONS.md` 里的一行**引用**抄进了 `final_selfcheck.md`；我改了 `DECISIONS.md` 之后，`25`（跑在 `26` **前面**）读到的还是**上一轮那份**产物 |

**处置**：第一组把交代写进**单元格内部**（不依赖分段实现）；第二组**不加特例**，
先把 `26` 单独跑一次把产物刷新到当前文档，再跑闸门 —— 它滞后一轮、自己会好，
加特例反而等于承认「生成物里的引用不算引用」。机理与理由见 `DECISIONS.md` D-45 末尾。

### 本轮的实测数字（口径 = 脚本 + 命令）

| 命令 | 退出码 | 实测 |
|---|---|---|
| `python -m pytest -q` | 0 | **530 passed / 0 skipped** |
| `experiments/19_verify.py` | 0 | 32 条：**PASS 29** / FAIL 0 / **SKIP 3** / ERROR 0 |
| `experiments/25_deliverable_check.py` | 0 | 8 条：PASS 8 / FAIL 0 / SKIP 0 |
| `experiments/26_final_selfcheck.py` | 0 | 9 项：PASS 9 / FAIL 0 / ERROR 0 |
| `tools/check.py` | **0** | 4 步全过 |

**上面那三条 SKIP 换过一次理由**，这一点值得记：阶段 7 时它们是
「需要模型凭据、尚未重跑」；凭据跑通之后，三条实测产物都落在盘上了，
于是理由变成**同一个**：「本轮未重跑；盘上已有实测产物 `reports/xxx`」
（只读台账 + `--rerun-llm` 显式开关，见 `DECISIONS.md` D-44）。条数从 4 变 3
是同一件事的另一面 —— 账本为空那条（`12_cost_report.py`）现在能正常跑完了。

### 「产物可复现」这条声明，这轮真的测了

`RESULTS.md` 第一节那行写着「跑闸门前后的聚合哈希相同」。这句话在阶段 6
写进去的时候是个**声明**，这轮按它自己给的口径实测了一遍：

| 步骤 | 实测 |
|---|---|
| `tools/check.py`（第 1 遍） | 退出码 0，4 步全过 |
| `tools/check.py --snapshot` | `48b91b92…d86a`（**38 个文件**） |
| `tools/check.py`（第 2 遍） | 退出码 0，4 步全过 |
| `tools/check.py --snapshot` | `48b91b92…d86a`（**同一个**） |

两遍相同 = 同一份代码跑两遍、`reports/` 下 38 个文件逐字节一样。

★ **这个哈希值不是写死的常量，是抄下来的读数** —— 它随文档内容变
（改一句话，`25` 的报告里那行「48 条命令 / 21 处引用」就可能跟着变）。
所以上面这张表记的是**那一刻**的两遍一致性；要判断现在一不一致，
得按 `RESULTS.md` 第一节给的三行命令自己跑一遍，而不是拿这里的字符串去比。

### 这一轮还改掉的一处文档与现实不一致

`BLOCKERS.md` 的**卡点 1 一直挂着「没有可用的模型凭据」**，而它其实早就跑通了
（卡点 2 当时已经按文件自己规定的格式改成了「已解除」，卡点 1 漏了）。
但改的时候顺手探了一下本机现在的状态，发现**不能简单写成「已解除」**：

| 探测项 | 实测 |
|---|---|
| 当前进程里的 `LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL` | 三项**都为空** |
| 用户级持久环境变量里的 `LLM_*` | **没有** |
| 机器级持久环境变量里的 `LLM_*` | **没有** |
| 仓库根 `.env` | **不存在** |

而 `mcp_server/env_file.py` **只读仓库根的那一份 `.env`**（它读不到仓库之外的文件）。
所以真实状态是：**跑通过一次，但本机现在凭据又不可达，重跑仍会退出码 5。**
根因是两件事本来就互斥 —— 跑评测要机器上有密钥，发布自检要求工作区里没有密钥文件。
照实写成「已跑通一次，但本机现在又不可达」比写成「已解除」准确，
也让下一个人不必先踩一遍「明明跑通过，怎么现在又不行」。

### 推送本身（第二轮，2026-09-18）

| 项 | 值 |
|---|---|
| 远端 | `https://github.com/Hone125/mcp-guarded-toolkit`（第二轮没有新建远端） |
| 命令 | `git push origin main`，退出码 0 |
| 推上去的 | `旧编号·M → 旧编号·G`，**2 个提交**（代码与产物 / 台账） |
| 推送前核对 | 本地 HEAD 与远端 `refs/heads/main` 同为 `旧编号·M`（也就是只多这两笔） |
| 推送后核对 | 本地 HEAD 与远端 `refs/heads/main` 同为 `旧编号·G` |

**提交之后又验了一遍「跑完闸门工作区是干净的」**（同上一条的理由，
这句话只有提交之后才验得了）：

| 步骤 | 实测 |
|---|---|
| `git status --short`（提交后） | 无输出 |
| `tools/check.py` | 退出码 0，4 步全过 |
| `git status --short`（跑完闸门后） | **无输出** |

## 迁移准备（2026-09-19）：把钥匙从墙上摘下来

这一轮**工作区的内容一个字节没改**。改的是三件「围着内容转」的事：
历史编号放在哪、仓库地址从哪读、负控靠什么证明自己还灵。

起因在 `BLOCKERS.md` 卡点 4：**工作区一直是干净的，而钥匙一直挂在公开文档里** ——
文档逐字引用了修复前那些历史提交的编号，而 **7 位缩写在托管站点上照样解析回旧对象**，
所以「只写缩写」不构成遮挡。判定（不重写历史）一个字没松，动的是编号放在哪里。
决策记在 `DECISIONS.md` D-54。

### 三件事

| # | 做什么 | 关键性质 |
|---|---|---|
| 1 | **干净快照**：用 `git commit-tree` 从当前树造一条初始提交，挂在本地分支上（**不动工作区、不动索引、不动 `HEAD`**） | ★ **新仓那一条提交的树 == 现在的树**：两个分支的 `^{tree}` 必须相等 —— **迁移不改内容，只改历史** |
| 2 | **编号搬去仓外**：台账与产物里的历史编号改写成标签 `旧编号·X`，原编号与它所在的原句留仓外 | 编号在仓内**归零** |
| 3 | **地址收成一处**：`mcp_server/paths.py` 新增 `repo_slug()` 一族 | 换远端 = 改**一处代码 + 一处仓外针脚**；仓内没有写死的地址 |

### 本轮的实测数字（口径 = 脚本 + 命令）

| 命令 | 退出码 | 实测 |
|---|---|---|
| `python -m pytest -q` | 0 | **598 passed / 0 skipped** |
| `experiments/19_verify.py` | 0 | 32 条：PASS 29 / FAIL 0 / **SKIP 3** / ERROR 0 |
| `experiments/25_deliverable_check.py` | 0 | 8 条：PASS 8 / FAIL 0 / SKIP 0 |
| `experiments/26_final_selfcheck.py` | 0 | 9 项：PASS 9；另交付级 15 项 **PASS 13 / FAIL 0 / SKIP 2**（闸门里那 2 项不联网，一定是 SKIP） |
| `experiments/27_leak_fix_verify.py` | 0 | 见下表 |
| `experiments/28_delivery_selfcheck.py --online` | 0 | 15 项：**PASS 15 / FAIL 0 / SKIP 0 / ERROR 0** |
| `python tools/check.py`（已设 `DELIVERY_RESUME_DIR`） | **0** | 4 步全过 |

`27` 那一份的逐行读数（**两条负控都在**）：

| 行 | 判定 | 读数 |
|---|---|---|
| 1 工作区 | PASS | 命中 0 条 / 出现 0 次 |
| **负控甲**（三段已知含词的合成样本，**只在内存里**过扫描器） | PASS | 命中 **5** 条 / 出现 **5** 次 |
| **负控乙**（修复前那一版里的同一个文件） | PASS | 命中 **50** 条 / 出现 **88** 次 |
| 2 公网 raw 上的同一个文件 | PASS | 命中 0 条 / 出现 0 次 |
| 3 本地与公网是不是**同一份** | PASS | 命中 0 条 / 出现 0 次 |

**pytest 条数 570 → 598（+28），逐条说明**（§8 第 12 项要的那张表）：
地址解析族 **20** 条（`tests/test_paths.py`：7 条认得出来的写法 + 6 条认不出来的 +
「不回显原值」「两处来源打架判读不到」「环境变量优先」「读不到时既不是通过也不是猜」
「仓外留档只当数据读、不执行」「修复前编号只能来自仓外」「仓外读不到时那句话不带路径」）；
交付级用例 **7** 条（地址没有时第 4/8/13 项判 SKIP、旧历史不在本机时第 15 项判 SKIP、
尺子失灵时第 15 项判 FAIL、地址有但没读到仍是 ERROR、第 13 项只印指纹）；
指纹 **1** 条（`tests/test_repo_hygiene.py`）。**没有删任何一条用例。**

### 摘钥匙这件事本身的读数（口径：仓外临时脚本，对 7 个文件扫 `[0-9a-f]{7,40}`）

| 量 | 搬之前 | 搬之后 |
|---|---|---|
| 不同的编号对象 | **22** 个（19 个提交号 + 2 个 blob 哈希 + 1 个属于另一个仓库） | 0 |
| 出现处数 | **72** 处（同一个扫描按「行」数是 57 行） | 0 |
| 涉及文件 | **7** 个 | 0 |
| 还能解析回真实对象的编号 | 22 | **0**（形态像编号的串还剩 **67** 处，逐个 `git cat-file -t` 全部解析不到 —— 那是产物哈希与第三方数据里的十六进制串，**要留着**） |

★ 第三、四轮台账里那组「7 个文件 / 14 个提交 / 3 处完整 40 位」**与本表对不上，
而且复算不出来**（两个口径都算过）。它按作废处理，订正写在本表与
`BLOCKERS.md` 卡点 4 的 2026-09-19 补记里 —— **复算不出来的读数不许继续引用。**

### 「产物可复现」这条声明，这轮又测了一遍

| 步骤 | 实测 |
|---|---|
| `tools/check.py --snapshot` | `2c2532ed…bfdc3`（**41 个文件**） |
| `tools/check.py` | 退出码 0，4 步全过 |
| `tools/check.py --snapshot` | `2c2532ed…bfdc3`（**同一个**） |

★ 同上一轮那句话：**这个哈希是抄下来的读数，不是常量**（改这里的任何一句话它都会变）。
要判断现在一不一致，按上面三行命令自己跑一遍。
★ 再补一句更具体的：**写完这一段之后，上面那个值就跟现在的读数对不上了** ——
因为「记下这一段」这件事本身改了台账，台账又进了 `25` / `26` 的产物。
**这不是产物不可复现**：可复现说的是「同一份代码跑两遍，前后两个哈希相同」，
上面那一对就是这么量出来的；换一份代码（哪怕只改一句话）本来就该是另一个值。

★ 同一类联动再记一次（上一轮也发生过，见「这一节自己也带出了一处联动」）：
把本节的命令写进台账之后，交付物核验的 **G4**（全仓文档里的命令条数）从 **66** 变成 **67**，
**G7**（引用的报告数）仍是 **24**。两处都已按实跑刷新，**判据没有放宽**。

### 本轮抓到的三个真问题

**① 搬完编号之后，是「产物」把钥匙又挂回了墙上。**
第一次重跑 `28 --online` 之后，用同一支扫描脚本再扫一遍，报出 **4 处能解析的编号** ——
全在 `reports/delivery_selfcheck.{md,json}` 里，是**第 13 项**自己写进去的：
它把「本地与远端是同一个提交」这句话写成了 `本地 xxxxxxx / 远端 xxxxxxx`。
也就是说：**把 22 个旧编号从文档里摘下来，产物当场又挂了一个上去。**
修法是加一个 `selfcheck.sha_fingerprint()`：产物里只印 `sha256(编号)[:12]` 的**指纹** ——
判定能力一点没丢（差一位指纹就不同），但它**不是钥匙**（单向，推不回对象），
而且在有仓库的人那里照样能复核（`git rev-parse HEAD` 再 sha256 一次，前 12 位对得上，
同 `DECISIONS.md` D-09 的口径）。负控写在 `tests/test_repo_hygiene.py`。

**② 我自己新写的用例踩了本仓自己的检查。**
`tests/test_paths.py` 里那条「认不出来时不回显原值」的用例，我拿一个
「盘符 + 用户目录」形状的串当样本 —— 那个形状正是本仓那条**不许出现作者机器绝对路径**的
检查（`ABS_PATH_PATTERN`）要拦的东西。闸门第 1 步当场报红，命中的就是**我那一行**。
修法是**样本攒出来、不写成字面量**（被测的字符串一个字节没变），
**没有去动那条检查的判据** —— 它拦得对。

**③ 新写的注释里把决策号引错了。**
`mcp_server/paths.py` 里「不猜远端地址」那条教义引的是 `D-30`，实际是 `D-37`。
改掉。**这类错自己不会红**，是核对时看出来的。

### 为什么负控要从一条改成两条（这一轮的核心）

编号搬走之后，「修复前那个提交还在不在」不该再是**唯一**证据：在新克隆的人那里
那段历史根本不在，这条证据天然缺席。所以：

| 证据 | 怎么判 | 覆盖谁 |
|---|---|---|
| **甲**：尺子当下可证其灵 | 仓外词表的样本**只在内存里**过一遍扫描器（**落盘就等于在仓里造一份明文**），必须报红 | 任何机器，**不需要 git 历史** |
| **乙**：旧历史没被无声抹掉 | 仍按原来的判据读那个对象（命中 > 0 → PASS；命中 = 0 → FAIL） | 维护者本机 |

★ **判定门槛只平移，不放松**，逐条对照表在 `DECISIONS.md` D-54：
FAIL 分支一条没删（原 3 条，现 4 条），**新增 1 条原来不存在的检测**，
唯一改掉的那一条（「本机只有一条初始提交的历史」）**原本判错** —— 它当时报的是
「历史被重写过」，而那在新仓的正常状态下是**一句不实的严重指控**；
现在判「无实测证据」，落在本仓自己那套「**未完成 ≠ 通过**」的口径里。

### 推送本身（2026-09-19）

| 项 | 值 |
|---|---|
| 远端 | `https://github.com/Hone125/mcp-guarded-toolkit`（这是**旧仓**，本轮仍然推它；新仓地址由用户提供后另起一轮） |
| 命令 | `git push origin main`，退出码 0 |
| 推上去的 | **代码与产物** / **台账** / **台账收尾**（本节这一段自己的实测读数），后面还跟着**推送之后**刷新产物的那一笔（见下一节）。代码与台账分两笔的理由同上一轮：混在一起时，「代码改了什么」要翻过一大段中文才看得见；后面那两笔都是「写完这一段之后才存在的东西」，只能跟在后面 |
| 推送前核对 | 本地 `HEAD` 与远端 `refs/heads/main` 同为同一个提交 |
| 推送后核对 | 本地与远端仍为同一个提交 —— 机械判据是交付级**第 13 项**，它报「一致/不一致」加**指纹** |

★ **这张表故意不写提交号**（前几轮写的是 `旧编号·X`）。理由就是本轮做的那件事：
7 位缩写在托管站点上能解析回对象，写进台账等于把钥匙挂回墙上。
想知道是哪个提交，第 13 项给了可复核的指纹。

### 推送之后（2026-09-19）：第 4、6、8 项，以及产物为什么要跟着再刷新一次

任务书那九项里有三项**只能在推送之后跑**：第 4 项（跟远端核对）、第 6 项（扫日志）、
第 8 项（逐字节三方核对）。它们跑出来的读数要落进台账，所以「推送本身」后面必然还跟着一笔 ——
本节就是那一笔的内容；这一笔提交之后，闸门再跑一遍、再普通推送一次。

| 项 | 口径（脚本 + 命令） | 读数 |
|---|---|---|
| 第 1 项 | `git status --short` | 推送之前是干净的；跑完第 4 项之后**有且只有两份产物**变成「已修改」：`reports/delivery_selfcheck.md` 与 `.json`。原因见下面「产物为什么慢一步」 |
| 第 4 项 | `experiments/28_delivery_selfcheck.py --online` | 15 项：通过 15 / 未完成 0 / 失败 0。第 13 项报「本地指纹 `c5a99db2f121` / 远端指纹 `c5a99db2f121`」—— 两边一致 |
| 第 6 项 | 仓外临时脚本；口径 = `git log -p` 的补丁文本比仓外那一整张表（识别性名称 + 领域术语 + 不许出现的数字，**数字带词边界**） | 见下 |
| 第 8 项 | 仓外临时脚本；口径 = 16 份文件各取一次 `sha256`，比工作区 / 本地对象库里的 blob（`git cat-file blob HEAD:<路径>`）/ 公开 raw 直链三方 | 见下 |

**第 6 项：三个口径分开报，混在一起会看错**

| 口径 | 补丁行数 | 命中行 | 其中新增行 |
|---|---|---|---|
| 全历史（`--all`） | 4959 | 3 | 1 |
| **本轮这几笔自己**（区间端点按标题与时间定位，不写提交号，理由同上一节） | 707 | **0** | **0** |
| 当下 `HEAD` 上那三份台账（整份文件） | BLOCKERS 545 行 / DECISIONS 1810 行 / PROGRESS 1045 行 | **0 / 0 / 0** | —— |

那 3 行命中的**类别**是仓外词表里的两条**识别性名称**（序号 #4、#5；值照规矩不打印）。
三行是**同一处**早期台账的文字：一句 `git -C ../<旧仓名> status --short` 的核验记录，
同一行里还引着那个第三方索引仓的完整 40 位对象号。它在补丁里出现两次
（`-` 一版、`+` 一版 —— 是后来某次改动把这一行重写过）外加一行上下文。
**本轮自己的补丁 0 命中、当下 `HEAD` 上 0 命中**，说明它只存在于**旧历史**里，后来已被改写掉。

★ 这一条要连着上一轮那个教训一起看：上一轮把「40 位十六进制里的数字段撞上数字型条目」
当成了真命中，那是**口径**的问题（数字型条目必须带词边界，否则读数被自己的口径污染）。
这一轮把口径统一到与真实扫描器一致，剩下的 3 行是**真的**名称命中，
但既不在本轮新增的文字里、也不在当下的工作区里。
也就是说：**本轮写进公开面的每一个字，过的是与闸门同一把尺子，读数是 0。**

**第 8 项：本地对象库与公开 raw 逐字节相同**

| 判定 | 份数 | 说明 |
|---|---|---|
| ① 工作区 ② blob ③ 公开 raw 三方一致 | 7 | 代码与测试那几份 |
| ② ③ 一致，① 只差换行符 | 7 | 本机 `core.autocrlf = true`：工作区是 CRLF、blob 是 LF；把工作区那份折成 LF 后与 blob 相同 |
| ② ③ 一致，① 比 ② 新（不只是换行符） | 2 | 就是那两份交付级产物 —— 第 4 项是推送之后跑的，它按新的 `HEAD` 重写了这两份 |
| ★ ② 与 ③ **不一致** | **0** | **这是本轮真正要证的那件事：推上去的 16 份文件，公开直链取回来的字节与本地对象库一模一样** |

只打印 `sha256` 的前 8 位：完整值不是钥匙（单向），但报告是公开产物，按老规矩只留可比对的前缀。

**产物为什么慢一步**

`reports/delivery_selfcheck.{md,json}` 里有一条读数是「本地 `HEAD` 与远端 `main` 是不是同一个提交」，
它只能**先知道 `HEAD` 是谁**才写得出来。于是它天然比自己慢一格：写这一节的时候，
它记的是**上一次**那个 `HEAD` 的指纹；这一节提交之后，`HEAD` 又前进一格。
这不是不一致，而是「产物要跑完才知道 `HEAD` 是谁」的必然滞后 ——
**而且任何人都能自己复算**：跑一次 `28 --online`，看第 13 项那行指纹，它报一致就是一致。

**这一轮又踩的一个坑（照实记）**

第 8 项第一版脚本把 `paths.repo_raw_url()` 的返回值当裸地址用了，
报出来的错是 `unknown url type: ('https` —— 看着像网络不通，其实是这个助手返回的是
`(值, 说明)` 二元组，调用方解错了。改掉之后 16 份全部取到。
**顺带一处自我订正**：那一版的判定列写死了「只差换行符」，有两行其实不是换行符的事；
表的措辞不该比读数更肯定，所以判定改成**先算真值再落字**。
这两处都写下来，是因为**读数由脚本给出，脚本错了读数就没有意义** ——
只把脚本改掉、不写出来，等于把一段假读数留在台账里。

**这一笔提交之前，闸门再跑一遍**

`tools/check.py`，退出码 0：pytest **598 通过 / 0 跳过**；`19_verify` **32 条**（通过 29 / 失败 0 / 未完成 3）；
`25` **8 条全通过**；`26` 的发布前红线 **9 项全通过**，交付级 **15 项**报「通过 6 / 未完成 9 / 失败 0」。

★ 交付级那 9 项「未完成」，是因为这一遍是**不带 `--online`** 的跑法 —— 跟远端有关的那几项
没有实测依据，按本仓规矩判**未完成**（不是通过、也不是失败）；带了 `--online` 的那一遍
就是上面第 4 项的 **15/15**。两个数**口径不同**，既不要互相印证，也不要当成矛盾。

### 迁移本身（2026-09-19）：`origin` 已换到新仓，历史自一条初始提交起算

闸门 4 走完了：用户给了一个**空的**新仓地址，并明确说「推」。本节记这一轮
**实际做了什么、每步的判据是什么、读数是多少**，以及**计划与事实不符的两处**（照实记）。

#### 一、推之前先只读预检（`preflight.py`：一次未认证的 GET + 几条本地查询，不推不改）

| 查什么 | 判据 | 读数 |
|---|---|---|
| 新仓是不是**空的** | `size == 0` **且** 提交列表 HTTP **409**「Git Repository is empty」 | **是空仓** —— 所以普通推送能被接受 |
| 本机待推状态 | 本地 `HEAD` vs `origin/main`；`publish^{tree}` vs `main^{tree}` | 同一个提交；两个分支**树相同** |
| 仓外全量备份 | `git bundle verify` + 里面含不含当前的分支 | 通过；含 `main` / `publish` / `legacy/history-2026-09-19` |

★ **为什么先查这个**：新仓只要不是空的，普通推送就是非快进、会被拒，
而唯一的出路是硬禁止的 `--force`。所以「空」是这一步**唯一**的准入条件，
不是礼貌性检查。

★ **预检脚本第一版把判据写错了**：拿 `default_branch is None` 当「空仓」的标志，
而空仓**也有**默认分支名（建仓时就设成 `main` 了），于是把一个真·空仓判成了「不是空仓」。
改成按证据判（`size == 0` **且** 409）。**这是本轮第三个脚本错误**，前两个见上一节。

#### 二、执行的顺序，以及每步的判据

| # | 动作 | 判据 / 读数 |
|---|---|---|
| 1 | `git remote set-url origin <新仓>` | 纯本地动作，不碰任何远端 |
| 2 | `git push origin publish:main` | `* [new branch] publish -> main`，退出码 0。**没有用任何 `--force`** |
| 3 | `git reset --soft publish` | 把签出的 `main` 指到快照那一笔。**树相同**，所以工作区与索引一个字节没动 |
| 4 | `git fetch` + `set-head` | 新仓 `main` = **1 笔提交**；本地 `HEAD` == `origin/main` |
| 5 | **同一口气**改仓外留档里的 `REPO_URL` | 脚本自证三件事：只有那一行变了、改完还能被 `ast` 解析、`repo_slug()` 解出来就是新地址。**旧值一个字不打印** |
| 6 | 仓外简历里那条 facts 链接 | 用交付级**第 8 项同一套切块逻辑**替换（自己另写一套的话，「我改的地方」与「它查的地方」就不是同一块，改完还红而不知道红在哪）；改完复查：MCP 条里指向本仓 **1 处**、指向别处 **0 处** |

- 第 3 步为什么不用 `git branch -f`：git **拒绝**移动签出中的分支，`reset --soft` 才是这里的手法。
- 第 5 步为什么必须与第 1 步**同一口气**：`repo_slug()` 拿 `origin` 与仓外留档**对表**，
  两处说法不一致时它返回「读不到」并判「**切换做了一半**」。只改一处的话，
  第 4 / 8 / 13 / 15 项会去查**另一个仓库**并且报绿 —— 那比「没查」危险得多（D-54）。

#### 三、迁移不改内容：两条**独立**证据

1. **树相同**：快照那一笔的 `^{tree}` == 现在的树（`commit-tree` 造出来的，天生如此）；
2. **逐字节相同**：推送之后拿 16 份文件做三方核对（工作区 / 本地对象库的 blob / 公开 raw 直链），
   **16/16 取到且 `sha256` 一致**；其中 3 份是「① 比 ② 新」（产物滞后，见上一节），
   另外 7 份只差换行符（本机 `core.autocrlf`，折算成 LF 后相同）。

#### 四、迁移之后重跑的读数（口径 = 脚本名 + 命令）

| 项 | 口径 | 读数 |
|---|---|---|
| 闸门 | `tools/check.py` | 退出码 **0**，4 步全过 |
| 泄漏复核 | `experiments/27_leak_fix_verify.py` | 退出码 **0**：本地 0 命中 / 公网 0 命中 / 两边同一份 / 两条负控都按期望报红 |
| 交付级（联网） | `experiments/28_delivery_selfcheck.py --online` | 15 项：**通过 15 / 未完成 0 / 失败 0**，退出码 0。第 4 项 `urllib` HTTP 200；第 8 项新地址在 MCP 条里出现 **1 次**；第 13 项本地与远端指纹同为 `a5f255987864`；第 14 项实测 2 页 |
| 编号清零 | 仓外临时脚本：对 `*.md` / `mcp_server/*.py` / `reports/*` 扫 `[0-9a-f]{7,40}`，逐条用 `git cat-file -t` 判「是不是真实对象」 | 形态像编号的串 **72 处**；**能解析回真实对象的 0 处**。★ 这 72 处里有本节自己写下的那个 12 位指纹（形态像编号、但 `sha256` 是单向的，取不回对象）—— 写完这一节会再多一处，**判据看的是「能不能解析回对象」，不是个数** |
| 日志扫描（九项里的第 6 项） | 仓外临时脚本：`git log -p` 的补丁文本比仓外那一整张表 | 全历史 **8556 行**补丁、**3 行命中**（仍是旧历史里那 3 行，类别与位置同上一节）；当下 `HEAD` 上那三份台账 **0 / 0 / 0** |

#### 五、在新 clone 的一台机器上跑一遍（模拟新访客）—— 三个读数，三处修

新仓刚建出来，任何人都可以从零 clone。所以这一轮**另起一个目录做了新克隆**，
在那上面把同一套东西跑一遍。读数：

| 检查 | 在新 clone 的读数 | 判定 |
|---|---|---|
| 交付级第 15 项 | 「本机只有 1 笔提交（历史自一条初始提交起算），那段旧历史不在本机、存档在维护者本地 —— 本项**没有实测依据**：既不是通过，**也不等于**「历史被重写过」」 | **未完成**（设计如此，D-54 那一行就是为它加的） |
| `27_leak_fix_verify.py` | 退出码 **3** | **未完成**（不是 FAIL、不是离线） |
| `python -m pytest` | **1 红 / 595 通过 / 2 跳过**（合计 598） | ★ **真口子**，见下 |
| `19_verify.py` | **9 红**，且**退出码 1**（整个闸门第 2 步会红） | ★ **真口子**，见下 |

**修一：那条红的用例假定「本机有那段旧历史」**（`tests/test_delivery_selfcheck.py`）。
实现判**未完成**是对的（第 15 项那三分支就是上一轮加的设计），红的是**用例**自己：
它直接断言 PASS。修法是加一个**很窄**的跳过条件 ——
「本机取不回那一版」**且**「本机历史不超过一条」两者同时成立才跳。
维护者本机（历史多于一条）上它**仍然必须 PASS**，所以将来若有人把第 15 项改成永远未完成，
这一条照样会红；而「未完成」那条分支本身另有专门用例钉住。**断言一个字没改。**

**修二：`19_verify` 那 8 条把「没做」写成了「做坏了」**。8 条全是同一个原因：
`chinook.db` 是**构建产物**、按设计不入库（源 SQL 随仓库分发，一步就能建出来，不需要联网），
于是新 clone 的机器上，凡是查这个库的入口脚本都会以 **3** 结束并写明
「[未完成] 示例库还没构建」。脚本自己说的是**未完成**，而台账当时把它写成
`FAIL 退出码 3，期望 0` —— **同一件事，一处说没做、一处说做坏了**。

修法用的是这张台账里**本来就有**的机制 `alt`（`12_cost_report` 的退出码 5、
以及 `25` 早先那一条都是它），**没有新造判据**：`expect` 仍然是 0，一个字没松；
被点名的条目**名单不写死**，测试从脚本源码里读「谁声明了 `E_NO_DB`」，
所以将来新增一个查库的脚本也会被这条测试发现。改完两边读数：

| 机器 | 19 的读数 | 退出码 |
|---|---|---|
| 维护者本机（示例库已建） | 32 条：通过 29 / 失败 0 / 未完成 3 | 0 |
| **新 clone 的机器**（示例库未建） | 32 条：通过 20 / 失败 0 / **未完成 12** | **0**（改之前是 1） |

★ **维护者本机那一栏没变** —— 变的只有「本来就没建库的机器」上那几个字的分类。
★ 新 clone 上那 12 条「未完成」逐条对：**8 条**是示例库未建（本节这一修）、
**3 条**是「本轮未重跑、盘上已有实测产物」（需要模型凭据那三条，老样子）、
**1 条**是「账本为空」（`12_cost_report` 的退出码 5，也是老样子）。
**未完成 ≠ 通过**：它们照样不计入通过数。

**修三（顺手）**：`tools/resume_fillrate.py` 里用 `text=True` 收 Chrome 的 stderr，
Python 会按**系统 ANSI 代码页**（本机是 GBK）去解那段 UTF-8 —— 于是导出明明成功了，
终端里却先蹦一串解码报错。改成收字节，要带话时自己按 utf-8 解、解不动就替换。

★ 这三处**都不是迁移带来的**，是迁移**把它们露出来了**：以前没人从零 clone 过一遍。
把它们跟迁移记在同一节，是因为「新仓的新访客第一次跑是什么样」本来就是这次要交付的东西之一。

#### 六、`pytest` 条数变化：598 → 599

| 变化 | 说明 |
|---|---|
| **+1** | 新增 `tests/test_verify_kit.py::test_every_entry_that_needs_the_sample_db_declares_the_no_db_alternative` —— 钉住「修二」那份声明不被悄悄摘掉 |
| 0 | 那条改法的用例**条数没变**：改的是**跳过条件**，不是删用例。在新 clone 上它以「跳过」计入，在维护者本机上照样实跑 |

#### 七、这一轮**没有**做的事（照实写）

- 没有任何 `--force`（前提是新仓真是空的，这也是第一节那个预检存在的理由）；
- 没有任何历史重写；没有创建 / 删除 / 改名任何远端仓；**没有改任何仓库的可见性**；
- 旧仓**还没**转私有 —— 那是**闸门 5**，等用户做；删不删以后再定。
  〔★ 当天晚些时候用户把闸门 5 做了：旧仓已转私有，匿名实测读数见本节「十一」。〕

#### 八、晚些时候的补记：修一那条跳过条件**换掉了**（`pytest` 599 → 601）

上面「五」那张表与修一，记的是**当天早些时候**的读数，照实留着。但修一那条跳过条件
写错了方向：它问的是「本机历史不超过一条」，而**新仓的历史自己会长** ——
落笔时新仓已经有 6 笔提交了。

| 什么时候量的 | 新 clone 上的读数 |
|---|---|
| 迁移那一轮（新仓 1 笔提交） | 1 红 / 595 通过 / 2 跳过 —— 那时它看着是对的 |
| **新仓 5 笔提交时再 clone 一次** | **1 红**：用例断言 PASS，实现判 FAIL，理由写「本机存着 5 笔提交，却取不回 `旧编号·F` 那一版……历史被重写过」 |
| 换掉证据之后（新仓 6 笔提交） | **0 红**：`598 通过 / 3 跳过`；第 15 项 **未完成**、`27` 退出码 **3**、交付级 15 项 `PASS 12 / FAIL 0 / SKIP 3` |
| 维护者本机（同一版代码） | 不变：`601 通过`；第 15 项照样实测 `命中 50 条 / 出现 88 次` → **通过** |

★ 与新 clone 那 3 条跳过逐条对：**1 条**是本项（那段旧历史不在本机，没有实测依据）、
**2 条**是按设计不入库的构建产物还没建（示例库 `chinook.db`、真实索引）。

★ **这不是「新仓多攒了一笔就把检查弄坏了」，是判据钉错了东西。** 它钉在「提交笔数」上，
而笔数会随新仓自己长 —— **不用谁去改代码，它自己就会开始误报**；误报的措辞还是一条
不实的严重指控。那正是这一整套修复（D-53 / D-54）要拆的东西，所以改它是**修 bug**，
不是放松判据。

改成什么：新增 `legacy_history_refs()` —— 问本机有没有 `legacy/…` 分支，
也就是「那段旧历史还在不在本机」。旧历史按设计只留在本地（永不推送），问它就行。

| 试过的问法 | 为什么不用 |
|---|---|
| 本机提交笔数 > 1（原来） | 会随新仓自己长 → 每一台新 clone 都误报 |
| 有没有任何引用落在 `main` 历史之外 | 太宽：谁在本地开个自己的分支也会命中 → 同样误报 |
| **本机有没有 `legacy/…` 分支**（采用） | 旧历史放哪儿是有名字的，问它就行 |

★ 三个状态的语义与措辞**一个字没动**。代价照实写：要是有人把那条 `legacy/…` 分支
连引用一起删掉，本项会判「未完成」而不再判「失败」—— 宁可**少报一次**，
也不对着新克隆的人说一句不实的话（与「五」里修一那段同一个取舍）。

★ 用例：`pytest` **599 → 601**。加了两条 —— 一条钉 FAIL 那一支（`legacy/…` 还在、
却取不回那一版 → 必须失败），一条钉 SKIP 那一支（**把提交笔数按成 5 笔**、`legacy/…`
按成空 → 必须判未完成、不许判失败；放在改判据之前这一条必红，它就是这次这个坑的钉子）。

★ 还有一处**差额要说清**：这一版报告是本机**带了 `DELIVERY_RESUME_DIR`**那一跑产出的，
所以交付级第 5~10 项是**实测 PASS**；上一版报告是没带那个环境变量那一跑留下的，
那几项当时判 SKIP。两份都照实，差额在环境变量，不在判据。

#### 九、同一件事：上面「四」里那条「编号清零」的读数**过期了**，照实更新

上面「四、迁移后读数」那张表里写着：形态像编号的串 72 处、**能解析回真实对象的 0 处**。
那是**当时**量的，没错。但那之后，这一轮又往下写了两段，各自带进了几个字符串：

| 量的时候 | 读数 |
|---|---|
| 「四」那一节落笔时 | 72 处形态像编号；**能解析回真实对象的 0 处** |
| **现在（本节落笔时）** | **81 处**（去重 39 个串）；能解析回真实对象的：**去重 5 个串** —— 2 个**提交号**、3 个 **blob sha** |

★ 这里只写「去重几个串」，不写「出现几处」：本节一旦把那些串引一遍，出现的次数就跟着变 ——
**判据看的是「有哪些串、各自指向哪里」，不是次数**（上面「四」那张表里也是这么写的）。


逐条判（这就是那条判据要的「逐条判」，一个不落）：

| 能解析的串 | 在哪 | 是什么 | 判 |
|---|---|---|---|
| `a43a543`、`c27d45c` | `PROGRESS.md` 推送记录那一行（`git push` 的原样输出） | **新仓自己的**提交号 | **留**：指向的是**新仓自己的公开历史**（新仓本来就是公开的，它的提交号人人 `git log` 都看得到），走不回旧历史；而且这一段原样输出正是「**快进的普通推送**、一次 `--force` 都没用」的证据 |
| `9aad1c62`、`27362857`、`5c129f60` | `PROGRESS.md` 逐字节核对表那一处 | **blob sha**（8 位前缀） | **留**：内容寻址 —— 谁拿到那份内容都能自己算出来，它不含任何「只有我们知道」的指向；本轮的取舍里 blob 哈希与 sha256 本来就是**要留**的（它们是产物可复算的依据） |

★ **判据要说准**：它守的是「**能不能走回旧历史**（旧仓 / 旧对象）」，不是
「能不能解析回**任何**一个对象」。按前一条读：**0 处**，没变；按后一条读：5 处，
但每一处都指向新仓自己或指向内容本身。上面「四」那一行、以及 `DECISIONS.md` D-54
与 `BLOCKERS.md` 卡点 4 里同样口径的那两句话，都在原处加了指向本节的提示 ——
**原读数与原文一字未改，改的是「它还成不成立」这件事要说清**。

★ 顺带一条：扫描脚本走的是工作区，仓库里有一个**没有入库**的 `.tmp/`（`.gitignore` 挡着），
它里面的临时文件也会被扫到 —— 上面那个 81 里有 **1 处**来自它（`.tmp/probe_auth_codes.py`），
去掉它、只算入库的东西是 **80 处**。这 1 处**解析不到任何对象**，也不影响上面任何一条判定；
交付出去的是 git 里那一份，所以哪个数当准要说清：**80**。

#### 十、加一条更省事的逐字节判据：整棵树比 blob，不走 raw 直链

**八**里那次复核走的是 raw 直链，它的毛病已经踩过一次（对刚推上去的内容有几分钟缓存，
会报假不一致）。这回换一条没有缓存的走法，顺手把范围从 16 份文件扩到**整棵树**：

| 怎么比 | 读数 |
|---|---|
| 本地 `git ls-tree -r HEAD` 的每个 blob sha vs 远端 `GET /repos/<slug>/git/trees/main?recursive=1` 的每个 blob sha | **两边各 152 个 blob；只在本地有的 0、只在远端有的 0、两边字节不同的 0** |
| 本地 `git rev-parse HEAD` vs 远端 `GET /repos/<slug>/commits/main` | 同一个（落笔时指纹 `b5164a698048`） |

为什么这条更省事：**blob sha 就是字节**，两边相等即逐字节相同，而这两边都是 git 自己算的，
**没有 CDN 缓存这回事**；一次请求拿整棵树，比 16 次 raw 直链还快。
`raw.githubusercontent.com` 那条路留着做**独立复核**（它慢、有缓存，但它是另一条通路）。

★ 一条**天生的次序**要说清：这类报告**不可能写上自己那一笔的指纹** —— 产物是「先跑后提交」，
报告里记的恒是**上一笔**的状态。所以核对的正确姿势是「跑一次、看它报一致」，
不是拿报告里的指纹去比当前 `HEAD`（`PROGRESS.md` 上面那句「跑一次 `28 --online` 看第 13 项」
就是这个意思）。

#### 十一、闸门 5 已由用户执行（2026-09-19）：旧仓已转私有，匿名访客取不到了

**闸门 5 是用户的动作**（可见性变更只能用户做，本仓一次都没碰）。用户做完之后，
本机把那条路**匿名**走了一遍 —— 这是「顺着编号点回旧对象」那条路的机械判据。
全程**匿名请求**：不带 token、不读任何凭据、不用任何登录身份。

| 请求 | 读数 | 说明 |
|---|---|---|
| 新仓：`GET /repos/<新仓>` 与 `/commits/main` | **200 / 200** | **对照**。没有它，一个 404 也可能只是网络不通、或名字写错了 |
| 旧仓：`GET /repos/<旧仓>` | **404** | 匿名访客连它存不存在都看不到 |
| 旧仓：`raw.githubusercontent.com/<旧仓>/main/README.md` | **404** | 直链那条路也关了 |
| 旧仓：按**编号**取对象（7 位与完整 40 位各取一个试） | **404 / 404** | **这就是本次要切断的那条路本身** |

★ 编号只从**仓外**留档取，只在内存里拼进请求，**不回显、不落盘**。
★ **读数边界**（照实说）：对匿名访客来说，「已转私有」与「已改名 / 已删」在读数上**长得一样**
（都是 404）。所以本机实测到的是「**匿名访客从这里取不到任何东西**」——这正是本次迁移要的
那个结果；至于「它是私有、不是被删」，以用户自己做的操作为准。
★ 同样要说清：拿到授权的登录用户当然还看得见那个仓 —— 那正是「留存量仓、先不删」的本意，
不是漏洞。

★ 于是 `BLOCKERS.md` 卡点 4 里选的那条路（**不重写历史、把编号搬去仓外、旧仓转私有**）
**到此闭环**：文档里再没有能解析回旧对象的编号（见本节「九」），旧仓对匿名访客也不再开口。
**还剩一件可做可不做的**：删不删旧仓 —— 那是用户以后的事，本仓不做、也不催。

### 推送之后（2026-09-19）：两笔推完，第 8 项逐字节复核

| 项 | 口径（脚本 + 命令） | 读数 |
|---|---|---|
| 推的哪两笔 | `git push origin main` | `a43a543..c27d45c  main -> main`，退出码 0 —— **快进的普通推送**，一次 `--force` 都没用 |
| 交付级（联网） | `experiments/28_delivery_selfcheck.py --online`（带 `DELIVERY_RESUME_DIR`） | 15 项：**通过 15 / 未完成 0 / 失败 0**，退出码 0。第 13 项本地与远端指纹同为 `2e334a62648a` |
| 第 8 项 | 仓外临时脚本：16 份文件各取一次 `sha256`，比工作区 / 本地对象库里的 blob（`git cat-file blob HEAD:<路径>`）/ 公开 raw 直链三方 | 见下 |

**第 8 项那张表（要证的是 ② = ③ 这一列，不是 ① 那一列）**

| 判定 | 份数 | 说明 |
|---|---|---|
| ① ② ③ 三份一致 | 6 | 代码与测试那几份 |
| ② ③ 一致；① 只差换行符 | 8 | 本机 `core.autocrlf = true`：工作区是 CRLF、blob 是 LF；折成 LF 后与 blob 相同 |
| ② ③ 一致；① 比 ② 新（不只是换行符） | 2 | 就是那两份交付级产物 —— 它是推送之后跑的，按新的 `HEAD` 重写了自己 |
| ★ ② 与 ③ **不一致** | **0** | **推上去的 16 份，公开直链取回来的字节与本地对象库一模一样** |

只打印 `sha256` 的前 8 位：完整值不是钥匙（单向），但报告是公开产物，按老规矩只留可比对的前缀。

**这一轮遇到一次网络中断，照实记**

- 第一次跑 `28 --online` 时，第 4 项与第 13 项判 **ERROR**：「联网取回失败（不是「干净」，是「没取到」）」。
  同一时段 `urllib` 取任何地址都被对端重置（`WinError 10054`），而 `curl.exe` 取同一个地址是 HTTP 200；
  紧接着 `curl.exe` 也断了一次 —— 第一次跑第 8 项时 `BLOCKERS.md` 那一份没取到，
  表里写的是「公开 raw = 取不到 / ★URLError」，**不是**「② ③ 不一致」。
- **重跑即恢复**：`28 --online` 15/15、第 8 项 16/16、全部 ② ③ 一致。
- ★ 两次都**没有**把「没取到」写成「不一致」，也没有写成「干净」：第 4 / 13 项判的是 **ERROR**、
  第 8 项那一格写的是「取不到」—— **「没查」与「查了、结果是坏」是分开的**，这正是这几项要守的东西。
- ★ 一处照实说、别把话写大：第 8 项那个脚本**在表里**把两者分开了，但**退出码是同一个 1**
  （它把「取不到」与「不一致」一起累计）。所以「退出码 1」在这支脚本上只说明**有事要看**，
  要分清是哪一种，得读表里那一格 —— 这是这支仓外临时脚本的粗糙处，记在这里。
- `27` 的取法本来就写成「先 `curl.exe`、起不来退回 `urllib`」，并在报告里写明**用的是哪个**
  （它那句「公网取法：本次成功的是 …」就是为这件事留的）。这一轮两种取法**交替**失灵，
  说明那条备注不是多余的；`28` 的第 4 项走的是同一个取法，所以同样写明了工具名。

**这一笔提交是什么**

台账补记 + 推送之后刷新的产物（`reports/verify.md`、`reports/leak_fix_verify.md`、
`reports/deliverable_check.md`、`reports/final_selfcheck.md`、
`reports/delivery_selfcheck.{md,json}`）。产物为什么总比台账慢一步，见前面「产物为什么慢一步」那一节。

**又踩一个坑：第 8 项报了一次「假不一致」，是 raw 直链的缓存，不是字节不同**

推完最后一笔之后立刻再跑第 8 项，16 份里有 3 份报 **★ ② ③ 不一致** ——
正好是**这一笔刚改过**的那 3 份（`PROGRESS.md` 与两份交付级产物），
而它们的「公开 raw」那一格打印出来的是**上一版**的内容哈希。

先不下结论，换一条**不走那条缓存**的核法（口径：GitHub 的内容接口
`GET /repos/<owner>/<repo>/contents/<路径>?ref=main`，取回 `sha` 与字节，
把字节重新算一遍 `sha1("blob <长度>\0" + 字节)`，再与本地 `git rev-parse HEAD:<路径>` 比）：

| 文件 | 远端接口的 blob sha | 本地 `HEAD` 里的 blob sha | 字节重算 |
|---|---|---|---|
| `PROGRESS.md` | `9aad1c62` | `9aad1c62` | 与两者相同 |
| `reports/delivery_selfcheck.md` | `27362857` | `27362857` | 与两者相同 |
| `reports/delivery_selfcheck.json` | `5c129f60` | `5c129f60` | 与两者相同 |

也就是说：**推上去的字节是对的**，错的是取法 —— `raw.githubusercontent.com`
对刚推上去的内容有几分钟的缓存，过期之后再跑同一支脚本，16 份**全部** ② ③ 一致、退出码 0。

★ 记这一条是因为它是**仪器**的问题，不是仓库的问题：那一刻脚本如实打印了
「② ③ 不一致」，而真相是「③ 还是旧的」。凡是拿公网 raw 当对照物的核对
（第 8 项、第 4 项、第 3 项）都会遇到同一件事，**刚推完就核，要先想到缓存**；
要当场定论，就走内容接口那条路（它不经那条缓存）。
口径没变、脚本也没改，改的只是「读到不一致时先怀疑谁」。

## 阶段 5 明细（已完成）

### 建了什么

- [x] `mcp_server/selfcheck.py` —— 扫描口径单点收敛：词表、范围、掩码、`scan_report_carrier()`、
      负控样本 `NEGATIVE_FIXTURES`
- [x] `mcp_server/verify_kit.py` —— 退出码台账 + 判定（纯函数，可被负控直接喂数据）
- [x] `mcp_server/deliverable_kit.py` —— 交付物核验 G1~G8 的判定部分
- [x] `experiments/19_verify.py` —— 台账式自检，**32 条**：S1~S9 静态污染 + A\* 产物互斥 +
      D\* 文档 + X\* 退出码 + P\* pytest/注册表；带 `--selftest` 与 `--quick`、`--json`
- [x] `experiments/25_deliverable_check.py` —— 交付物核验 **8 条**（G1~G8）
- [x] `tools/check.py` —— 守门链，退出码 = 卡住那一步的序号；`--list` 只看不跑
- [x] `tests/test_verify_kit.py`、`tests/test_deliverable_kit.py` —— 逐条证明检查**会红**
- [x] `docs/design.md` —— 设计说明（为什么长成这样，逐条取舍指向 `DECISIONS.md`）

### 阶段 5 实测数字（口径见各脚本的文件头）

| 数字 | 值 | 口径 / 出处 |
|---|---|---|
| 台账条数 | 32（PASS 26 / FAIL 0 / SKIP 6 / ERROR 0） | `19_verify.py`，退出码 0 |
| 台账里的 SKIP 是什么 | 3 条需模型凭据 + 1 条「账本为空」+ 1 条「README 未创建」+ 1 条 D2 | 同上：SKIP 全部有声明过的理由，**不是**「通过」 |
| 交付物核验 | 8 条（PASS 6 / FAIL 0 / SKIP 2 / ERROR 0） | `25_deliverable_check.py`，退出码 **5**（设计如此：README 未创建，依赖它的两项判 SKIP） |
| 交付物核验 G4 扫到的命令 | 25 条 | 同上：文档里每条 `python <脚本>.py --flag` 都核过脚本与参数 |
| 负控（`--selftest`） | 退出码 0 | 同上：S1~S8 各放一种污染 → 逐项报红；清掉 → 逐项报绿；掩码**双向**验（掩码后必须干净、未掩码必须被抓到） |
| 守门链 | 3 步 | `tools/check.py --list`，退出码 0 |
| 本仓 pytest | **511 passed / 1 skipped** | `python -m pytest` |

### 阶段 5 抓到的真问题

1. **交付物核验第一轮就报了 4 处「指向一份不存在的报告，且当场没交代」** ——
   分布在 `BLOCKERS.md`、`DECISIONS.md`（两处）、`reports/colloquial_NOT_RUN.md`
   （那份未完成声明是当时的中间产物，跑通后已按设计删除，**现在不存在**）。
   前三处是手写的，改完就稳定。**第四处是脚本生成的**：`13` 每次以退出码 5 结束都会
   重新写一遍那一页，手改 markdown 下一轮就被覆盖回去。
   修法只能落在生成器里 —— 而修好之后重跑，同一处又红了第二次，因为
   那一句里点名的成品报告**当时确实还不存在**。最终改成「点名 + 同段交代尚未生成」：
   判据（`deliverable_kit` 的 G7）按**自然段**找交代声明，因为文档是折行的。

2. **`.jsonl` 被正则截成 `.json`** —— 记账文件 `reports/usage_ledger.jsonl`
   **（尚未生成：还没有任何真实模型调用）** 因此被判成「引用了不存在的
   `usage_ledger.json`」。这是检查**自己**的 bug，边界加 `(?![A-Za-z0-9])` 修掉，
   并留了一条用例（`test_g7_does_not_mistake_jsonl_for_json`）。

3. **报告把自己报告的污染抄了进去（同一种回路三次）** ——
   ① 报告逐字引用命中行 → 报告自己成了载体；
   ② 某条检查的**标题**里那个词本身就在词表里 → 这条检查自己就是一处命中；
   ③ 「命中的行」被引用后每轮多套一层反引号（实测长过三代）。
   处置按「引用是为了什么」分：只需指出位置的用**掩码**；必须显示路径/模式的
   把**生成物**排除出它自己喂的那条检查。两种都配了机械保证（写盘前扫一遍 +
   S9 渲染后回读、迭代到不动点）。理由见 `DECISIONS.md` D-30。

4. **绝对路径从脚本的 stdout 漏进报告** —— 脚本打印
   `print(f"报告：{REPORTS / 'x.md'}")`，`19_verify.py` 把子进程输出的尾部**原样**
   抄进 `reports/verify.md`，报告于是成了那个路径的载体，下一轮 S7 在自己的报告里命中它。
   根因修法是 `mcp_server/paths.rel()`：**落盘用绝对路径、打印用相对路径**；
   同时在引用之前加一道掩码，并在掩码发生时补一句「引用前已隐去敏感形态」
   （剪过却不说，比剪了更糟）。

### 为什么「未完成」也算不合格

`tools/check.py` 对「未完成」也给非零退出码 —— 闸门只认「全都真的过了」。
但它会把「失败」和「未完成」分开打印，因为处理方式不同：一个是改代码，一个是配凭据。
阶段 6 之前这条链子稳定停在第 3 步，这是**当前状态的真实反映**，不是缺陷。

### 最近一次全量验证（阶段 5 收尾）

| 命令 | 退出码 | 说明 |
|---|---|---|
| `python -m pytest` | 0 | **511 passed / 1 skipped**（阶段 4 收尾时是 439 passed） |
| `experiments/19_verify.py` | 0 | 32 条：PASS 26 / FAIL 0 / SKIP 6 / ERROR 0 |
| `experiments/19_verify.py --selftest` | 0 | 负控全部按预期报红/报绿 |
| `experiments/25_deliverable_check.py` | **5** | 设计如此：README 未创建，依赖它的两项判 SKIP |
| `tools/check.py --list` | 0 | 链条 3 步列得出来 |
| `tools/check.py` | **3** | 设计如此：停在第 3 步（未完成）。阶段 6 写出 README 后应转 0 |

## 阶段 3 明细（已完成）

- [x] `mcp_server/t2sql_core.py` —— 有界重写状态机，`llm` 可注入（控制流与模型表现切开）
- [x] `mcp_server/server.py` —— `MCPServer` 注册 6 个工具 + 启动自检（注册表 vs `TOOL_SCOPES`）+ `--check`
- [x] `experiments/10_auth_test.py` —— 鉴权矩阵，**应拒 36/36、应放行 13/13、入口一致性 18/18**
- [x] `experiments/11_guardrail_test.py` —— 护栏负例矩阵，**41/41**，每条含期望 / 实测 / **退出码**
- [x] `experiments/08_mcp_smoke.py` —— 真实 stdio 握手，**15/15**
- [x] `tests/test_t2sql_state_machine.py` —— 假模型驱动，控制流全覆盖（不联网、不花钱）
- [x] `tests/test_db_tools.py` —— `db_tools` 原本零测试，本轮补出（并因此抓到表名渲染 bug）
- [x] `tests/test_server_selfcheck.py` —— **故障注入**：证明自检会红，而不只是会绿

### 阶段 3 实测数字（口径见各脚本的文件头）

| 数字 | 值 | 口径 / 出处 |
|---|---|---|
| 鉴权 应拒 | 36/36 | `10_auth_test.py`：每种失败模式一条用例，比对 `err_code` 逐字相等 |
| 鉴权 应放行 | 13/13 | 同上：合法令牌必须**真的走到业务代码**（反向证明，防止「一刀切全拒」拿满分） |
| 鉴权 入口一致性 | 18/18 | 同上：经 `server.py` 的工具入口再跑一遍拒绝侧；`ask_database` 放行侧会真调模型，标 `skipped` |
| 护栏 用例 | 41/41 | `11_guardrail_test.py`：24 条 L1 应拒 + 10 条 L1 应放行 + 4 条 L3/三态 + 3 条逐层隔离 |
| 护栏 写操作落地检查 | Genre 25 → 25 | 同上：`--raw` 直奔 L2 的那条 INSERT，前后用只读连接数一次行数，证明**真的没写进去** |
| 握手 用例 | 15/15 | `08_mcp_smoke.py`：12 条协议用例 + 3 条超时用例（含「连上但不回话」的假服务端） |
| 握手 报出的工具数 | 6 | 同上：三处工具名集合必须两两相等（线上 schema / `TOOL_SCOPES` / 本地注册表） |
| 协商到的协议版本 | `2025-11-25` | 同上，从 `initialize` 结果里读 |
| 本仓 pytest | 372 passed / 1 skipped | `python -m pytest`（见下方「最近一次全量验证」） |

### 阶段 3 抓到的四个真 bug（都已在代码注释里留痕）

1. **`server.py` 把启动信息打到了 stdout** —— 而 stdio 服务端的 stdout 是 JSON-RPC 通道。
   现象是握手直接报 `Invalid JSON: ... input_value='已注册 6 个工具：…'`。
   进程内测试**全都看不见**这个 bug，只有真实 stdio 握手能抓到。
2. **`db_tools.table_schema()` 返回的键叫 `table`，而 `schema_doc()` 读的是 `name`** ——
   渲染出来的结构里表名全是 `?`。函数不报错、格式也整齐，只是模型拿不到表名。
   若不修，阶段 4 的现象会是「模型写的 SQL 表名不对」，排查方向会指向模型。
3. **`t2sql_core.extract_sql()` 只认 `SELECT`/`WITH`** —— 模型回 `DROP TABLE artist` 时被判成
   `kind="empty"`（「说了句废话」），**护栏根本没机会看到它**，`R4_FORBIDDEN_KEYWORD`
   这条规则在状态机路径上永远走不到，且回灌给模型的是误导性的提示。
4. **脚本的退出码会被控制台编码带歪** —— 在 GBK 控制台上直接跑
   `python experiments/11_guardrail_test.py`（不带 `-X utf8`）：41 条用例全部跑过、
   打印出「41 / 41」，然后在最后一行打印 ✅ 时抛 `UnicodeEncodeError`，**进程以 1 结束**。
   结论变成「护栏实验失败」。修法是 `mcp_server/console.py`，每个入口自己在 `main()`
   开头调一次，`tests/test_repo_hygiene.py` 静态扫描所有入口防止新脚本漏掉。
   详见 `DECISIONS.md` D-19（含一条**被负控推翻的错误解释**）。

### 最近一次全量验证

三个实验在**普通控制台**和 **GBK 控制台**（`PYTHONIOENCODING=gbk`）下各跑一遍，
退出码必须一致 —— 这正是 bug 4 的回归口径：

| 命令 | 普通 | GBK |
|---|---|---|
| `python experiments/10_auth_test.py` | 0 | 0 |
| `python experiments/11_guardrail_test.py` | 0 | 0 |
| `python experiments/08_mcp_smoke.py` | 0 | 0 |
| `python -m mcp_server.server --check` | 0 | — |
| `python -m pytest` | 0（372 passed / 1 skipped） | — |

## 阶段 4 真实运行（2026-09-18，配好凭据之后）

这一节是**真的调用模型**跑出来的，数字见 `RESULTS.md` 第二节；
这里只记「跑了什么、踩到什么、改了什么」。上面那一节（阶段 4 明细）保留原样，
它记的是**凭据到位之前**的状态 —— 删掉它就没有「当时卡在哪」的记录了。

### 跑的顺序（顺序本身有个坑）

```
09_text2sql_eval.py            # 36 题 × 3 版
21_noise_band.py               # 必须先于 20 和 13：它产出噪声带
13_text2sql_colloquial.py      # 口语 vs 正式
20_pair_test.py                # 纯分析
12_cost_report.py              # 纯分析
```

**这条顺序是踩出来的**：先跑 `13` 再跑 `21`，`13` 的报告里会写
「配对比较未完成：缺实测噪声带」，得再跑一遍 `13`。原因是 `13` 也要读噪声带。
已写进 `BLOCKERS.md`。

### 实测到的、值得单独记的负结果

**v3（加了列名指引 + 「答不了」自救的那版提示词）在可答题上比 v1/v2 差。**
严格 EX：v1 29/32、v2 30/32、v3 27/32。与 v1 差 2 题、与 v2 差 3 题，
两对都**超出**实测噪声带（1 题）；同时 v3 在不可答的 4 题上是 4/4，
而 v1、v2 都是 0/4。

**这一条不删。** 它是「提示词越改越好」这个直觉的反例，而且它是唯一能说明
「这套评测真能测出方向」的样本。另外查过代价的**形态**：v3 丢掉的 3 道题
（q16、q22、q25）在 v3 的记录里 `status=ok`、`attempts=1` —— 不是拒答，是答错了。
所以不能写成「模型变保守了」，那是没测过的说法。

### 这一轮改掉的两件事

1. **台账默认只读。** 原先 `19_verify.py` 里那几条「需要凭据」的检查在配好凭据后
   会**真的重跑** —— 每跑一次闸门就重发约 200 次调用并覆盖 `reports/` 里的评测产物。
   现在默认只核对「产物在不在、自不自洽」，重跑要显式加 `--rerun-llm`。
   判定多了第三档：`SKIP`（本轮无凭据、未重跑，且写明盘上已有哪个实测产物）
   —— **不是 PASS**。理由见 `DECISIONS.md` D-44。
2. **措辞检查撞自己的回路修了两处。** `20_pair_test.py` 的报告原来为了声明
   「我不用那几个词」而把那几个词逐个写了出来，报告自己撞上自己的措辞检查
   （实测退出码 2）。改成指向唯一的词表定义处。同类回路在本仓是第三次出现。

## 阶段 4 明细（凭据到位之前的状态，保留作记录）

**这一阶段的所有代码都已就位、且被证明能跑**，缺的只有一份可调用的模型凭据。
本仓库**没有编造任何 EX / token / 成本 / 噪声带数字**。

### 已完成的部分

- [x] `data/eval/questions.json` —— 36 题 = **32 题可答**（各带一条可执行参考解）+ **4 题本来就答不了**
- [x] `data/eval/questions_colloquial.json` —— 15 题口语化问法，`ref_sql` **程序化**从正式题集抄来
      （手抄会把两条 SQL 抄出细微差别，那样比较的就不是措辞了）
- [x] `mcp_server/eval_grading.py` —— 评分口径（双口径 / 浮点容差 / McNemar / 噪声带 / 措辞纪律）
- [x] `mcp_server/eval_runner.py` —— 跑参考解、判一题、跑一版、假模型、报告落盘
- [x] `mcp_server/env_file.py` —— 读 `.env`（补上了一条「文档说了、代码没做」的链路，见下）
- [x] `BLOCKERS.md` —— 两个卡点的最小动作（模型凭据 / 远端凭据）
- [x] `experiments/09_text2sql_eval.py` —— 36 题 × 3 版，`--check-questions` / `--fake`
- [x] `experiments/13_text2sql_colloquial.py` —— 口语 vs 正式，按 `same_as` 配对
- [x] `experiments/21_noise_band.py` —— 同一臂连跑两遍实测噪声带
- [x] `experiments/20_pair_test.py` —— McNemar + 噪声带对照（**纯分析，不需要 LLM**）
- [x] `experiments/12_cost_report.py` —— 读账本出报告（**不需要 LLM**）
- [x] `tests/test_eval_grading.py`（24）、`tests/test_noise_band.py`（13）、`tests/test_cost.py`（17）

### 阶段 4 实测数字（都不涉及模型，因此是**真数字**）

| 数字 | 值 | 口径 / 出处 |
|---|---|---|
| 正式题集参考解核验 | 32/32 | `09 --check-questions`：非空、≤200 行、连跑两次一致 |
| 口语题集核验 | 15/15 | `13 --check-questions`：另加「`ref_sql` 与正式题逐字相同」 |
| 假模型 oracle | 严格 32/32、容忍 32/32、答不了命中 4/4、调用 36 次 | `09 --fake oracle`：满分模型必须拿满分，否则是评分有 bug |
| 假模型 naive | 严格 0/32、容忍 0/32、答不了命中 0/4 | `09 --fake naive`：故意答错必须拿不到分，否则是评分太松 |
| 假模型 flaky | 严格 32/32、调用 68 次 | `09 --fake flaky`：32 可答题各调 2 次 + 4 不可答各 1 次，重写路径确实被走到 |
| 噪声带尺子 正控 | 量出 0 | `21 --fake stable`：两遍完全一样时必须量出 0 |
| 噪声带尺子 负控 | 正好量出 3 | `21 --fake jitter`：第二遍故意抖 3 题，必须正好量出 3 |
| 配对统计自检 | 7 组通过 | `20 --selftest`：合成数据，含措辞禁用词检查 |
| 假模型是否污染账本 | 未创建账本文件 | 自检后 `reports/usage_ledger.jsonl` 不存在（`record_cost=False`） |

### 为什么噪声带要单列一个「尺子自检」

噪声带是所有版本差异的判据。它量大了会把真实差异盖住（表现为「两版一样」，看着很稳健），
量小了会把抖动写成差异（表现为「改进了」，看着有成果）—— **两种错法都不会自己暴露**。
所以除了「量出 0」，还得证明「**能**量出非 0」，见上表最后两行。

### 阶段 4 抓到的真问题

1. **全量 `pytest` 在阶段 4 的新文件上是第一次跑** —— 阶段 3 那次 372 passed 是在
   `eval_grading.py` 等文件存在**之前**。跑完立刻红了两条，都是我自己写的：
   - `reports/cost_report.md` 里写进了**作者机器的绝对路径**（盘符开头的那种，
     这里连示例都不敢写 —— 写了它自己就会被同一条检查扫到）；
   - 夸大措辞扫描把「文档字符串里列举禁用词」当成了违规（26 处命中，**全部**是
     `BANNED_WORDS` 元组、禁止这些词的说明文字、以及断言能抓住它们的测试）。
   第 2 条的处置是**改扫描范围**而不是删词表：静态扫描只看给人读的成品
   （`reports/**` + 根目录结果文档），程序生成的报告改由
   `eval_runner.write_report()` 在**真实产物**上查一遍全文。
   两条都补了负控（`test_the_reader_prose_scope_is_not_empty`、
   `test_the_overstated_scan_would_actually_catch_a_report`）。理由见 `DECISIONS.md` D-20。

2. **`.env` 是一份「文档说了、代码没做」的链路** —— `.env.example` 第一行写着
   「复制成 `.env` 再填值」，`requirements.txt` 也声明了 `python-dotenv` 并注明
   「只在需要 LLM 的链路上用到」，但**没有任何一处代码读过 `.env`**。
   照文档做的人会填一个谁也不看的文件，然后拿到「缺少环境变量 `LLM_API_KEY`」，
   再回头怀疑是自己填错了。
   修法是 `mcp_server/env_file.py`（+ 在 `real_llm()` 里调一次，一处覆盖命令行脚本
   与长期运行的服务端两种入口），并补了**成对**的两条用例：
   两处都没凭据 → 必须报缺；**只有 `.env` 里有凭据 → 必须真的走到模型调用**。
   只写后者的话，一个「把凭据吞掉然后静默返回空串」的实现也能通过；
   只写前者的话，这条链路等于没测。理由见 `DECISIONS.md` D-28。

### 最近一次全量验证（阶段 4 收尾）

| 命令 | 退出码 | 说明 |
|---|---|---|
| `python -m pytest` | 0（**439 passed / 1 skipped**，共 440 条） | 新增 65 条：评分子 24 + 噪声带 13 + 账本 17 + `.env` 11 |
| `09 --fake oracle` / `--fake naive` / `--fake flaky` | 0 / 0 / 0 | 见上表 |
| `13 --fake oracle` | 0 | 同上 |
| `21 --fake stable` / `--fake jitter` | 0 / 0 | 噪声带尺子的正控与负控 |
| `20 --selftest` | 0 | 7 组统计与措辞 |
| `12_cost_report.py` | **5** | **设计如此**：账本为空 → 报告照写、内容写明「未完成」，但以非零码结束 |
| `10_auth_test.py` / `11_guardrail_test.py` / `08_mcp_smoke.py` | 0 / 0 / 0 | 阶段 3 的三份报告，本次改动未触及它们 |

## 阶段 2 明细（已完成）

- [x] `data/chinook/Chinook_Sqlite.sql` —— 595545 字节，sha256 已核验（来源 URL 见 `NOTICE.md`）
- [x] `data/kb/raw/*.txt` —— 40 篇公版中文文本，覆盖 34 部作品，694005 字符，6 篇截断
- [x] `experiments/00_fetch_corpus.py` —— 可重跑的语料抓取（带下载缓存，重跑不打对方站点）
- [x] `experiments/01_build_db.py` —— 构建 + 行数核验 + 确定性检查（连建两次哈希一致）
- [x] `experiments/02_build_kb.py` —— jieba 切词 → FTS5，1339 个索引块，抽样往返核验通过
- [x] `experiments/03_write_notice.py` —— 生成 `NOTICE.md`，`--verify-source` 用哈希核验来源
- [x] `mcp_server/db_tools.py` —— 列结构 / 看表结构 / 执行查询（含给模型看的 schema 文档）
- [x] `mcp_server/kb_tools.py` —— 检索 / 取原文，44 条敌意输入的 FTS5 语法已实测
- [x] `tests/test_kb_search.py`、`tests/test_passage_offsets.py`、`tests/test_repo_hygiene.py`

### 阶段 2 实测数字（口径见各脚本的文件头）

| 数字 | 值 | 出处 |
|---|---|---|
| 语料篇数 / 作品数 | 40 / 34 | `00_fetch_corpus.py` 输出，`manifest.json` |
| 语料正文合计 | 694005 字符 | 同上（NFC 归一化、去页眉页脚、逐篇截断后） |
| 截断篇数 | 6 | `manifest.json` 的 `truncated` 字段计数 |
| 索引块数 | 1339 | `02_build_kb.py` 输出，`reports/kb_build.json` |
| 块平均长度 | 518 字符 | 同上（正文合计 ÷ 块数，目标 500） |
| 示例库行数合计 | 15607 | `01_build_db.py` 输出（11 张表逐表计数） |
| chinook.db sha256 | `5fdff3d7…33810` | `01_build_db.py --check-determinism`（两次构建一致）|

## 状态图例

- **未开始** / **进行中** / **已完成** / **未完成：<卡点>**

## 阶段 0 明细

- [x] 建立目录树
- [x] `.gitignore`（先建，含 `.env` / `*.key` / `*.pem` / `*.db` / `__pycache__` / `.venv` / `cache/`）
- [x] `.env.example` 与 `requirements.txt`
- [x] `git init -b main` + 首次提交

## 阶段 1 明细

- [x] `mcp_server/paths.py` —— 数据根单点收敛，环境变量 `MCP_TOOLKIT_DATA_ROOT` 覆盖
- [x] `mcp_server/auth.py` —— HS256 签发/校验、工具级 scope、`guard()` 永不抛异常
- [x] `mcp_server/guardrails.py` —— 三层护栏（文本校验 / 只读连接 / 引擎级 authorizer）
- [x] `mcp_server/llm_retry.py` —— 限流指数退避
- [x] `mcp_server/cost.py` —— 用量账本（JSONL 追加，金额允许为空）
- [x] `tests/`（paths / auth / guardrail / guard contract 四组）

## 会话中断后如何续跑

1. 读本文件的「下一步」列。
2. 看 `reports/` 里已有哪些产物 —— 有产物的阶段视为已完成。
3. 直接重跑对应脚本即可，所有脚本都是幂等的；带缓存的脚本用 `--force` 或环境变量强制重跑。

---

# 任务 2（2026-09-18 起）：把「实习链路」与「MCP 工具层」彻底解耦

> 上面那张阶段表是**任务 1**（建仓 → 跑通 → 推送）的台账。
> 本任务是在它之上的第二次收尾，单独开一节，**不覆盖也不删除**上面的任何一行。

## 目标（一句话）

让简历里的 MCP 条目**只指向这个新仓**，并且让新仓里**再也没有一处能逐字读出
公司名 / 课程名 / 原仓库名 / 原项目的那些数字** —— 包括那个一直在公开仓库里
逐字写着它们的 `mcp_server/selfcheck.py`。

## 阶段清单与当前状态

| 阶段 | 做什么 | 状态 | 产物 |
|---|---|---|---|
| 0 | 核基线 + 建台账 | **已完成** | 本节 |
| 1 | 拆掉 `selfcheck.py` 里的「明文桥」：词表外置到仓外，读不到判 SKIP | **已完成** | `mcp_server/selfcheck.py`、仓外词表、`reports/leak_fix_verify.md` |
| 2 | 简历 4.0.html：MCP 条换数字与链接、删掉与实习链路的互指 | **已完成** | `4.0.html`、`DECISIONS.md` D-48 〔★ 2026-09-19 复核：交付物已换到带新标签的一版；标签只在 `delivery_selfcheck.RESUME_TAG` 一处定义（D-55）。本行的 `4.0.html` 是当时那版的产物名，**保留**〕 |
| 3 | 量填充率 + 导出 2 页 PDF | **已完成** | `4.0.pdf`、`tools/resume_fillrate.py` 〔★ 2026-09-19 复核：同上；`4.0.pdf` 是当时那版的产物名，**保留** —— 它是本轮改动的对照物〕 |
| 4 | 覆盖写版本说明（第 10 版） | **已完成** | `版本说明-投递前先看这个.txt` 〔★ 2026-09-19 复核：该文件已到第 11 版，本行记的是当时那一版〕 |
| 5 | 闸门 + pytest + 提交 + 推送 + 公网复核 | **已完成**（含一轮收尾，见「阶段 5 收尾」） | `reports/final_selfcheck.md` 新增一节、`reports/leak_fix_verify.md` |
| 6 | ★ **停手点**：历史残留写进 `BLOCKERS.md`，不执行任何重写 | **停手（按任务书，不回退）** | `BLOCKERS.md` 卡点 4 |

### 阶段 1 结果（一句话）

`mcp_server/selfcheck.py` 的词表搬到仓外（`MCP_TOOLKIT_WORDLIST` → 仓外 `.py`），
**判定口径一个字没改**。实测两侧都是 0 命中：本地工作区 0 条 / 0 次、
公网 raw 同一份文件 0 条 / 0 次，两边 sha256 相同（`89fc1ae9bc2e…`，均 31652 字节）；
负控（修复前的提交，现钉 `旧编号·F`）**50 条 / 88 次**，说明那套扫描是活的。
（负控原先钉在 `旧编号·E`，改钉 `旧编号·F` 的原因只是注释里「最后一版」那句话 ——
两者是**同一个 blob**，量出来逐字节相同；补记见 `BLOCKERS.md` 卡点 4。）
读不到词表时闸门退出码 **4**（卡在第 4 步，那一步自报 5 = 未完成），**不是 0**。
明细与实测输出：`reports/leak_fix_verify.md`；决定：`DECISIONS.md` D-46。

### 阶段 2 结果（一句话）

简历 MCP 条与实习链路**解耦**：MCP 条数字全换成本仓实测值、facts 改指本仓；
实习条技术栈删「 · MCP」、末行改中性、撤下 9 处没有公开对照的量。
扫描实测（按 §4 D2 的 15 个数字 + §8 的禁词表逐项 count）：**全部 0**。
明细与「撤了什么、换来什么」：`DECISIONS.md` D-48。

★ 阶段 2 顺带修了**交付级自检自己的三处判据**（第 5 / 7 / 8 项）：
它们的**作用域**取大了，拿真实交付物一跑就全红，且红的是合法内容
（雇主名、MCP 条自己的技术栈、另一个项目的正确链接）。口径没动、动的是范围：
第 5 项收成「词表划给交付物的那张名字表」（仓外词表新增第 4 个键
`RESUME_FORBIDDEN_NAMES`，缺键则**退回整张表**即更严）；第 7 项收成
`<h2>实习经历</h2>` 那一节（缺节判红）；第 8 项收成标题带 `MCP` 的那一条
（条目数 ≠ 1 判红），并允许网址不写协议头。新增 5 条用例钉住这些反直觉的判据，
其中一条专门钉「雇主名可以通过、旧仓库名必须红」。决定：`DECISIONS.md` D-47。

### 阶段 3 结果（一句话）

`tools/resume_fillrate.py --export`：
**p1 740.3pt（占整页 88%）、p2 805.7pt（96%）、第 2 页余量 7.8pt、页数 2**，
与任务书记的基线（740/806、余量约 7pt）**逐点一致** —— 版式参数与第 9 版完全相同
（`@page` A4、边距 11/13/10/13mm、正文 9.6pt、行距 1.40、`.newpage` 强制分页），
没有缩字号、没有改边距、没有改自动流。
口径（脚本文件头也写着）：PyMuPDF 取每页文字块 bbox 最大 `y1`，底线 = 841.89 − 10mm = 813.54pt；
两个百分比都给（占整页 841.89 与占底线 813.54）—— 只给一个的话，两种口径看着都合理。

### 阶段 4 结果（一句话）

`版本说明-投递前先看这个.txt` 已**覆盖**为第 10 版（文件头写明「取代本文件此前全部版本」），
内容含：投递用哪个 PDF + 生成命令、第 10 版做了什么（逐条）、
**为什么 MCP 条不能再指原仓**、被撤下的内容去哪了、两条必背话术、
版式容量实测、仍然要自己核实的 5 项、配套材料口径已过期的提醒。

### pytest 条数变化：逐条说明（§8 第 12 项要的那张表）

数字不是估的：三个时点各自 `git archive` 到临时目录、用同一个解释器
`pytest --collect-only -q` 点出来的（口径 = 收集数，不是「跑了多少条」）。

| 时点 | 提交 | 收集数 | 这一笔是什么 |
|---|---|---|---|
| 阶段 0 基线 | `旧编号·F` | 530 | — |
| 词表外置那一笔 | `旧编号·B` | 558 | `+15` 交付级自检（15 项各一条，其中三条是参数化的）；`+12` 词表加载器；`+1` 仓库卫生的「豁免区」用例 |
| 阶段 2 判据收范围 | 工作区 | 563 | `+5` 钉住「作用域」的用例（含「雇主名可以通过、旧仓库名必须红」那条） |
| D-49 词表不在本机 | 工作区 | 564 | `+1` 把第 15 项加进「词表读不到必须判 SKIP」那张参数化表 |
| D-50 联网项判据写反 | 工作区 | 567 | `+3` 钉住第 4 项的**两侧**（HTTP 200 算成功 / 非 200 算没取到 / 没取到判 ERROR 不判 PASS） |
| D-51 核验脚本改写别人的产物 | 工作区 | 570 | `+3` 钉住 `ProductsPreserved`（字节还原含 CRLF / 新出现的文件要删掉 / ★ 负控：写不回去时必须报 `restored=False`） |

**两个环境的口径**（同一份工作区，同一版代码，各跑一次）：

| 环境 | 收集 | 通过 | 跳过 | 失败 | 退出码 | 量的哪一版 |
|---|---|---|---|---|---|---|
| 本机（仓外词表在位） | 570 | 570 | 0 | 0 | 0 | D-51 之后，即提交里的那一版 |
| 真克隆（无词表、无构建产物） | 570 | 550 | 20 | 0 | 0 | 同一版 |

550 + 20 = 570，两边相加对得上。**「570 条通过」这句话只在「仓外词表在位」的机器上成立** ——
换了环境，靠词表的那一批判 SKIP（不是通过），条数不会假装变多。详见 `DECISIONS.md` D-49。

★ 上面这张表的两个数都是**这一轮重新量的**（D-51 加了 3 条用例之后）：
本机 `python -m pytest -q` → `570 passed`；克隆里同一条命令 → `550 passed, 20 skipped`。
D-49 那一轮记的是 567 / 547+20，是同一件事在加这 3 条用例之前的读数，**保留不改**。

★ 克隆环境怎么造出来的（要能重跑）：`git ls-files -co --exclude-standard | tar` 把
跟踪文件 + 未忽略的新文件拷进一个**父目录里没有词表**的空目录，再把 `.git` 一起拷过去
（缺了 `.git`，管历史的那几条会红得跟代码无关）。放在桌面下是不行的 ——
同级回退会在桌面找到那份词表，"无词表"这个前提当场就不成立了（第一次就踩了这个）。
本轮实测用的是 `_clonetest/repo3`（构造脚本：`git ls-files -co --exclude-standard` →
`tar` → 解开到空目录 → 拷 `.git`），跑完即删 —— 那是个**临时夹具**，不是产物。

### 阶段 5 结果（一句话）

提交 `旧编号·J` 已推送；`--online` 跑满 15 项 **PASS 15 / FAIL 0 / SKIP 0 / ERROR 0（退出码 0）**，
闸门四步全过、pytest 567；公网 raw 与本地同一份、两侧各 0 命中（`reports/leak_fix_verify.md` 五行全 PASS）。
本轮**真跑了以前一直跳过的联网那一项**，当场抓到一处判据写反（取回成功被读成「没取到」）——
已修、已补用例，见 `DECISIONS.md` D-50。

### 阶段 5 抓到的两个真问题（都是「第一次真跑」才露出来的）

1. **真克隆一次**（`git clone`，父目录没有词表）→ `pytest` 5 条红：4 条是「词表不在」、
   1 条是「示例库还没构建」。其中第 15 项不是红得难看，而是**结论写反**
   （「没查」被读成「历史被重写过」）。已修，见 `DECISIONS.md` D-49。
   修完实测：克隆里 **544 通过 / 20 跳过 / 0 失败**。
2. **联网那一项**（`--online`）→ 第 4 项 ERROR。原因是调用处按「0 + str 才算成功」判，
   而取回函数成功时给的是 **HTTP 200 + bytes**。判据已搬进可测试的模块并补了三侧用例，
   见 `DECISIONS.md` D-50。

### 阶段 5 收尾（第三轮，2026-09-18）：按任务书「推送后再跑一次公网 raw 验证」，又抓到两个

两个都不是新功能，是**跑第二遍才露出来的**，所以记在这里。

1. **核验脚本顺手改写了闸门自己的两份报告。** 跑完 27，`git status` 里
   `reports/verify.md`（567 → 549 passed, 18 skipped）与 `reports/final_selfcheck.md`
   （PASS 9 → PASS 6/SKIP 3；交付级 15 项 PASS 13 → PASS 1/SKIP 14）**被刷成了降级版**。
   根因：27 的「降级演示」那一步会真的跑闸门第 4 步，而第 4 步内部会跑
   `19_verify.py` 与 `pytest`，这两样正是那两份报告的生成者 —— 于是它们是在
   「本机没有词表」的环境下被重写的。**修法**：`verify_kit.ProductsPreserved`
   在跑之前按字节存、跑完原样放回，并把前后 sha256 记进报告。见 `DECISIONS.md` D-51。
2. **「公网取法」那一行是逐次可变的读数**（`curl.exe` / `urllib` 三次跑出两种结果），
   留在产物里等于这份报告不可逐字节复现 —— 同 D-38 拒绝时钟读数的理由。
   **修法**：产物里只留取法**策略**，哪一种成功只打控制台。见 `DECISIONS.md` D-52。

修完实测（口径 = 命令 + 脚本）：

| 命令 | 退出码 | 实测 |
|---|---|---|
| `python tools/check.py`（已设 `DELIVERY_RESUME_DIR`） | **0** | 4 步全过；pytest **570 passed**；19_verify 32 条 PASS 29/FAIL 0/SKIP 3；25 交付物 8 条全过；26 收尾 9 项全过 + 交付级 15 项 **PASS 13 / SKIP 2**（那 2 项要联网） |
| `experiments/27_leak_fix_verify.py`（连跑两次） | 0 / 0 | 五行全 `PASS`；产物 sha256 前 16 位 `549d5cba964e6934`，**两次逐字节相同** |
| 跑完 27 之后的 `git status --short` | — | **只有** 27 自己的产物 + 三份源码；两份属于闸门的报告**不再被改** |

★ 没设 `DELIVERY_RESUME_DIR` 时闸门照样退出码 0，但交付物那 6 项会判 `SKIP`
（实测 `PASS 6 / SKIP 9`）—— 那是**如实**的：交付物不在本机时本项没有实测依据。
要复现本文件里那组数字，得把那个环境变量指到简历目录。

### 历史残留收尾（第四轮，2026-09-18）：只动文档，判定结论不变

这一轮**一行代码都没改**，改的是三处文字：

1. `BLOCKERS.md` 卡点 4 加**第四轮补记**（外部前提变了、两个口径的对照、甲与乙都不执行的三条理由）；
2. `DECISIONS.md` 新增 **D-53**（不重写历史的决策 + 两条解锁条件）；
3. 仓外简历目录里三份文件的**顶部标注 / 补记**（版本号不变、正文未改、未重新导出 PDF；
   那三份不在本仓台账里，路径由 `DELIVERY_RESUME_DIR` 指过去）。

★ 一句话说清这一轮的性质：**结论没变（走丙：不重写历史），变的是「为什么」和「什么条件下才能改」。**
外部前提是那个与本仓同源的存量索引仓已转为 private —— 判别方式与后果写在卡点 4 的补记里，此处不复述。

#### 任务书 §5 那九项，逐项实测输出（口径 = 命令 + 脚本）

| # | 命令 / 动作 | 退出码 | 实测输出（照抄，不是结论） |
|---|---|---|---|
| 1 | `git status --short`（动手之前） | 0 | 只有 ` M BLOCKERS.md` 与 ` M DECISIONS.md`；**`reports/` 下没有产物被改**（D-51 生效） |
| 2 | `python tools/check.py`（已设 `DELIVERY_RESUME_DIR`） | **0** | 4 步全过；pytest **570 passed**；19_verify 32 条（PASS 29 / FAIL 0 / SKIP 3）；25 交付物 8 条 PASS 8；26 收尾 9 项全过 + 交付级 15 项 **PASS 13 / SKIP 2** |
| 3 | `python experiments/26_final_selfcheck.py` | **0** | 9 项：PASS 9 / FAIL 0 / SKIP 0 / ERROR 0 |
| 4 | `python experiments/28_delivery_selfcheck.py --online` | **0** | 15 项：PASS 15 / FAIL 0 / SKIP 0 / ERROR 0；第 13 项本地与远端同为 `旧编号·N`；第 12 项 570 条通过；第 14 项 2 页；第 15 项负控 50 条 / 88 次 |
| 5 | `python experiments/27_leak_fix_verify.py`（**关键回归**） | **0** | 五行全 `PASS`；跑完 `reports/leak_fix_verify.md`、`reports/verify.md`、`reports/final_selfcheck.md` **都没被改** |
| 6 | `git log --all -p -- BLOCKERS.md DECISIONS.md` 扫全词表 | 0 | 补丁 **2632** 行、命中 **1** 行 —— 那一行是**旧提交** `旧编号·M` 的 `commit` 头，命中的是**数字型条目**在 40 位十六进制里撞车（同 `VERIFIED_COINCIDENCES` 那一族），**不是本轮新增的文字**；本轮三份工作区文件扫出来是 **0 / 0 / 0** |
| 7 | 提交两个 + `git push origin main`（**普通推送，不是 force**） | **0** | `旧编号·O`（台账：卡点 4 补记 + D-53）、`旧编号·P`（产物：闸门自己的三份报告按本轮实跑刷新） |
| 8 | 推送后逐字节核对（工作区 / 仓库 blob / 公网 raw） | **0** | 5 个文件**三方 sha256 相同**，HTTP 200，见下表 |
| 9 | 本文件这一节 | — | 即本节 |

#### 第 8 项那张表（三个 sha256 都相同才写一行）

| 文件 | sha256 前 8 位（工作区 = 仓库 blob = 公网 raw） | 字节 |
|---|---|---|
| `BLOCKERS.md` | `1f624100` | 27698 |
| `DECISIONS.md` | `306b4e19` | 95714 |
| `reports/deliverable_check.md` | `8ee96b8c` | 4050 |
| `reports/delivery_selfcheck.json` | `34250990` | 7898 |
| `reports/delivery_selfcheck.md` | `3b85cb31` | 10264 |

★ 为什么只写 8 位：这份表最初写的是 16 位，结果本文件的自检**报出 1 条命中** ——
`DECISIONS.md` 那份 sha256 从**第 9 位**起就碰巧含了一个数字型条目（实测 9 / 10 / 12 / 16 位都命中，
8 位不命中）。**没有改数字、没有放宽判据**，只是把这一列统一改成 8 位前缀。

#### 第 8 项顺手带出来的一件事：工作区那 5 个文件当时是 CRLF

第一次核对时，公网 raw 与**仓库里那份**逐字节相同，与**工作区**却不同，差的正好是行尾字节：
本机 `core.autocrlf=true`，而仓库的 `.gitattributes` 声明的是 `* text=auto eol=lf`。
把这 5 个文件按 LF 重写之后：

- `git hash-object --path=<文件> <文件>` 与 `HEAD:<文件>` 的 blob 哈希**逐字相同**（`旧编号·Q`）
  —— 说明改掉的只是行尾，**内容一个字没动**；
- 重写完 `git status --short` 一度报出 5 个 ` M`，那是索引的 stat 缓存没刷新；
  `git add` 之后重算的 blob 哈希不变、状态回到干净 —— **它从头到尾就不是内容差异**；
- 这件事**只发生在本机工作区，仓库里那份自始至终是 LF**，公网 raw 也一直是 LF。

#### 这一节自己也带出了一处联动（照实刷新，没有放宽判据）

把上面这些命令写进台账之后，交付物核验的 **G4**（全仓文档里的命令条数）从 **62** 变成 **66**，
**G7**（引用的报告数）仍是 **24**；`reports/deliverable_check.md` 与
`reports/delivery_selfcheck.json` / `.md` 已在同一次提交里照实刷新，
其中交付级第 13 项记的是**跑那一刻**的读数：本地与远端同为 `旧编号·P`。

#### 第三笔推送（就是本节这一笔）也核了一遍

| 文件 | sha256 前 8 位（工作区 = 仓库 blob = 公网 raw） | 字节 |
|---|---|---|
| `PROGRESS.md` | `767d60f6` | 61844 |
| `reports/deliverable_check.md` | `501078b4` | 4050 |
| `reports/delivery_selfcheck.json` | `356e5847` | 7898 |
| `reports/delivery_selfcheck.md` | `c89bb71e` | 10264 |

★ 第三笔是 `旧编号·R`（这一节 + 那三份报告的联动刷新）。推送之后
`git rev-parse HEAD` 与 `git rev-parse origin/main` 都是 `旧编号·R`，工作区干净；
四个文件按上表三方逐字节相同。**交付级第 13 项在产物里记的仍是它跑那一刻的读数**，
理由同本文件前面那条：报告自己一被提交，HEAD 就往前走了。

#### 关于 sha 的书写纪律（这一轮新加的）

本文件里历史 sha **一律写 7 位缩写**。理由不是省字：完整 40 位十六进制里可能**碰巧**含有
词表里的数字型条目 —— 上面第 6 项那唯一一行命中就是这么来的（旧提交自己的 `commit` 头，
不是谁写进去的）。把 40 位往台账里抄，等于往台账里塞一条会自我命中的字符串。

### §8 收尾自检十五项 → 落在哪个文件

| 项 | 落在哪 |
|---|---|
| 1、2、3、5、6、7、8、9、10、12、14、15 | `reports/final_selfcheck.md` 的「交付级自检（15 项）」一节（离线视图；第 4、13 项在那里判 `SKIP`） |
| 4（公网 raw）、13（远端 sha == 本地 HEAD） | 要联网：15 项跑全的那一份在 `reports/delivery_selfcheck.md`，专项实测在 `reports/leak_fix_verify.md` |
| 11（闸门四项全过） | `reports/final_selfcheck.md` 第 11 项；15 项跑全时的「本项自己跑出来的」那一份在 `reports/delivery_selfcheck.md` |

### 推送与远端核对（第三轮）

**第二笔推送后的核对（§5 要求）**：GitHub API
`repos/Hone125/mcp-guarded-toolkit/commits/main` 的 `sha`、本地 `HEAD`、
`origin/main` 三者同为 `旧编号·S`（**相同**）。

**第三轮的推送与核对**：

| 项 | 值 |
|---|---|
| 推上去的 | `旧编号·S → 旧编号·T`，**2 个提交**（本轮源码与台账 / G4 条数刷新） |
| 命令与退出码 | `git push origin main`，退出码 0 |
| 本地 `HEAD` | `旧编号·T` |
| `origin/main` | `旧编号·T` |
| GitHub API 的 `sha` | `旧编号·T` |
| 判定 | **三个相同** |

**推送之后又跑了一次公网 raw 验证**（§5 那一条，第三次跑）：

| 项 | 实测 |
|---|---|
| `experiments/27_leak_fix_verify.py` | 五行全 `PASS`，退出码 0 |
| 工作区 vs 公网 raw 的 sha256 | 同为 `12bc32b05808…`（**同一份**） |
| 负控（`旧编号·F`） | 50 条 / 88 次（未变） |
| 跑完之后 `git status --short` | **无输出** |

★ 最后那一行是这一轮修完 D-51 之后**新有的**性质：以前跑一次核验脚本，
工作区必然脏两份降级报告；现在跑完还是干净的 —— 也就是说
「跑完还是干净的」这句话，对这一条核验路径也成立了。

**随后还有一笔**（台账与交付级报告的刷新，`旧编号·T → 旧编号·U`）。它之后的三方核对
仍然是三个相同：本地 `HEAD`、`origin/main`、GitHub API 的 `sha` 同为
`旧编号·U`，工作区干净。

★ **上面这两组 sha 都是「生成那一刻」的读数**：交付级自检第 13 项比的正是
「本地 HEAD 与远端 main」这一对，而报告本身随后又被提交一次 ——
所以报告里那个 sha 与提交之后的 HEAD 不同属**正常**，不是不一致。
要判断现在一不一致，重跑一次 `experiments/28_delivery_selfcheck.py --online`，
而不是拿这里的字符串去比。**保留旧读数、不改写**，是本仓台账的一贯做法。
## 阶段 0：核基线（2026-09-18，实测）

核基线是为了让后面每一步都有「相对什么变了」的参照。**没有基线就没有负结果可言** ——
「工具数从 8 变成 6」这句话，只有在知道原来是多少、谁量的时候才有意义。

| 核验项 | 命令 | 实测值 |
|---|---|---|
| 本地 HEAD | `git rev-parse HEAD` | `旧编号·E` |
| 远端 `main` | `git rev-parse origin/main` | `旧编号·E`（**与本地相同**） |
| 工作区 | `git status --short` | 空（干净） |
| 闸门 | `python tools/check.py` | 4 步全过，退出码 0 |
| 单元测试 | `python -m pytest -q` | 530 passed / 0 skipped，退出码 0 |
| 台账式自检 | `python experiments/19_verify.py` | 32 条：PASS 29 / FAIL 0 / SKIP 3 / ERROR 0，退出码 0 |
| 交付物核验 | `python experiments/25_deliverable_check.py` | 8 条：PASS 8 / FAIL 0 / SKIP 0，退出码 0 |
| 收尾自检 | `python experiments/26_final_selfcheck.py` | 9 项全部通过，退出码 0 |
| 原仓（只读） | 对原仓目录跑 `git status --short` 与 `git rev-parse HEAD` | 输出为空；HEAD 仍为 `旧编号·V` |
| 仓外词表位置 | `git -C ../ rev-parse --show-toplevel` | `fatal: not a git repository` —— 确认不在任何 git 仓里 |

**基线这一栏的用处**：阶段 1 做完之后 `pytest` 的条数会变（多出机械钉住词表位置的新用例），
**每一处增减都要在阶段 5 逐条说明**，而不是把新数字往这里一贴了事。

---

## 选择器标签收成一处（2026-09-19）

**这一轮改什么**：交付物检查按**文件名里的版本标签**认交付物，那个标签原来写死在 9 处
（3 个文件），简历换到新版之后一处没改 —— 于是第 5~10 项与第 14 项一直去查**已作废的那一份**，
而且**照样报绿**。本轮把标签收成**一处定义**（`mcp_server/delivery_selfcheck.py` 的 `RESUME_TAG`），
其余 8 处改为引用它。**判定口径一个字没改**（带该标签的必须恰好一个，0 个或多个都报错）。
理由、取舍、不做环境变量覆盖的原因见 `DECISIONS.md` D-55。

### 改之前实测到的病灶（未改动的代码）

```
python tools/resume_fillrate.py
文件  : 刘玉良-大模型应用开发-4.0.pdf（508265 字节）
| 1 | 19 | 740.3 | 88% | 91% | 73.2 |
| 2 | 23 | 793.0 | 94% | 97% | 20.5 |
页数 = 2
```

4.0 与 5.0 并存时它选中 **4.0** —— 读数还是绿的，从报告上看不出任何异常。

### 改之后同一条命令

```
文件  : 刘玉良-大模型应用开发-5.0.pdf（508495 字节）
| 1 | 19 | 740.3 | 88% | 91% | 73.2 |
| 2 | 23 | 793.0 | 94% | 97% | 20.5 |
页数 = 2
```

两个 `y1` 读数与上一版**逐点一致**（这一版只改了一行措辞，没动版式）。

### 红先绿后的新用例

新增 3 条到 `tests/test_delivery_selfcheck.py`，**真调选择器**（不 `monkeypatch`）：
`3 failed, 33 deselected` → `36 passed`。改前那三条里最直接的一条是「目录里只有 4.0 时」——
未改动的选择器**把它当成交付物返回了**（`assert WindowsPath('…-4.0.html') is None` 不成立）。

### 验证（本机实测，逐条照抄）

| 项 | 命令 | 实测 |
|---|---|---|
| 收集条数 | `python -m pytest --collect-only -q` | 604（本轮开工前基线 601） |
| 单元测试 | `python -m pytest -q` | **604 passed**，退出码 0 |
| 闸门 | `python tools/check.py` | 4 步全过、退出码 0：1 `pytest` 604 passed ｜ 2 `19_verify` 32 条 PASS 29 / FAIL 0 / SKIP 3 ｜ 3 交付物核验 8 条 PASS 8 ｜ 4 收尾自检 9 项全部通过 |
| 收尾自检 | `experiments/26_final_selfcheck.py` | 9 项全部通过；交付级 15 项 PASS 13 / FAIL 0 / SKIP 2 —— **那 2 项是「没跑」，不是通过** |
| 交付级 15 项（联网） | `experiments/28_delivery_selfcheck.py --online` | PASS 15 / FAIL 0 / SKIP 0 / ERROR 0，退出码 0 |
| 填充率 | `tools/resume_fillrate.py` | 量的是 **5.0.pdf**；740.3 / 793.0；2 页 |
| 泄漏复核 | `experiments/27_leak_fix_verify.py` | 5 行 PASS，退出码 0 |
| 选择器直读 | `resume_html()` / `resume_pdf()` | 返回的都是 **5.0** 那一份（`RESUME_TAG = 5.0`） |

### 条数与产物的变动，逐条说明（不许静默吸收）

1. `pytest` **601 → 604**：新增 3 条回归用例。为什么原来没有：现有夹具把
   `resume_html`/`resume_pdf` 整个 `monkeypatch` 掉了，**选择器自己长期没人测** ——
   这正是这个坑能一直绿着的原因。**没有删任何用例。**
2. 本轮之前那次「598 passed + 3 skipped」与本轮「604 passed」的差**不是行为变化**：
   那一次没设 `DELIVERY_RESUME_DIR`，凡依赖交付物的用例诚实地判了 SKIP；本轮设了，
   它们真的跑了。口径照旧：**SKIP ≠ PASS**。
3. 退出码：本轮 `pytest` 退出码是 **0**。本机那个安全钩子偶尔会拦住临时目录清理，
   造成「断言全绿、退出码 1」；真出现时按 D-42 记原始读数，**不写成 FAIL、也不写成 PASS**。
4. `reports/` 三份产物由脚本重跑刷新（D-51，不手改）：`verify.md` 的 P1 601 → 604；
   `deliverable_check.md` 的 G4 命令条数 **67 → 71**（本轮新增的引用共 **4 条**，
   逐条都确认过脚本与参数真的存在：本节验证表的 `tools/check.py`、本节填充率的
   `tools/resume_fillrate.py`、`DECISIONS.md` D-55 的 `tools/resume_fillrate.py`、
   以及**下面第 6 点自己写进去的那条** `experiments/25_deliverable_check.py`）。
   ★ 最后那一条是**记账行为自己带来的 +1**，特此写明，不静默吸收；
   同一份报告里 G7 的引用数 **24 → 25**（第 6 点提到了 `reports/deliverable_check.json`，
   取证时它确实存在，所以「文件存在」那一档 20 → 21）；
   `leak_fix_verify.md` 里随之更新的 sha。
5. `28 --online` 报告里两边指纹从 `511e067dcf8f` 变成 `3ee187253402`（**指纹不是提交号**，
   理由见 `selfcheck.sha_fingerprint`）：原因是 HEAD 比上一轮前进了一笔（远端那一笔改的是
   README，本地这一笔是本轮改动）。**本地与远端仍相同**，第 13 项照旧 PASS。
6. ★ `reports/deliverable_check.json` **此前一直没跟上**（2026-09-19 复核时发现）。
   它停在初始提交，内容还是「25 条命令」、G5/G6 判 `SKIP`「README.md 尚未创建」——
   而仓库早就有 README、命令数也早不是 25。原因是 `experiments/25_deliverable_check.py`
   的 `--json` 是**可选开关**，而 `tools/check.py` 调它时**不带**这个开关，
   于是 `.md` 每轮都刷、`.json` 永远不动。本轮补跑一次
   `python experiments/25_deliverable_check.py --json` 让它对齐。
   它**不被任何文档引用**（G7 扫的是文档里出现的报告名，扫不到它），所以此前没有让红线变红；
   但它是公开仓里一份**内容与现状相反**的产物，读的人会先信它。

### 同批做的台账校准（旧读数一律保留在批注里，不覆盖）

`BLOCKERS.md` 开场白的用例数与卡点数（三个 → 四个）、卡点 4 的两处指向 2026-09-19 的迁移记录、
`PROGRESS.md` 阶段表第 8 行、上一轮阶段清单的三行、`DECISIONS.md` 第 5 节那条填充率输入。

### 推送之后（2026-09-19）：本轮的这一笔已上远端，并逐字节复核过

- 推送用的是**普通快进推送**（`git push origin main`）：没有强推、没有历史重写、
  没有动远端仓库的任何设置。
- 推送后按 **D-52** 核对：7 个文件（改动的两个源码 + 一个测试 + 三份台账 +
  交付级自检报告）的 **blob sha 本地与远端逐一相同**；读远端用的是 GitHub API，
  不用 raw CDN（它有几分钟缓存，上一轮上过当）。
- 两边指纹相同 → 第 13 项 PASS。本小节**不引用提交号**，
  理由见 `selfcheck.sha_fingerprint`（写了就会自己过期）。

---

## 交付物换版 5.0 → 6.0：改的是一行字，牵动的是版本号那一处

这一轮**不是代码轮**，是交付物轮：简历从 5.0 换到 6.0，仓库这边跟着动的只有
「交付物选择器」那一行与几处门面读数。改动逐条记在下面，**旧读数一律保留**。

### 一、`RESUME_TAG`：5.0 → 6.0（唯一一处版本标签）

交付物文件名里的版本标签只在这一处定义（D-55 收的），换版就只改这一行。
改完之后这套检查去找的是 `刘玉良-大模型应用开发-6.0.html` / `.pdf`，
**恰好一个**才算过（0 个或多个都报错；判据一个字没改）。

★ 换版真正要防的不是「找不到」，是「**找错**」：5.0 与 6.0 同时躺在那个目录里，
标签写错一格，第 5~10 / 14 项就会去查**上一份**，而且照样报绿 ——
这正是 D-55 的那条实测（当时它只找到 4.0）。所以本轮改完，
**第 14 项报出来的文件名是 6.0 那一份**，这就是「没查错」的证据。

### 二、6.0 那份交付物实测（读数由脚本产出，不手抄）

- 6.0.pdf = **512,267 字节**（5.0 是 508,495），页数 **2**，
  `/Type /Page` 计数与 PyMuPDF 两边一致；sha256 前 12 位 `bb18d9e4b958`。
- 填充率（口径见 `tools/resume_fillrate.py`）：第 1 页 max y1 **740.3pt**（余 73.2）、
  第 2 页 max y1 **805.7pt**（余 **7.8pt**）。第 1 页与 5.0 **逐点一致**；
  第 2 页比 5.0 多 **12.7pt，正好一行** —— 新增的那半句把 facts 那段顶成了三行。
- ★ 余量 7.8pt 是**这一版的真实边界**：第 2 页已经没有再加字的空间。
  这句话写在这里，是因为下一轮想加字的人应该先看到它，而不是先去量。
- 交付物正文只改了**一行**（MCP 条 facts 里加一个括注）。逐行 diff 自证过：
  166 行对 166 行，差异行数 **2**（一行删＋一行增 = 同一行的改写）。
  换行符与 BOM 状态与 5.0 一致（LF、无 BOM）—— 这一条是单独验的：
  第一次生成时**误加了一个 BOM**（字节数对不上才发现），已改回。

### 三、门面读数：`RESULTS.md` 的那一格是**手抄的**，它过期了

`RESULTS.md` 里「单元测试」那一行写的是 `530 passed`，而实测是 **604**。
**那张表里别的数字都由脚本产出，唯独这一格是手写的。**

| | |
|---|---|
| 这一格的历史值 | 530（阶段 5）→ 543 → 570 → 598＋3 skipped → **604** |
| 谁会改它 | 每加一条用例它就变一次，而**没有任何一处会去校它** |
| 机器为什么没抓到 | `mcp_server/verify_kit.py` 的 D2 只查 `README.md` / `RESULTS.md` **在不在**，不校里面写的数 |
| 影响面 | `RESULTS.md` 是 `README.md` 直接链过去的那一份 —— 全仓「每个数字可复算」**门面自己差一个数** |
| 本轮处置 | 改成实测的 604，把旧值与它的来历写在同段里（**不覆盖、不静默**） |

★ 这条值得单独记：**手抄的读数不会因为别人改了而变红，只会越来越假。**
凡是「由人抄进文档、没有脚本校」的数字，都属于这一族。

### 四、`LICENSE` 的外壳拆了：中文补充挪进 `NOTICE.md`

`LICENSE` 原来是「MIT 全文 ＋ 一段中文补充（这条许可只覆盖代码，数据另有出处）」，
后果是托管站点**认不出这是 MIT**，仓库页面上显示成 `Other`。

- 中文那段挪进 `NOTICE.md` 的「本仓库自己的代码」一节 —— 那里本来就该讲这件事；
- ★ 挪的不是文件，是**生成它的那个常量**：`NOTICE.md` 由
  `experiments/03_write_notice.py` 的 `HEADER` / `FOOTER` 生成（手改会被下一轮覆盖），
  所以改的是 `FOOTER`，然后**重跑那个脚本**产出新文件。这一条是纪律，不是细节。
- `LICENSE` 现在只剩 MIT 全文（尾部空行收干净），内容一个字没删。

### 五、本机跑闸门这次的读数（照实报，含一条环境噪声）

| 步 | 结果 |
|---|---|
| 1 单元测试 | 进程退出码 **1** —— 但那是环境的批量删除钩子，**不是测试失败**：junit 报 `tests=604 failures=0 errors=0 skipped=0` |
| 2 台账式自检 | 32 条 **PASS 28 / FAIL 1 / SKIP 3**；那 1 条红是 P1「pytest 退出码 0」—— **上一条的级联** |
| 3 交付物核验 | **8 条全过**（G4 71 条命令、G7 25 处引用，与上一轮相同） |
| 4 收尾自检 | 9 项之外，交付级 15 项 **PASS 11 / FAIL 1 / SKIP 3**；红的是第 12 项「单元测试退出码 0」—— **同一条级联** |

★ 处置照 D-42 的先例：**先分类，再推回绿**。本轮没有把这份红快照写进仓库 ——
`reports/verify.md` 与 `reports/final_selfcheck.md` 保留上一轮的绿读数，
本轮的读数（含钩子原文与 junit 结论）记在这一节里。
理由很直接：这两个文件是**公开交付物的一部分**，把一条由本机钩子造出来的红留在里面，
读的人只会得到「这个仓库的闸门是红的」这个错误结论。

★ 判「测试到底有没有过」的唯一可靠信号是 **junit xml 的 `failures` / `errors`**，
不是进程退出码，也不是去数进度行里的字母 —— 钩子自己那行提示里就带着 `F` 与 `E`。

### 六、这一轮**没有**动的东西

- 没有改任何业务代码（`mcp_server/` 只有 `RESUME_TAG` 那一行）；
- 没有动历史、没有强推、没有改远端仓库的任何设置；
- 没有为这次门面改动新加 D 条目或卡点 —— 它是执行，不是新判断。

## 第五轮：HTTP 服务层与 asyncio 并发层（2026-09-22）

**一句话。** 在不另起项目、不改上游已有代码的前提下，给**同一批 6 个工具**加了一层 HTTP 入口
（`mcp_server/http_api.py`）和一层 asyncio 并发调用（`mcp_server/concurrency.py`），
并且把「并发」从**设定值**变成了**实测计数**：串行 1 / 天真 gather 1 / 正确并发 6。
需要模型凭据的那条链路又在并发下重跑了一遍账本对账（两批各 12 题，行数对得上）。

### 一、这一轮加了什么

**新增**（一条旧逻辑都没改；`mcp_server/` 下三个新文件全是新名字）：

| 文件 | 是什么 | 判据（谁在守它） |
|---|---|---|
| `mcp_server/dispatch.py` | 名字 → 工具函数的派发器；唯一真值源是 `auth.TOOL_SCOPES` | `tests/test_dispatch.py` 29 条，含「派发器认识的名字集合 == `TOOL_SCOPES` 键集合」与一条反证（把工具函数打桩成炸弹，无令牌调用仍须返回拒绝） |
| `mcp_server/http_api.py` | FastAPI 入口：6 条工具路由 + `/tools` + `/healthz` + OpenAPI 文档 | `tests/test_http_api.py` 23 条 + `experiments/30_http_smoke.py`（真子进程、真端口）20 项 |
| `mcp_server/concurrency.py` | 限流（`Semaphore`）+ 超时降级（`wait_for`）+ 阻塞调用挪出事件循环（`to_thread`） | `tests/test_concurrency.py` 15 条，含「实测峰值 == limit」与「`offload=False` 时峰值退化成 1」 |
| `experiments/29_concurrency_bench.py` | 三种跑法的并发度对照 | 退出码 0；`reports/concurrency.{md,json}` 两遍逐字节一致 |
| `experiments/30_http_smoke.py` | HTTP 入口冒烟 | 退出码 0；`reports/http_smoke.{md,json}` |
| `experiments/31_ledger_under_concurrency.py` | 并发下的账本对账（需要模型凭据） | 退出码 0（本轮真跑通）；`reports/ledger_under_concurrency.{md,json}` |

**修改**（只有这几处，且都是加法）：`requirements.txt`（显式钉住三个新依赖 + 传递依赖注释）、
`README.md`（快速开始第 4/5 步、起服务端、HTTP 入口小表、目录结构、完成状态）、
`RESULTS.md`（4 行新读数 + 校准两个过期的手抄读数）、`DECISIONS.md`（D-56）、
`mcp_server/verify_kit.py`（`EXIT_LEDGER` 追加 29/30/31 三条）。
`BLOCKERS.md` 本轮**没有**新增卡点 —— 卡点 1 那条「凭据与发布自检互斥」用的是它自己写的最小动作
（进程环境变量注入），没有新的阻塞项。

### 二、逐阶段记录（判据一律先写、后跑）

| 阶段 | 做了什么 | 判据 | 实测 |
|---|---|---|---|
| 0 环境与基线 | `uv` 建 venv、装 `requirements.txt` | 基线 `pytest` ≥ 604 / 0 失败 | 基线 **604 passed**；`git check-ignore -v .env` 命中 `.gitignore:2` |
| 1 派发器 | `dispatch.py` | 名字集合与 `TOOL_SCOPES` 相等；无令牌 dispatch 不得碰到业务代码 | 29 条用例全绿（其中反证那条：炸弹被打桩后仍返回拒绝） |
| 2 HTTP 入口 | `http_api.py` | 冒烟 20 项全过；`--check` 退出码 0 | **20 / 20**（19 条用例 + 1 条边界：stdout 里一行人话都没有） |
| 3 并发层 | `concurrency.py` | 峰值计数、超时降级、异常隔离、长度不变式 | 15 条用例全绿，连跑 5 次稳定 |
| 4 并发实测 | `29_concurrency_bench.py` | A 峰值 1、C 峰值 == limit；B 那一行**不许删** | A **1** / B **1** / C **6**；产物两遍 sha256 相同 |
| 5 账本对账 | `31_ledger_under_concurrency.py` | 账本新增行数 == `usage.calls` 之和 | 并发批 **12 == 12**、串行批 **12 == 12** |
| 6 文档与闸门 | 本节 + `tools/check.py` | 退出码 0；`--snapshot` 跑闸门前后哈希相同 | 见第七节 |

**测试条数：604 → 671**（+67：派发器 29、HTTP 23、并发 15；**一条旧用例都没删**）。

### 三、A/B/C 并发对照：这一轮的核心读数

同一份活、同一个量具，三种跑法（`experiments/29_concurrency_bench.py`，读数落 `reports/concurrency.md`）：

| 跑法 | 做法 | 调用数 | 业务成功 | 实测峰值 |
|---|---|---|---|---|
| A 串行 | 逐个 await | 24 | 24 | **1** |
| B 天真 gather | `offload=False`：在事件循环里直接调同步函数 | 24 | 24 | **1** |
| C 正确并发 | `run_many(limit=6, offload=True)` | 24 | 24 | **6** |

**「实测峰值」是量出来的，不是推出来的。** 每条调用进出时过一个带 `threading.Lock` 的计数器，
取「同时在里面」的最大值；每条调用再背一个 **20 ms 固定延迟**当量具（真活照干，
延迟叠在上面）—— 没有它，一次查询在微秒级跑完，多个线程还没都站起来前一批就结束了，
峰值会变成调度噪声，同一份代码跑两遍能量出不同的数。

**B 为什么不提速**（这一行不许删，它是负结果）：那 6 个工具是**同步函数**，
在事件循环里直接调，事件循环就被占住，协程之间**根本没有让出点**，`gather` 于是退化成串行。
「设了 6、实际 1」这件事，不量就看不见 —— 这正是量具存在的理由。
**所以 `offload=True` 不是优化，是前提**；同一个函数一个开关，差别只有这一处。
另外三种跑法的**成功数与调用数都是 24** —— 提速不能靠少干活，这一栏必须一起报。

**超时降级**：受控探针里塞一条必然超时的调用，结果是 **1 条超时 + 23 条正常返回**，整批不塌。
代价照实写：`wait_for` 只能放弃 await，`to_thread` 里的线程杀不掉，会自己跑完
（用例里用 `threading.Event` 钉住「它确实跑完了」）。

### 四、并发下的账本对账（需要模型凭据，本轮真跑通）

`experiments/31_ledger_under_concurrency.py`：12 道题 × 2 批（先 `limit=6` 并发，再 `limit=1` 串行），
走 `dispatch` → `ask_database`。口径只认**行数**：一行账本 = 一次模型调用（重写重试各算一次）。

| | 并发批（limit=6） | 串行批（limit=1） |
|---|---|---|
| 调用批内成功 | 12 | 12 |
| 账本**新增行数** | **12** | **12** |
| `usage.calls` 之和 | **12** | **12** |
| 输入 token | 3017 | 3017 |
| 输出 token | 203 | 201 |

- **对账的是行数，不是 token**：输入侧逐字相同（同一批题、同一份 schema 提示），
  输出侧差 2 —— 模型本身有不确定性，**这一项不要求相等**；把它说成「一致」就是把噪声当证据。
- 报告里有一节是**构造性的，不算证据**：逐题 `attempts` 与账本行数的对齐同出一源（都是 `usage.calls`），
  真正的独立证据是账本**行数**这一列，所以那一节自己声明了不算数。
- ★ **`cost.LOCK` 在 `to_thread` 之下才真正成为必需品**：多个工作线程同时 `record()`，
  没有这把锁就会写坏行。它在串行时代就已经在，本轮才有人证明它**不是摆设**。
- 金额仍然**留空**，原因照抄（无单价条目），不填 0、不拿别家单价兜底。

### 五、新增依赖，以及「不许把编排外包出去」那条为什么还成立

| 包 | 实测版本 | 为什么是它 |
|---|---|---|
| `fastapi` | 0.141.1 | 路由 + 自动 OpenAPI（`/docs`、`/openapi.json`） |
| `starlette` | 1.6.0 | 直接用它的 `HTTPException` 改写 `/tools/` 下的 404 响应体 |
| `uvicorn` | 0.53.0 | ASGI 服务器；**服务端自己先 bind socket 再交给它**，端口确定 |
| `httpx` | 0.28.1 | 冒烟脚本与 `TestClient` 用（**开发/测试**依赖） |

- **`uvicorn` / `starlette` 本来就以 `mcp` 的传递依赖装着，这里显式钉住**：直接 `import` 的东西
  不该靠别人顺带装。这条纪律是 `deliverable_kit` 的 G8 抓出来的 —— `http_api.py` 里
  `from starlette.exceptions import ...` 一度只写在注释里，G8 直接判 FAIL（注释不算声明）。
- ★ **查出来的一个坑**：`openai` 3.x 的默认 HTTP 客户端**不是 `httpx`，是 HTTPX2**
  （`Requires-Dist: httpx2<3,>=2.7.0`，实测装到 2.13.0 + `httpcore2` 2.13.0）。
  证据是压日志噪声时压不掉：按 `logging.getLogger("httpx")` 设 WARNING，一行都没压掉，
  探针打出来的 logger 名字是 `httpx2`。两者都是纯 Python，所以 `requirements.txt` 里
  「有没有编译扩展」那句话的真假不受影响（那句话的真话是：`pydantic-core` 是 Rust 编译扩展）。
- **`fastapi` 与「刻意不要 langchain / agent 框架」不冲突**：那条针对的是**替你决定调哪个工具、
  按什么顺序调**的编排器。FastAPI 只是 HTTP 传输层，底下调的仍然是本仓手写的那 6 个函数，
  路由表照着 `auth.TOOL_SCOPES` 现生成，重写状态机仍然是 `t2sql_core.py` 那台。
  这一条已写进 `requirements.txt` 的注释与 `DECISIONS.md` D-56。

### 六、假设与偏离（任务书 vs 仓库实际）

| # | 任务书写的 | 仓库实际 | 处置 |
|---|---|---|---|
| A | 把 `.env` 复制到仓库根 | `experiments/26_final_selfcheck.py` 第 7 项用 `**/.env` 全仓 glob，**即使被 gitignore 也判 FAIL**；这条互斥早已记在 `BLOCKERS.md` 卡点 1 | **不落 `.env`**，改用**进程环境变量**注入（`env_file.apply()` 的规矩 1：显式环境变量永远赢过 `.env`）—— 这正是卡点 1 自己写的最小动作。于是「闸门退出码 0」与「阶段 5 真跑」同时成立，明文凭据不落仓库盘 |
| B | 「把新脚本登记进 `experiments/19_verify.py` 的退出码台账」 | 台账不在 `19_verify.py`，在 `mcp_server/verify_kit.py` 的 `EXIT_LEDGER`（19_verify 只负责起子进程、收退出码、拼表） | 往 `EXIT_LEDGER` **追加**三条；不动 19_verify 的判定逻辑（只加，不松） |
| C | 「工具集表加 HTTP 端点」 | `deliverable_kit` 的 **G5** 会把 README 里所有 `\| 小写名 \| db:xx / kb:xx \|` 的行当成工具表**双向**比对 | HTTP 端点写成**独立小表**（表头 `方法 / 路径 / 说明`，路径写 `/tools/<工具名>`），**不新增**任何 `\| 名字 \| scope \|` 形态的行 |
| D | 文件清单里没有阶段 5 的脚本 | 阶段 5 需要一个入口脚本 | 新增 `experiments/31_ledger_under_concurrency.py` + 对应报告（只加不减） |
| E | 「起服务端」用 uvicorn 命令行 | 本仓需要「端口确定」这个性质给冒烟脚本用 | 改成 `python -m mcp_server.http_api`：**自己先 bind 好 socket 再交给 uvicorn**，并把真实端口打成一行到 stderr（`--port 0` 时由系统分配）。这样不存在「先探测空闲端口、启动时被抢走」的竞态 |

另：任务书列的阶段 0 第 4 步说 `data/` 下的库「可能需要重建」—— 实测**已构建**，
所以本轮没有重跑 `01_build_db.py`（本轮的分母都是现跑现造的临时库）。

### 七、§10 自检清单：16 项逐条读数

**这份表里没有一项是「应该会过」。** 判据先写死，读数照抄命令的输出；
闸门相关的三项（第 6、7、16 项）以最后一次跑闸门的结果为准。

| # | 检查项 | 判据 | 实测读数 |
|---|---|---|---|
| 1 | 单元测试 | `python -m pytest -q` 通过数 ≥ 604、失败 0、退出码 0 | **671 passed / 0 failed**，退出码 **0**（闸门第 1 步；`19_verify` 的 P1 同样 PASS） |
| 2 | 服务端自检 | `python -m mcp_server.server --check` 退出码 0，且仍注册 **6** 个工具 | 退出码 **0**；6 个：`ask_database, get_passage, get_schema, list_tables, run_sql, search_passages` |
| 3 | stdio 冒烟没被改坏 | `experiments/08_mcp_smoke.py` 15/15 | 台账 **X08 PASS**（退出码 0）；六处工具集合两两相等 |
| 4 | HTTP 冒烟全过且产物落盘 | `experiments/30_http_smoke.py` 退出码 0；`reports/http_smoke.{md,json}` 在 | 退出码 **0**；**20 / 20**（19 条用例 + 1 条边界：stdout 里一行人话都没有）；产物在 |
| 5 | 并发实测退出码 0 且产物落盘 | `experiments/29_concurrency_bench.py` 退出码 0；`reports/concurrency.{md,json}` 在 | 退出码 **0**；台账 **X29 PASS**；产物在，两遍 sha256 相同 |
| 6 | 守门链 | `python tools/check.py` 退出码 0 | 退出码 **0**，4 步全过（pytest / 19_verify / 25_deliverable_check / 26_final_selfcheck） |
| 7 | 产物可复现 | `--snapshot` 在跑闸门**前后**读到同一个哈希 | **两次哈希相同**（读数见本节末尾） |
| 8 | `GET /tools` 的工具数 | == 6，且名字与 `TOOL_SCOPES` 逐条一致 | **6 个**，scope 逐条一致（冒烟第 2、3 条） |
| 9 | HTTP 鉴权三态 | 无令牌 → **403 NO_TOKEN**；scope 不足 → **403 INSUFFICIENT_SCOPE**；齐备 → **200** | 三条都在，且响应体里没有业务字段（另加：伪造签名 403 `BAD_SIGNATURE`、未知工具 404、缺参数 400） |
| 10 | 被护栏拦下的 SQL | **200** 且 `blocked` 为真 | **200 + blocked=True**，且响应体里**没有 `rows`**（证明真没执行） |
| 11 | 实测并发峰值 == limit | C 的峰值等于 limit（6） | A **1** / B **1** / C **6** == limit；B 也报（不许删） |
| 12 | 超时降级 | 1 条超时 + 其余正常返回 | 受控探针 **1 超时 + 23 正常**（`n=24`），整批不塌 |
| 13 | 并发下的账本 | 账本新增行数 == 实际调用次数 | 并发批 **12 == 12**、串行批 **12 == 12** |
| 14 | 并发报告的口径与红线 | 有 `## 口径`；无小数秒；无绝对路径 | `## 口径` **1 处**；扫 `数字.数字 + 秒/ms`、盘符路径、`/Users/`、`/home/` 全部 **0 命中** |
| 15 | 凭据不落盘 | `git check-ignore -v .env` 有输出 | 命中：`.gitignore:2:.env` |
| 16 | 工作区干净 | `git status --short` 提交后为空 | 见下面的提交记录 |

**第 7 项的读数**（照抄命令输出，两次之间的那一步会重写 `reports/` 下的产物）：

```powershell
python tools/check.py --snapshot   # 记下哈希
python tools/check.py              # 跑闸门，重写 reports/ 下的产物
python tools/check.py --snapshot   # 应当还是同一个哈希
```

**本轮实测**（2026-09-22，最后一次收口跑）：

| | 读数 |
|---|---|
| 闸门前 `--snapshot` | `fe9d97bfdd09777d9c5ed81bbe9afbcc0874a7e86a0a834c3c1e4807df67cd2f`（47 个文件） |
| 闸门 | 退出码 **0**（4 步全过） |
| 闸门后 `--snapshot` | `fe9d97bfdd09777d9c5ed81bbe9afbcc0874a7e86a0a834c3c1e4807df67cd2f`（47 个文件） |

两次同一个哈希，意思是**同一份代码跑两遍，`reports/` 下 47 个产物逐字节一样** ——
也就是跑完闸门 `git status` 里的 `reports/` 不会再多出一处改动。这条不是自动成立的：
产物里只要混进一个时钟读数或一次端口分配，就会每跑一次都不一样（`tests/test_repo_hygiene.py`
静态守着这一条）。★ 上面那个哈希是**读数**，不是判据 —— 换一台机器、换一批文件它就会变，
判据是「前后两次相同」。

### 八、可复用简历表述（每条都指得出文件与数字）

- 「给一套本地 MCP 工具集补了 HTTP 服务层：路由照着鉴权表 `auth.TOOL_SCOPES` **现生成**
  （不是第二份工具清单），启动自检断言『路由名集合 == scope 表键集合』，不一致就拒绝启动；
  真实子进程 + 真端口的冒烟 20 项全过（`experiments/30_http_smoke.py`）；
  错误映射不一刀切 —— 拒绝 403、未知工具 404、参数绑不上 400，
  **被 SQL 护栏拦下仍是 200**（那是业务结论，不是传输失败）。」

- 「用 asyncio 给同一批**同步阻塞**工具做了并发调用层：`Semaphore` 限流 + `wait_for` 超时降级 +
  `to_thread` 把阻塞调用挪出事件循环。并发度是**实测**的：串行 1 / 天真 gather **1** /
  正确并发 **6**（`experiments/29_concurrency_bench.py`，24 条真调用、带锁计数器量峰值）。
  天真 gather 不提速的原因一并写进报告：同步 I/O 没有让出点，事件循环被占住，
  所以 `to_thread` 不是优化而是前提。15 条用例里有一条专门钉住『拿掉 `to_thread` 就红』。」

- 「在并发下复核了用量账本的一致性：12 题 × 2 批（并发 6 / 串行 1），
  两批**账本新增行数都等于实际模型调用次数 12**（`experiments/31_ledger_under_concurrency.py`），
  token 数按口径**不要求相等**并说明了理由；顺手证明账本里那把 `threading.Lock`
  在多线程下才是必需的。」

### 九、这一轮**没有**动的东西

- 没有改任何上游已有代码：`mcp_server/` 三个新文件是新名字，旧文件一个字没动；
- 没有 push、没有建 PR；没有碰 `legacy/history-2026-09-19`，也没有碰 `publish` 分支；
- 没有把凭据写进任何会被 commit 的文件（仓库里没有 `.env`，`git check-ignore` 仍命中）；
- 没有动 `_仓外留档`（只读），也没有把它复制进仓库；
- **没有删或弱化任何已有的负例与测试**（41 条护栏负例、36 条鉴权应拒、三条 LLM SKIP 全在），
  测试只增不减：604 → 671；
- 没有让耗时读数落进 `reports/`。
